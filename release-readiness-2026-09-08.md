# نتائج تنفيذ إصلاحات الجاهزية — 2026-09-08

هذا التقرير يحل محل تقديرات الجاهزية السابقة. نجاح الاختبارات لا يعني إغلاق كل شروط الإطلاق، ولا توجد حالياً أدلة تبرر 10/10.

## التغييرات

- نسخة أمان قبل التعديل: `D:\Programming\Faramace-safety-20260908-171816`، تشمل ملفات العمل الجديدة وGit؛ تستثني مجلدات الاعتمادات والبناء المؤقتة.
- أُخرج `apps/desktop/prisma/local.db` و`apps/desktop/dist-electron/main.js` من فهرس Git فقط. النسختان المحليتان موجودتان، ولا يحذف ذلك نسخهما من تاريخ Git. حذف بيانات حساسة من التاريخ يحتاج مراجعة منفصلة إذا ثبت وجودها.
- حارس الاعتمادات `scripts/check-dependencies.mjs` يقارن overrides في الملفات الثلاثة، ويفحص manifests السبعة مقابل lockfile، ويدعم فحص index بواسطة `--staged`. يسمح بتعديلات الوصف/scripts دون تغيير غير لازم للقفل.
- أُضيف hook في `.githooks/pre-commit` وفُعّل محلياً. تشغيل `pnpm hooks:install` يفعّله في نسخة أخرى. CI هو الحارس المركزي ولا يعتمد على تثبيت hook.
- workflow `.github/workflows/readiness.yml` يثبت pnpm 8.15.4 وNode 22، ويستخدم frozen lockfile ويشغّل الاختبارات والتكامل والبناء والتدقيق. لم يُدفع أو يُنفّذ على GitHub في هذه الجلسة.
- أصبحت دفعات المذاخر تقفل الفاتورة داخل معاملة، وتعيد الدفعة السابقة عند تكرار المفتاح، وترفض تغيير المبلغ أو الطريقة أو المرجع أو الملاحظات مع المفتاح نفسه. طلبان مختلفان يمكنهما تحديث الرصيد دون فقدان إحدى الدفعتين.
- طلبات B2B تحفظ بصمة الطلب الأصلي في حدث SENT وتستخدم قفلاً للمفتاح أثناء الإنشاء. إعادة الطلب المطابق تعيد المعرّف نفسه؛ تغيير الأصناف/الكميات/الأسعار/الملاحظات/التاريخ يُرفض بـ409. المفاتيح التاريخية التي لا تحمل بصمة تُرفض احترازياً بدلاً من تأكيد تطابق لا يمكن إثباته.
- تصحيح اختبار الدفع/الإلغاء حتى يرسل idempotencyKey صالحاً، وإضافة اختبارات تكرار الطلب والدفع وتغيير المحتوى. تحديث HTTP smoke ليرسل المفتاح ويفحص إعادة الطلب.

## تدقيق الأمن

`pnpm audit` التقليدي انتهت مهلته. `pnpm audit:prod` يستعلم الآن واجهة npm Bulk Advisory الرسمية باستخدام أسماء وإصدارات lockfile، ويشمل جميع مدخلات الإنتاج والمشتركة والاختيارية، ويستثني فقط `dev: true`. هذا فحص advisories للإصدارات، وليس مولّداً لخطة إصلاح npm أو عدّاداً لمسارات الاعتماد.

المصدر: https://docs.npmjs.com/cli/v11/commands/npm-audit/#bulk-advisory-endpoint

يحفظ التقرير الكامل وبصمة lockfile وقائمة الإصدارات في `artifacts/audit-prod.json`. رمز الخروج 0 يعني لا تحذيرات، و1 يعني تحذيرات موجودة، و2 يعني فحصاً غير مكتمل. لا توجد قائمة إعفاءات تخفي التحذيرات.

بعد إصلاح uuid وdecode-uri-component وفحص 1163 مدخلاً للإنتاج/المشترك: **0 حرج، 2 عالٍ، 0 متوسط، 0 منخفض**، محسوبة حسب GHSA الفريد. تحذيرا الصور ما زالا يظهران حسب رقم الإصدار، لكن أضيف لهما لاحقاً تصحيح محلي بطلب المستخدم؛ لا توجد إعفاءات في التدقيق.

تصحيح `patches/image-size@1.2.1.patch` يمنع أطوال ICNS غير المتقدمة أو المتجاوزة لطول الملف، ويرفض صناديق JXL/HEIF ذات رؤوس ناقصة أو أطوال أقل من الحد الأدنى، ويتحقق من طول رأس jxlp. يُطبّق آلياً بواسطة pnpm، ومسجل في manifests والقفل. نجحت الاختبارات الثمانية للصور المشوهة والسليمة على المكتبة التي يستدعيها Metro فعلياً، ونجحت اختبارات الحارس والتوافق العشرة الأخرى والتثبيت بالقفل المجمّد. لا يعني هذا اختفاء تنبيهات سجل npm أو تدقيق كل محللات الصور الأخرى. صناديق ISO ذات الطول الممتد أو الطول الصفري حتى نهاية الملف تُرفض صراحةً؛ لم يُضف دعم لها.

أُغلق تحذير uuid بترقية الاعتماد لدى ExcelJS وxcode إلى 11.1.1. أُغلق تحذير decode-uri-component بترقيته إلى 0.5.0 مع patch محفوظ لإضافة CommonJS للمستدعي query-string؛ خوارزمية الإصلاح الأصلية وواجهة ESM لم تتغيرا. نجحت اختبارات التوافق الخمسة، وبناء الويب، وتجميع Android بواسطة Metro/Hermes. لم يُبنَ تطبيق iOS الأصلي على Windows؛ اختبار xcode يغطي توليد معرّفات المشروع.

أظهر اختبار النسخة الفعلية مجلداً قديماً متبقياً لـuuid 8.3.2 داخل node_modules/exceljs رغم تحديث القفل. نُقل إلى `D:\Programming\Faramace-stale-uuid-20260908-180707` كنسخة قابلة للاسترجاع، وأكد الاختبار بعدها استخدام ExcelJS للإصدار 11.1.1 فعلياً.

| المتبقي | المسار المثبت في lockfile | سبب عدم فرض ترقية عمياء |
|---|---|---|
| image-size: تحذيران عاليان | metro → image-size 1.2.1 | أحدث dist-tag وقت الفحص 2.0.2 مشمول أيضاً بالنطاق المتأثر؛ لا يوجد إصلاح ضمن major 1 في السجل المفحوص |

هذه البنود لم تُعتبر آمنة تلقائياً. فحص CI الأمني سيظل غير ناجح حتى المعالجة أو مراجعة مخاطر معتمدة. تحذير peer الخاص بـlucide-react/React 19 باقٍ أيضاً.

## قاعدة جديدة والترحيلات

أثبت اختبار قاعدة فارغة أن تاريخ SQL القديم يفشل عند `20260225000000_add_suspension_fields`: جدول DeviceLicense لم يُنشأ قبله في التاريخ. لذلك لا يكفي تقرير «كل الترحيلات مطبقة» لإثبات إمكانية التثبيت الجديد.

أُضيف `scripts/bootstrap-empty-db.mjs` مع `scripts/database-baseline.json`: يتطلب `NEW_DATABASE_URL` صريحاً، ويفحص خلو القاعدة من الجداول والكائنات، ويثبت بصمات المخطط والترحيلات، ثم ينشئ المخطط المُراجع ويسجل الترحيلات السابقة بواسطة `prisma migrate resolve --applied`. لا يعدّل ملفات SQL التاريخية، ولا يُستخدم لترقية قاعدة موجودة. إذا تغيّر المخطط أو مجموعة الترحيلات، يتوقف ويطلب مراجعة baseline جديدة؛ لا يُحدّث البصمات تلقائياً.

تم اختبار رفضه لقاعدة الاختبار غير الفارغة قبل أي تغيير. قواعد هذه الجلسة جميعها PostgreSQL محلية؛ لم تُعدّل قاعدة العملاء أو يُنفّذ نشر إنتاجي.

## التحقق

- بعد تصحيح image-size: نجحت اختبارات الصور 8/8، وتجميع Android عبر Metro/Hermes (1806 وحدة) برمز خروج 0 إلى `.expo/image-security-export`. هذا تجميع حزمة JavaScript والأصول، وليس إنشاء APK أو اختباراً على جهاز فعلي.

- حارس الاعتمادات: ناجح؛ 5 اختبارات تشمل staged/unstaged والانحراف والبيانات الوصفية.
- تثبيت lockfile مجمّد دون شبكة: ناجح.
- الويب: 444/444.
- تكامل PostgreSQL: 22/22 بعد الإصلاحات وتحديث الاعتمادات.
- سطح المكتب: 34/34.
- TypeScript للويب والموبايل: ناجح.
- بناء إنتاج Next.js 15.5.25 في `.next-readiness`: ناجح برمز خروج 0 بعد تحديث الاعتمادات وإصلاحات التكرار.
- اختبار HTTP المحلي: حُدّث السكربت، لكن تشغيل خادم الاختبار رُفض بواسطة المراجعة الآلية بعبارة `blocked by policy` دون سبب تفصيلي؛ لم يُنفّذ الاختبار ولا يُحسب نجاحاً.
- تهيئة قاعدة فارغة عبر baseline: ناجحة، 28/28 مسجلة ولا ترحيلات معلّقة. مقارنة القاعدة الناتجة بالمخطط بواسطة `prisma migrate diff --exit-code`: لا اختلاف، رمز 0.
- استرجاع pg_dump إلى قاعدة منفصلة: ناجح؛ ظهرت 16 دفعة اختبار في القاعدة المسترجعة. هذا تمرين بيانات اختبار، وليس شهادة استعادة إنتاج.

## ما يحتاج إغلاقاً قبل اعتماد الإطلاق

1. تحذيرا image-size عولجا بتصحيح محلي واختبارات انحدار؛ يظل سجل npm يعرضهما لعدم تغير الإصدار. يلزم الاحتفاظ بالتصحيح والتحقق منه في CI حتى اعتماد إصدار رسمي مصحح، ولم يُخفيا أو يُستثنيا من التدقيق.
2. تشغيل workflow فعلياً بعد مراجعة التغييرات ورفعها، وتفعيل required checks في حماية الفرع.
3. Canary وHTTP smoke على بيئة النشر الفعلية، ومراقبة الأخطاء وتمرين rollback، والتحقق من نسخة احتياطية للإنتاج.
4. مراجعة وفرز التغييرات القديمة غير الملتزمة إلى commits مترابطة وقابلة للبناء؛ لم يُجمع عمل سابق غير مُراجع في commit شامل. النسخة الاحتياطية متوفرة قبل التعديلات الحالية.
