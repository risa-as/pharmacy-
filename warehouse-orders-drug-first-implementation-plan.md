# خطة تنفيذ طلب الأدوية حسب الاحتياج ومقارنة أسعار الموردين

تاريخ الخطة: 2026-09-10

المشروع: `D:\Programming\Faramace`

الصفحة المستهدفة: `/dashboard/purchases/warehouse-orders/new`

الحالة: خطة للتنفيذ بواسطة موديل آخر؛ لا تتضمن هذه المهمة تنفيذ الميزة.

## 1. الهدف وحدود التكليف

تحويل إنشاء طلب المذخر من «اختيار مذخر ثم تصفح كتالوجه» إلى «إدخال الأدوية المطلوبة وكمياتها ثم اختيار المورد المناسب لكل دواء».

متطلبات المستخدم المعتمدة:

1. يكتب صاحب المؤسسة أسماء الأدوية ويحدد الكميات.
2. يعرض النظام **أرخص سعر بين آخر أسعار الموردين للدواء نفسه**؛ وليس آخر شراء إجمالًا، وليس أقل سعر تاريخي على الإطلاق.
3. يمكن عرض آخر سعر لكل مورد وتاريخه، وفتح السجل التفصيلي للأسعار المتاحة.
4. يستطيع المستخدم تغيير المورد المختار لكل صنف قبل الإرسال.
5. تضاف إمكانية ربط المورد المحلي بالمذخر في صفحة السوبر أدمن.
6. تقسم القائمة إلى طلب `WarehouseOrder` مستقل لكل مذخر. تعرض شاشة مراجعة واحدة المجموعات قبل الإرسال.
7. لا ينشأ كيان أب جديد يجمع الطلبات ضمن هذه النسخة.

قرارات تنفيذية موصى بها في هذه الخطة:

- الأسعار التاريخية تقديرية؛ السعر النهائي يؤكده المذخر عبر دورة عرض السعر الحالية.
- لا تشترط تعبئة كتالوج المذخر قبل إرسال الطلب إذا أمكن مطابقة الدواء عالميًا.
- تبقى الأصناف غير القابلة للإرسال ظاهرة، ويمكن إخراجها في قائمة طلب يدوي قابلة للطباعة. لا تُنشأ لها فواتير أو ديون تلقائيًا.
- لا دمج تلقائي للموردين، ولا تصحيح تلقائي للتكاليف القديمة.
- لا تغييرات بنيوية على قاعدة البيانات مطلوبة لجوهر النسخة الأولى. أرشيف أسعار دائم، ومسودة محفوظة على الخادم، وعلاقة أب للطلبات خارج النطاق.

## 2. أساس الخطة وما يجب عدم افتراضه

تمت مراجعة الكود المحلي الحالي؛ مجلد مهمة Codex في `C:\Users\dell\Documents\ChatGPT\Faramace` كان يحتوي مستودعًا فارغًا. التنفيذ يكون في المشروع الفعلي أعلاه.

المستودع الفعلي يحتوي تغييرات كثيرة سابقة، وملفات ميزة المذاخر بينها ملفات غير متتبعة. لا تعمل reset أو clean أو تستبدل تلك الملفات بنسخة HEAD. افحص الحالة أولًا واحفظ أعمال المستخدم.

لم تُفحص قاعدة البيانات الحية ضمن المراجعة. الأعداد الواردة في المحادثة السابقة عن الموردين والدفعات والكتالوج ليست حقائق متحققة لهذه الخطة، ولا يجوز اعتمادها كمعايير قبول.

حقائق مثبتة من الملفات:

- `Supplier.warehouseId` موجود، مع قيد `@@unique([organizationId, warehouseId])`.
- `WarehouseOrder` مرتبط بمذخر واحد وفرع واحد، ويحتوي مفتاح منع تكرار `idempotencyKey`.
- `WarehouseOrderItem` يحتوي `unitPrice` و`requestedPrice` و`quotedPrice` و`quotedQuantity` وحالة الصنف والبونص.
- إنشاء الطلب يقبل باركودًا يطابق دواء عالميًا، ولا يشترط وجود صنف في كتالوج المذخر.
- الدواء العالمي في الكود يعني `organizationId: null` و`warehouseId: null` معًا.
- مسار الشحن الحالي يتجاوز فحص المخزون للصنف غير المتتبع بدفعات. الحفاظ على ذلك ضمن هذه المهمة، دون توسيع سياسة الشحن.
- `Batch` لا يحتوي تاريخ شراء مستقلًا ولا علاقة مباشرة ببند فاتورة الشراء. `createdAt` تاريخ تسجيل الدفعة، وقد يكون وقت الاستلام أو الاستيراد.
- الدفعات قابلة للحذف؛ سجلها ليس أرشيف مشتريات دائمًا.
- البونص يسجل كبند ثم دفعة بكلفة صفر، ولذلك لا يدخل في مقارنة السعر المدفوع.
- دالة `getSupplierPriceComparison` في `ai-data.ts` تقرأ `PurchaseItem` وتجمع بالاسم؛ لا يعاد استخدامها بوصفها الخدمة الجديدة.
- ربط المورد يمنع حاليًا تعديله وحذفه عبر إجراءات الموردين. يجب معالجة أثر التعديل المحلي كما هو محدد لاحقًا.
- ملف `apps/web/scripts/cost-conflicts-review.csv` الحالي يحتوي 40 اسمًا مختلفًا، ولا يثبت وحده استمرار الخطأ أو القيمة الصحيحة.

شغلت في المراجعة 62 اختبارًا ونجحت ضمن ملفات warehouse-catalog وwarehouse-purchase-bridge وwarehouse-quote وwarehouse-access وwarehouse-stock-deduction. هذه اختبارات خط أساس، وليست اختبارًا للميزة الجديدة.

## 3. تجربة المستخدم المطلوبة

### 3.1 إدخال الاحتياج

- عنوان الصفحة: «طلب أدوية من المذاخر».
- اختيار فرع الاستلام إلزامي إذا كان المستخدم يستطيع العمل على عدة فروع ولا يملك فرعًا مفروضًا في سياقه.
- إذا كان المستخدم مقيدًا بفرع، يعرض اسمه دون السماح بتجاوزه.
- حقل بحث بالاسم التجاري والعلمي والباركود، مع تأخير قصير وإلغاء الاستجابة القديمة عند تغير البحث.
- النتائج تعرض الاسم والباركود والمعلومات المتاحة التي تميز الصنف. لا تختر دواء تلقائيًا عند تشابه الاسم أو التركيز.
- يختار المستخدم سجل دواء فعليًا؛ النص الحر غير المطابق يبقى بحثًا، ولا يتحول إلى صنف قابل للإرسال.
- لا تحصر البحث في الأصناف ذات المخزون الموجب. يمكن طلب دواء نفد مخزونه أو ليس له تاريخ سعر.
- لا تعرض أدوية خاصة بمؤسسة أخرى أو بمذخر آخر ضمن البحث.
- إضافة الدواء نفسه مرة أخرى تركز سطره الموجود وتتيح تعديل الكمية، بدل إنشاء سطر مكرر غير مقصود.
- الكميات أعداد صحيحة موجبة آمنة، مع فحص الخادم أيضًا.

### 3.2 جدول الأصناف

الأعمدة الأساسية: الدواء، الكمية ووحدتها، أقل آخر سعر مسجل، المورد المختار، تاريخ السعر، الإجمالي التقديري، حالة الإرسال، إجراء مقارنة الأسعار، حذف.

- اختيار الأرخص افتراضيًا يكون فقط عند وجود سعر مؤهل للمقارنة ووحدة واضحة.
- إذا كان الأرخص غير مربوط، يظهر بوصفه الأرخص مع حالته. يعرض النظام أيضًا أرخص خيار مؤهل للإرسال، لكن لا يستبدل اختيار المستخدم بصمت.
- إذا تغيرت الأسعار بعد إعادة التحميل، لا تغيّر اختيار المستخدم اليدوي دون إظهار التغيير.
- إذا لم يوجد سعر، يعرض «لا يوجد سعر شراء مسجل»؛ لا يعرض صفرًا كأنه سعر مجاني.
- يسمح باختيار مذخر نشط يدويًا لدواء بلا تاريخ، لإرسال طلب تسعير بسعر متوقع غير محدد.
- الإجمالي الذي يحتوي أصنافًا بلا سعر يسمى «إجمالي الأصناف المسعرة تقديريًا» مع عدد الأصناف غير المسعرة، وليس إجمالي الطلب الكامل.

### 3.3 مقارنة الأسعار

لكل مورد: آخر سعر مدفوع مؤهل، تاريخ ومصدر السجل، الفرع المصدر عند اختلافه، عمر السعر، الربط بالمذخر وحالته.

- الترتيب الأساسي تصاعدي بالسعر، وعند التعادل بالأحدث، ثم بمعرف ثابت لضمان استقرار العرض.
- وسم السعر الأقدم من 90 يومًا كتنبيه إرشادي، دون استبعاده لمجرد قدمه. الرقم ثابت قابل للتعديل من موضع واحد، وليس قاعدة محاسبية.
- الأسعار غير المؤهلة تعرض منفصلة مع السبب ولا تشارك في إبراز الأرخص.
- خيار «سجل الأسعار» يجلب سجلًا مرقم الصفحات عند الطلب؛ لا يحمل جميع الدفعات لكل دواء عند فتح الصفحة.
- يظهر بوضوح أن هذا تاريخ بيانات مسجلة وقد لا يشمل العمليات المحذوفة.

### 3.4 المراجعة والإرسال

- مجموعة لكل مذخر مختار، مع اسم المورد المحلي واسم المذخر إن اختلفا.
- لكل مجموعة: الأصناف، الكميات، الأسعار المطلوبة، الإجمالي التقديري، ملاحظات خاصة بتلك المجموعة.
- الطلب النهائي للمذخر لا يتضمن أسعار الموردين المنافسين أو سجل المؤسسة الكامل.
- قسم منفصل للأصناف غير القابلة للإرسال، مع سبب كل صنف وطباعة قائمة طلب يدوي.
- زر الإرسال يوضح عدد الطلبات الإلكترونية وعدد الأصناف المتبقية خارجها.
- بعد التنفيذ تظهر نتيجة كل مذخر ورقم الطلب ورابط متابعته، وتبقى المجموعة الفاشلة قابلة للمراجعة وإعادة المحاولة.
- لا تنتقل تلقائيًا إلى صفحة أخرى عند أول نجاح إذا بقيت مجموعات فاشلة أو غير مرسلة.

## 4. تعريف السعر التاريخي وخوارزمية المقارنة

### 4.1 تعريف «الأرخص»

لكل دواء محدد ولكل `supplierId`: استخرج أحدث سجل سعر مدفوع مؤهل ضمن نطاق المستخدم، ثم رتب الأسعار الناتجة تصاعديًا.

مثال قبول: المورد أ باع سابقًا بـ800 ثم مؤخرًا بـ1200؛ المورد ب آخر سعره 1000. الفائز هو ب بـ1000، وليس أ بـ800.

لا تجمع الموردين بالاسم، ولا الأدوية بالاسم العلمي. مطابقة الأدوية المحلية والعالمية بالباركود لا تتم إلا بعد التحقق من أنه يمثل الصنف التجاري نفسه ولا توجد مطابقة غامضة.

### 4.2 سياسة المصادر للنسخة الأولى

لتجنب دمج غير موثوق بين فاتورة ودفعة استلامها، اعتمد السياسة التالية صراحة:

1. الدفعات ذات المورد المعروف هي المصدر الأساسي لسعر كل زوج دواء/مورد في النسخة الأولى، لأنها تحفظ السعر الفعلي المسجل عند إدخال المخزون، بما فيه البيانات القديمة.
2. إن لم توجد دفعة مدفوعة مؤهلة لذلك الزوج، يمكن استخدام بند من فاتورة `COMPLETED` كسجل بديل، مع إظهار مصدره. لا تدخل فواتير PENDING أو الملغاة.
3. لا تدمج المصدرين في سلسلة زمنية واحدة ثم تفترض أنهما عمليات شراء مستقلة. في السجل التفصيلي افصل سجلات الدفعات عن سجلات الفواتير عند عرض الاثنين.
4. سجل الدفعة يعرض «تاريخ تسجيل الدفعة»، والفاتورة «تاريخ تسجيل الفاتورة»، ما لم يوجد دليل صريح على تاريخ شراء أصلي.
5. إذا كانت الفاتورة البديلة تحمل أكثر من سعر مدفوع مختلف للصنف نفسه في الفاتورة الأخيرة، لا تختَر أحدها اعتباطيًا؛ أعرض تعارضًا يحتاج مراجعة.

قيد معروف: هذه سياسة «آخر سعر مسجل مؤهل» وليست إثباتًا لآخر شراء فعلي إذا حُذفت دفعات أو استوردت بيانات قديمة حديثًا. يجب شرحها باختصار في مساعدة الواجهة وتوثيقها في التسليم. أرشيف مشتريات نهائي يحتاج ربط مصدر دائم ويؤجل لميزة مستقلة.

### 4.3 أهلية السجل

- السعر عدد متناهٍ موجب؛ الصفر والبونص والسالب وغير الصالح لا يمثل سعرًا مدفوعًا للمقارنة.
- لا يشترط أن تكون كمية الدفعة المتبقية موجبة ولا أن تكون صلاحيتها مستقبلية؛ نحن نقارن سعرًا تاريخيًا لا توفر المخزون الحالي.
- المورد ينتمي للمؤسسة الصحيحة. المورد ذو `organizationId: null` لا يربط تلقائيًا بالمؤسسة.
- السجلات بلا مورد تظهر كمعلومات غير منسوبة ولا تنتج موردًا مختارًا.
- إذا كان آخر سجل مدفوع معروف متعارض الوحدة أو الكلفة، لا تتخطه بصمت لتسمية سعر أقدم «آخر سعر». اعرض أن السعر الأحدث يحتاج مراجعة، ويمكن عرض السابق بتاريخ واضح.
- السعر المتعادل زمنيًا داخل عدة دفعات يحتاج حسمًا: إن اختلفت القيم ولا يمكن معرفة ترتيبها الحقيقي، تعامل معه كتعارض. ترتيب UUID لا يثبت أسبقية الشراء.

### 4.4 الوحدات وجودة البيانات

- راجع مسارات إدخال الدواء والمخزون والشراء قبل تثبيت تسمية الوحدة. بعض الواجهات تحسب سعر الشريط من الباكيت، لكن المخطط لا يخزن وحدة طلب صريحة.
- النسخة الأولى تستخدم وحدة المخزون الحالية نفسها للكميات والأسعار؛ لا تضف تحويل باكيت/شريط/حبة غير مدعوم في النموذج.
- اكتب تسمية الوحدة المتحقق منها. إن تعذر إثباتها لصنف، اعرض «وحدة المخزون» مع حالة تحتاج مراجعة، ولا تدّعِ أنها شريط أو باكيت.
- لا تعتمد الاسم في CSV مفتاحًا إنتاجيًا لوضع تحذير على كل دواء مشابه.
- حوّل الحالات التي يمكن التحقق منها إلى قائمة مراجعة بمعرف صنف وفرع/مؤسسة موثوقين، مع سبب وحالة تحقق. إذا تعذر التحقق من ملف التعارضات، وثق ذلك ولا تزعم تغطية الحالات الأربعين.
- لا تصلح أي سعر أو رصيد أو بيانات تاريخية تلقائيًا.

## 5. نطاق المؤسسة والفروع والدواء

- استعمل `getTenantContext` وأدوات النطاق الموجودة؛ لا تثق في organizationId أو branchId القادم من العميل.
- مالك المؤسسة/المخول له يرى تاريخ الفروع المسموح بها داخل مؤسسته. المستخدم المقيد بفرع يرى تاريخه المسموح فقط.
- فرع الاستلام مستقل عن الفرع الذي جاء منه السعر؛ اعرض المصدر عند الحاجة.
- فرع الإرسال يمر عبر `resolveOrderBranch` والتحقق الفعلي الموجود، ولا يتجاوز فرع المستخدم المفروض.
- مصدر معرف الدواء الداخلي للمقارنة قد يكون محليًا، أما الإرسال فيحتاج مقابله العالمي.
- أعد استخدام تعريف `GLOBAL_DRUG_SCOPE`؛ لا تكتب شرط `organizationId: null` وحده.
- أظهر حالات باركود مفقود، مقابل عالمي مفقود، أو مطابقة غامضة قبل الإرسال. لا تنشئ/ترقِّ أدوية عالميًا بصورة تلقائية ضمن هذه الميزة.

## 6. ربط الموردين في السوبر أدمن

### 6.1 الواجهة

في `/dashboard/admin/warehouses` أضف إجراء «ربط الموردين» لكل مذخر.

النافذة تعرض:

- اسم المذخر وحالته.
- بحث المؤسسات واختيار مؤسسة صريح.
- المورد المرتبط حاليًا بهذه المؤسسة إن وجد.
- بحث موردي المؤسسة وعرض الاسم وبيانات تعريف مناسبة مثل الهاتف، دون عرض أرصدة لا يحتاجها الربط.
- معاينة واضحة: «المورد المحلي س في المؤسسة ص سيرتبط بالمذخر ع».
- تنبيه بأن الربط يوجّه فواتير الطلبات الجديدة إلى المورد الموجود ويحافظ على تاريخه؛ لا ينقل أرصدة أو يدمج سجلات.

### 6.2 قواعد الخادم

- صلاحية SUPER_ADMIN فقط على GET/POST أو PATCH الخاصة بالربط.
- تحقق من وجود المؤسسة والمذخر والمورد، وأن المورد تابع للمؤسسة المختارة.
- القيد الفريد هو الحكم النهائي، مع معالجة تعارض السباق برسالة 409 واضحة.
- تكرار الربط نفسه يعيد نجاحًا دون أثر إضافي.
- إذا كان المورد مرتبطًا بمذخر آخر، أو للمذخر مورد آخر في المؤسسة، ارفض الاستبدال التلقائي.
- لا تتيح فك الربط أو إعادة توجيهه ضمن النسخة الأولى؛ يمكن إضافته لاحقًا كعملية مدروسة بسبب أثره على الطلبات الجارية.
- اكتب سجل تدقيق بالهوية والمورد والمؤسسة والمذخر والقيمة القديمة والجديدة.

### 6.3 أثر الربط على العمليات الحالية

- حافظ على منع حذف المورد المرتبط.
- عدّل حظر تعديل المورد الشامل بحيث يمكن تعديل بياناته المحلية المعتادة بصلاحية المؤسسة، دون تغيير `warehouseId` أو `organizationId` عبر نموذج المورد.
- تعديل اسم المورد المحلي لا يغيّر اسم المذخر أو بيانات حسابه، والعكس صحيح. لا تضف مزامنة ضمنية بين الكيانين.
- افحص اعتماد الطلب بالتزامن مع إنشاء الربط: قرار المورد الحالي يُقرأ قبل المعاملة في الكود. أعد التحقق داخل معاملة الاعتماد عند الحاجة، وتعامل مع القيد الفريد بإعادة جلب المورد الصحيح دون تكرار فاتورة.
- لا تعدّل منطق الفواتير والديون خارج ما يلزم للحفاظ على المورد الصحيح ومنع السباق.

## 7. تقسيم الطلبات وإعادة المحاولة

- التجميع بحسب `warehouseId` الحقيقي، وليس اسم المورد أو اسم المذخر.
- القائمة تخص فرع استلام واحدًا في النسخة الأولى. عند تغيير الفرع أعد التحقق من النطاق والأسعار والطلبات غير المرسلة.
- لكل مجموعة مفتاح idempotency مستقل ينشأ مرة عند تثبيت حمولة الإرسال.
- عند فشل الشبكة بنتيجة غير مؤكدة، أعد نفس الحمولة بنفس المفتاح. لا تنشئ مفتاحًا جديدًا قد يكرر الطلب الذي نجح في الخادم.
- إذا عُدلت مجموعة ثبت أنها لم ترسل، أنشئ مفتاحًا جديدًا لتلك الحمولة. إذا كانت النتيجة مجهولة، احسمها أولًا بإعادة الطلب الأصلي أو استرجاع نتيجته قبل السماح بإرسال بديل.
- حافظ على ترتيب الأصناف والحقول في إعادة المحاولة؛ requestHash الحالي يتأثر بترتيب البنود.
- المجموعة الناجحة تصبح للعرض فقط، ولا تدخل إعادة إرسال باقي المجموعات.
- الإرسال المتتابع كافٍ في النسخة الأولى؛ لا حاجة لمعاملة واحدة تشمل كل المذاخر.
- الحد الحالي 100 صنف لكل طلب: أظهر تجاوز المجموعة للحد قبل أي إرسال. لا تقسم المذخر نفسه تلقائيًا إلى عدة طلبات خلافًا لاختيار «طلب لكل مذخر». تحسين الحد مهمة منفصلة عند الحاجة.
- تحقق من أن المذخر نشط ومن الحظر وحد الائتمان عبر المسار الحالي؛ لا تستنسخ تلك الشروط في مسار أقل حماية.
- إذا حُفظت حالة إعادة المحاولة في المتصفح، استخدم sessionStorage بمفتاح المستخدم والمؤسسة والفرع، واحذفها عند اكتمال العملية/الخروج. لا تحفظ سجل أسعار كل الموردين أو بيانات حساسة غير لازمة. التعامل مع استرجاع الحالة يفحص الصلاحيات مجددًا.
- تخزين المتصفح وسيلة استرداد لهذه الجلسة، وليس مسودة مؤسسية دائمة أو بديلًا عن قاعدة البيانات.

## 8. عقود الخدمات وواجهات API المقترحة

الأسماء التالية مقترحة، ويمكن تكييفها مع تنظيم المشروع دون تغيير السلوك:

### 8.1 خدمة مقارنة نقية

ملف `apps/web/app/lib/supplier-price-comparison.ts`:

- أنواع السعر ومصدره وتاريخه ووحدته وحالة جودته.
- اختيار آخر سجل مؤهل لكل معرف مورد.
- ترتيب آخر الأسعار واختيار الأرخص.
- حساب عمر السعر والإجمالي التقديري.
- لا تستورد Prisma أو next في المنطق النقي.

### 8.2 طبقة بيانات

ملف `apps/web/app/lib/supplier-price-history.ts`:

- تحميل سجلات الأصناف المطلوبة دفعة واحدة ضمن النطاق.
- معالجة هوية الدواء المحلية/العالمية دون دمج غامض.
- تحميل حالة ربط المورد والمذخر.
- تطبيق سياسة المصادر وعدم خلط تواريخ التسجيل بالشراء.
- بحث مرقم الصفحات؛ لا تحمل مخزون المؤسسة بالكامل إلى المتصفح.
- لا تستخدم استعلامًا لكل صنف. يمكن استخدام استعلامات مجمعة محدودة؛ الوعد بـ«استعلام واحد» ليس معيارًا بحد ذاته.
- عند استخدام SQL خام يجب أن يكون بمعاملات مربوطة وبنطاق المؤسسة والفروع صراحةً. افحص خطة الاستعلام ببيانات مناسبة قبل اقتراح فهرس أو ترحيل.

### 8.3 واجهات الصيدلية

- بحث أصناف مؤهل لقائمة الاحتياج؛ افحص الخدمات الحالية أولًا وأعد الاستخدام إن حافظت على النطاق وأظهرت الأدوية النافدة.
- `POST /api/purchases/supplier-prices`: قائمة معرفات محدودة مع فرع السياق؛ يعيد مقارنة مجمعة.
- `GET /api/purchases/supplier-prices/history`: سجل صنف/مورد مرقم الصفحات ضمن النطاق.
- إنشاء الطلب يبقى عبر `POST /api/warehouses/orders` الحالي.

مثال بنيوي للاستجابة، وليس أسماء Prisma ملزمة:

```ts
type SupplierPriceOption = {
  supplierId: string;
  supplierName: string;
  warehouseId: string | null;
  warehouseName: string | null;
  price: number | null;
  recordedAt: string | null;
  source: 'BATCH' | 'COMPLETED_PURCHASE';
  sourceBranchId: string;
  unitLabel: string;
  comparable: boolean;
  qualityReason: string | null;
  orderable: boolean;
  orderabilityReason: string | null;
};
```

حالة orderable تعني الأهلية المبدئية للإرسال وليست ضمان توفر فعلي أو اجتياز حد الائتمان لحظة الإرسال. لا تجعل المقارنة نفسها تكتب طلبات أو موردين.

### 8.4 واجهات الإدارة

- `GET /api/admin/warehouses/[id]/supplier-links` لجلب الربط والبحث المحدود بحسب المؤسسة.
- `POST /api/admin/warehouses/[id]/supplier-links` لإنشاء الربط مع التحقق وسجل التدقيق.
- يمكن إعادة استخدام مسار بحث المؤسسات المناسب بعد مراجعة صلاحياته بدل إنشاء واجهة عامة تكشف المؤسسات.

### 8.5 التحقق والصلاحيات

- إنشاء الطلب يتطلب صلاحية `canCreatePurchase` والوصول لميزة `warehouseManagement` وفق النظام الحالي.
- عرض التاريخ يتطلب صلاحية مشاهدة بيانات الموردين/المشتريات المناسبة؛ افحص أن الأدوار لا تحصل على تكاليف محجوبة عنها في بقية التطبيق.
- ضع حدودًا لعدد المعرفات وطول البحث وحجم الصفحة، وأزل المعرفات المكررة.
- لا تمرر استعلامات العميل مباشرة إلى Prisma.
- الأخطاء المنظمة تميز عدم الصلاحية، وعدم وجود الصنف في النطاق، والتعارض، وعدم إمكانية الإرسال.

## 9. الملفات الحالية التي يجب مراجعتها أثناء التنفيذ

المسارات التالية نسبية إلى `D:\Programming\Faramace`:

| الملف | الاستخدام |
|---|---|
| `apps/web/prisma/schema.prisma` | التحقق من العلاقات والحقول؛ لا تعديل بنيوي افتراضيًا |
| `apps/web/app/dashboard/purchases/warehouse-orders/new/page.tsx` | حراس الصفحة وسياق الوصول |
| `apps/web/app/dashboard/purchases/warehouse-orders/new/WarehouseOrderClient.tsx` | استبدال تدفق الإدخال وإضافة المراجعة |
| `apps/web/app/dashboard/purchases/warehouse-orders/OrdersTrackClient.tsx` | التحقق من روابط المتابعة وعرض الأسعار بعد الإرسال |
| `apps/web/app/dashboard/admin/warehouses/page.tsx` | نافذة ربط الموردين |
| `apps/web/app/api/admin/warehouses/[id]/route.ts` | نمط صلاحيات الإدارة؛ لا تخلط تعديل بيانات المذخر بالربط دون تحقق |
| `apps/web/app/api/warehouses/orders/route.ts` | الإنشاء، المطابقة، الفرع، الائتمان، منع التكرار |
| `apps/web/app/api/warehouses/orders/[id]/route.ts` | الاعتماد واختيار المورد وإنشاء الفاتورة |
| `apps/web/app/api/warehouse-portal/orders/[id]/quote/route.ts` | دورة السعر النهائي الموجودة |
| `apps/web/app/api/warehouse-portal/orders/[id]/shipping/route.ts` | التحقق من استمرار مسار الأصناف غير المتتبعة |
| `apps/web/app/lib/warehouse-catalog.ts` | المطابقة العالمية الصحيحة |
| `apps/web/app/lib/warehouse-access.ts` | نطاق الفروع والطلبات |
| `apps/web/app/lib/warehouse-purchase-bridge.ts` | استخدام المورد المرتبط والبونص |
| `apps/web/app/lib/warehouse-quote.ts` | حساب السعر والكمية المعتمدين |
| `apps/web/app/lib/purchase-receipt.ts` | مصدر الدفعات وكلفة البونص |
| `apps/web/app/lib/actions/supplier.ts` | تعديل المورد المحلي ومنع حذف المرتبط |
| `apps/web/app/lib/ai-data.ts` | مرجع لفكرة المقارنة فقط؛ لا تنسخ التنفيذ الحالي |
| `apps/web/scripts/cost-conflicts-review.csv` | مادة مراجعة تاريخية، لا مصدر حقيقة تلقائي |

قسم الواجهة إلى مكونات مناسبة للبحث وجدول الاحتياج ومقارنة الأسعار ومراجعة المجموعات، بدل تضخيم ملف عميل واحد.

## 10. مراحل التنفيذ وتسليم كل مرحلة

### المرحلة 0 — تثبيت الواقع

- اقرأ AGENTS.md إن وجد في مسار المشروع أو مساراته الفرعية.
- افحص git status والتعليمات وإعدادات الاختبارات.
- تحقق من ملفات هذه الخطة مقابل النسخة الحالية؛ إذا تغير الكود فحدّث التفاصيل المستندة إليه.
- راجع وحدة التخزين ومسارات الإدخال وهوية الأدوية وصلاحيات البحث.
- عند توفر اتصال مصرح بقاعدة البيانات، نفذ قراءات تجميعية فقط للتحقق من جودة البيانات وحالات الربط. لا تطبع أسرار الاتصال ولا تُجرِ تنظيفًا ضمن الفحص.
- وثق القيود غير الممكن التحقق منها؛ لا تختلق أرقامًا أو تتوقف عن بناء الأجزاء المستقلة لمجرد غياب اتصال حي.

مخرج المرحلة: قواعد المصدر والوحدة والنطاق مثبتة في الكود/التوثيق قبل بناء ترشيح الأرخص.

### المرحلة 1 — ربط الموردين

- خدمة تحقق وربط مع اختبار الصلاحيات والتعارض والتكرار.
- واجهة السوبر أدمن.
- سجل التدقيق.
- معالجة تعديل البيانات المحلية للمورد والتحقق من سباق الاعتماد.

مخرج المرحلة: ربط مورد موجود دون إنشاء نسخة أو تغيير رصيد، واستخدامه عند الاعتماد.

### المرحلة 2 — المقارنة والبحث

- خدمة نقية وخدمة بيانات وواجهات API محمية.
- سياسة المصادر والوحدات والبونص والتعارضات.
- مطابقة عالمية واضحة، وبحث عن أصناف بلا مخزون أو تاريخ.

مخرج المرحلة: نتائج المقارنة صحيحة لحالات الاختبار، وغياب تسرب بيانات بين مؤسستين.

### المرحلة 3 — واجهة الاحتياج والمراجعة

- البحث والإضافة والكميات واختيار المورد.
- نافذة آخر الأسعار والسجل التفصيلي.
- اختيار الفرع وتجميع المذاخر والأسعار غير المعروفة.
- قائمة غير المرسل والطباعة اليدوية مع عبارة «لم يُرسل إلكترونيًا».

مخرج المرحلة: قائمة كاملة قابلة للمراجعة قبل أي إنشاء طلب.

### المرحلة 4 — الإرسال والاسترداد

- إنشاء المجموعات عبر المسار الحالي ومفاتيح مستقلة.
- نجاح جزئي وإعادة محاولة آمنة واسترداد جلسة عند الحاجة.
- اختبار وصول الطلب إلى المذخر وتسعيره واعتماده على المورد المرتبط.

مخرج المرحلة: كل مذخر يستقبل أصنافه فقط، وتبقى الأصناف الأخرى محفوظة للمستخدم دون ادعاء إرسالها.

### المرحلة 5 — التحقق والتسليم

- اختبارات المنطق والخادم والتكامل المناسبة، وفحص الواجهة العربية والطباعة.
- فحص TypeScript والبناء المتاحين حسب إعداد المشروع، مع فصل الأخطاء السابقة المثبتة عن أخطاء التغيير.
- تحديث هذه الخطة بحالة كل مرحلة وتوثيق أي انحراف فعلي عن قراراتها.
- لا نشر ولا ترحيل حي ولا دمج موردين/تصحيح تكاليف ضمن تسليم هذه الميزة ما لم يُطلب صراحةً لاحقًا.

## 11. معايير القبول والاختبارات الضرورية

### المقارنة

- مثال 800 ثم 1200 مقابل 1000 ينتج المورد صاحب 1000.
- موردان بالاسم نفسه وبمعرفين مختلفين لا يندمجان.
- أحدث دفعة بونص صفرية لا تلغي آخر سعر مدفوع ولا تجعل المورد مجانيًا.
- دفعة استهلكت بالكامل تبقى قابلة للاستخدام كتاريخ سعر.
- مورد مجهول أو سعر غير صالح لا يصبح اختيارًا تلقائيًا.
- تاريخ تسجيل الدفعة لا يعرض بوصفه تاريخ شراء مثبتًا.
- المصدر البديل لا يستخدم فاتورة معلقة أو ملغاة.
- تعارض الأسعار في الزمن نفسه لا يحسم اعتباطيًا.
- أحدث سعر غير موثوق يظهر بتحذير، والسعر السابق لا يقدم كأنه الأحدث.

### الصلاحيات والربط

- مستخدم مؤسسة أ لا يرى أسعار مؤسسة ب حتى لو أرسل معرفاتها مباشرة.
- المستخدم المقيد بفرع لا يختار فرعًا آخر بطلب مصطنع.
- غير SUPER_ADMIN لا يغير الروابط.
- المورد لا يربط بمؤسسة لا يخصها.
- تعارض ربط موردين بالمذخر نفسه في المؤسسة يعيد خطأ مفهومًا.
- تكرار الربط نفسه آمن.
- ربط مورد له فواتير ومدفوعات لا يغير تلك العلاقات أو الرصيد.
- تعديل اسم المورد المحلي لا يغير اسم المذخر، والحذف يبقى ممنوعًا.
- اعتماد طلب أثناء إنشاء الربط لا ينشئ موردًا أو فاتورة مكررين.

### الطلبات

- اختيار أصناف لمذخرين ينتج طلبين بالأصناف الصحيحة فقط.
- دواء عالمي غير موجود في كتالوج المذخر يمكن إرساله لطلب التسعير وفق المسار الحالي.
- دواء محلي بلا مقابل عالمي يبقى ظاهرًا كغير قابل للإرسال.
- دواء بلا سعر يرسل كتوقع غير محدد؛ لا يقدم سعر صفر على أنه عرض مجاني.
- نجاح المجموعة الأولى وفشل الثانية لا يؤدي لإعادة إرسال الأولى.
- فقدان استجابة نجاح الخادم ثم إعادة المحاولة يعيد نفس الطلب.
- لا يعاد استخدام مفتاح واحد لمذخرين أو حمولتين مختلفتين.
- تجاوز 100 صنف في مجموعة يعالج قبل الإرسال، دون إنشاء مجموعات غير متوقعة.
- السعر النهائي والكمية والبونص بعد عرض المذخر يمران عبر `effectiveLine` والجسر الحالي، دون إعادة تطبيق السعر التاريخي عند الاعتماد.
- قائمة الطباعة اليدوية لا تُنشئ طلبًا إلكترونيًا أو فاتورة أو دينًا.

### تجربة الواجهة

- RTL واضح، ومناسب للهاتف وسطح المكتب، مع حالات التحميل والخطأ والفراغ.
- نتائج بحث قديمة لا تستبدل نتائج الأحدث عند بطء الشبكة.
- تغيير المورد يعدل مجموعة المراجعة والإجمالي فورًا.
- تحديث الصفحة بعد إرسال جزئي لا يجعل إعادة المحاولة تكرر الطلب الناجح إذا كانت استعادة الجلسة مفعلة.
- التنقل والخروج لا يبقيان بيانات جلسة مستخدم سابق متاحة لمستخدم آخر.

## 12. تشغيل الاختبارات

من `D:\Programming\Faramace\apps\web`:

```powershell
pnpm exec vitest run app/lib/__tests__/warehouse-catalog.test.ts app/lib/__tests__/warehouse-purchase-bridge.test.ts app/lib/__tests__/warehouse-quote.test.ts app/lib/__tests__/warehouse-access.test.ts app/lib/__tests__/warehouse-stock-deduction.test.ts
```

أضف ملفات اختبار مخصصة للمقارنة والربط وتجميع الطلبات والاسترداد. اختبارات النجاح العادي وحدها غير كافية؛ الأهم هو اختلاف المؤسسات والبونص والتعارض والفشل الجزئي.

إعداد التكامل الحالي في `apps/web/vitest.integration.config.ts` يشترط `TEST_DATABASE_URL` وقاعدة محلية معزولة اسمها `faramace_readiness`. لا تستخدم قاعدة الإنتاج للاختبارات ولا تتجاوز هذا الحارس.

اختبارات الوحدات لا تثبت أن قاعدة البيانات الحية نظيفة أو أن الربط موجود بالفعل. في التسليم اذكر بوضوح ما اختُبر محليًا وما اختُبر في تكامل فعلي وما تعذر التحقق منه.

## 13. تعريف الإنجاز للموديل المنفذ

تُعد الميزة مكتملة عندما يستطيع صاحب المؤسسة تكوين احتياجه، ومقارنة آخر أسعار الموردين بصدق بشأن المصدر والوحدة، وتغيير اختياراته، ومراجعة مجموعات المذاخر، ثم إرسال كل مجموعة ومتابعة نتيجتها دون تكرار أو إسقاط أصناف؛ ويستطيع السوبر أدمن ربط المورد الصحيح بالمذخر دون تغيير التاريخ أو الأرصدة.

سلّم وصف التغييرات، والاختبارات ونتائجها، والقيود المتبقية، وأي خطوة تشغيلية لازمة مثل إنشاء الروابط الفعلية. لا تصف جميع المراحل بأنها منجزة إذا لم تختبر مسار الإرسال والاعتماد أو بقيت قاعدة الوحدة غير محددة.

---

# سجل التنفيذ

## المرحلة 0 — تثبيت الواقع ✅ (2026-09-10)

**فحص المستودع:** لا يوجد `AGENTS.md` في المشروع ولا في مساراته الفرعية. الفرع
`010-plan-enforcement`، وفيه 275 ملفاً غير ملتزم (172 معدّل، 101 غير متتبع، 2 محذوف).
لم تُنفَّذ أي عملية `reset` أو `clean` ولم يُستبدل أي ملف بنسخة HEAD.

**تحقق من ادعاءات §2 مقابل الكود الحالي — كلها صحيحة:**
`Supplier.warehouseId` + `@@unique([organizationId, warehouseId])` موجودان؛
`WarehouseOrderItem` يحمل `unitPrice`/`requestedPrice`/`quotedPrice`/`quotedQuantity`/
`status`/`bonusQuantity`؛ `resolveOrderBranch` في `app/lib/warehouse-access.ts:75`؛
حد 100 صنف و`requestHash` و`idempotencyKey` و`canCreatePurchase` و`creditLimit`
و`isBlocked` كلها مطبَّقة في `app/api/warehouses/orders/route.ts`.

**§4.4 وحدة التخزين — محسومة بالأدلة، والخلاصة أن الوحدة غير مضمونة:**
| مسار الإدخال | ما يُكتب في `Batch.costPrice` |
|---|---|
| `app/ui/inventory/add-batch-modal.tsx:115` | `packetPrice / stripsPerPacket` → سعر الشريط |
| `app/api/inventory/import/route.ts:155` | `row.cost` كما هو، **بلا قسمة** |
| `app/lib/purchase-receipt.ts:67` | `item.cost` كما هو، **بلا قسمة** |

ولا يوجد أي عمود وحدة في `Inventory` أو `Batch`. لذلك تُطبَّق §139 حرفياً:
تسمية الوحدة في الواجهة **«وحدة المخزون»** فقط، ويُمنع ادعاء «شريط» أو «باكيت».

**قراءات تجميعية من القاعدة الحية (2026-09-10، قراءة فقط، بلا طباعة أسرار):**
هذه أرقام تحققتُ منها بنفسي في هذه الجلسة، وتُلغي تحفّظ §39 عليها:
- الموردون: 21، **المرتبط منهم بمذخر: 0**، و**بلا مؤسسة: 0** ← القيد الفريد حَكَم موثوق (§130).
- المذاخر النشطة: 1 («نسيم البحر»)، كتالوجه **فارغ** ولم يستلم أي طلب قط.
- المؤسسات: 6. `GET /api/admin/organizations` موجود وSUPER_ADMIN فقط ويعيد `{id,name}`
  ← يُعاد استخدامه بدل واجهة جديدة تكشف المؤسسات (§261).
- `Purchase`: 3 فواتير و6 بنود على المنصة كلها؛ **دار التفاؤل: صفر** ← المصدر الأساسي
  للأسعار هو الدفعات كما قررت §118، لا الفواتير.
- `Batch`: 7794 إجمالاً، **5668 بمورد معروف** و**2126 بلا مورد** (27٪ تحتاج حالة
  «غير منسوب» صراحةً §131)، و7777 بكلفة > 0.
- دار التفاؤل: 6015 دفعة تغطي 2974 دواءً، و**1087 دواءً له دفعات من أكثر من مورد**
  ← حالة المقارنة الفعلية.
- تواريخ الدفعات حقيقية: 162 يوماً مختلفاً بين 2026-03-22 و2026-09-09.
- `scripts/cost-conflicts-review.csv`: 39 صفاً (لا 40). يُستعمل كمادة مراجعة فقط (§294).
- موردون مكرّرون ظاهرون في البيانات: `درة الشرق` (26 دفعة) مقابل
  `مدخر ادوية درة الشرق` (46)، و`سماء البركة` (44) مقابل `مدخر ادوية سماء البركة` (1).
  **لا دمج تلقائي** (§30) — يُوثَّق فقط.

**تحققان إضافيان طلبتهما الخطة:**
- §184 سباق الاعتماد: مسار P2002 في `app/api/warehouses/orders/[id]/route.ts:387`
  يعيد 409 صريحاً يطلب إعادة المحاولة، وعندها يجد `decideMirrorSupplier` المورد
  الموجود ويستعمله (`action: USE`) بلا فاتورة مكررة. السلوك المطلوب متحقق أصلاً.
- §182 تعديل المورد: `updateSupplier` يبني `data` من حقول صريحة
  (`name`/`email`/`phone`/`address`) ولا يمكنه كتابة `warehouseId` أو `organizationId`
  ← رفع حظر التعديل آمن، وحظر الحذف (سطر 89) يبقى كما هو.

## المرحلة 1 — ربط الموردين ✅ (2026-09-10)

**ملفات جديدة**
- `apps/web/app/lib/supplier-warehouse-link.ts` — `decideSupplierLink` نقية بلا Prisma،
  تُنتج `ALREADY_LINKED | LINK | CONFLICT_SUPPLIER_LINKED_ELSEWHERE |
  CONFLICT_WAREHOUSE_HAS_SUPPLIER | WRONG_ORG`، و`isLinkConflict` لتمييز 409 عن 400.
  ترتيب الفحوص مقصود: انتماء المؤسسة **قبل** أي فحص تعارض، وإلا أفصحت الرسالة عن
  حالة ربط مورد لا يخص المؤسسة المختارة (مغطّى باختبار صريح).
- `apps/web/app/lib/__tests__/supplier-warehouse-link.test.ts` — 11 اختباراً.
- `apps/web/app/api/admin/warehouses/[id]/supplier-links/route.ts` — GET + POST،
  SUPER_ADMIN فقط عبر `canCreateWarehouse` (نفس نمط `[id]/route.ts`)، ومفصول عنه
  لأن أثر العمليتين مختلف (§282).
- `apps/web/app/dashboard/admin/warehouses/SupplierLinksModal.tsx` — مكوّن مستقل (§296).

**ملفات معدَّلة**
- `apps/web/app/dashboard/admin/warehouses/page.tsx` — زر «ربط الموردين» في صف الإجراءات
  + عرض النافذة. لم يُمَس أي منطق قائم.
- `apps/web/app/lib/actions/supplier.ts` — رُفع حظر تعديل المورد المرتبط (§182).
  **حظر الحذف (سطر 89) باقٍ كما هو (§181).** الرفع آمن لأن `data` في `updateSupplier`
  مبنيّ من حقول صريحة (`name`/`email`/`phone`/`address`) فلا يستطيع كتابة
  `warehouseId` ولا `organizationId`.

**قرارات تنفيذية**
- حماية السباق ليست بقراءة-ثم-كتابة: الكتابة `updateMany` مشروطة بـ`warehouseId: null`،
  فإن ربط طلبٌ متزامنٌ المورد بيننا عاد العدد 0 ورجعت 409 برمز `RACE` بدل الكتابة فوقه.
  وP2002 يُترجَم إلى 409 لا 500 (§173).
- تكرار الربط نفسه يعيد نجاحاً **بلا سطر تدقيق** — أثر معدوم فلا يُسجَّل (§174).
- سجل التدقيق: `action: 'LINK'`, `entity: 'SUPPLIER'` مع الهوية والمورد والمؤسسة
  والمذخر و`previousWarehouseId`/`newWarehouseId` (§177). حقل `action` نص حر في المخطط.
- الرصيد غير مُعاد في استجابة GET (§165).
- **لم يُبنَ**: فكّ الربط وإعادة التوجيه (§176)، وواجهة ربط من جهة المؤسسة
  (اقتراح وجيه لكنه خارج نطاق الخطة — يُذكر في التسليم فقط).
- أُعيد استخدام `GET /api/admin/organizations` القائم بدل واجهة جديدة (§261).

**الاختبارات**: أمر §12 + الملف الجديد = **73 ناجحاً**. الحزمة الكاملة **468/468**.
`tsc --noEmit` = 0.

**لم يُنفَّذ على القاعدة الحية**: لم يُربط أي مورد فعلياً (0 روابط قائمة). إنشاء
الروابط خطوة تشغيلية يقوم بها المشرف من الواجهة — §417.

## المرحلة 2 — المقارنة والبحث ✅ (2026-09-10)

**ملفات جديدة**
- `apps/web/app/lib/supplier-price-comparison.ts` — منطق نقي بلا Prisma/next.
  `latestEligibleForSupplier` + `buildSupplierOptions` + `compareDrugPrices` +
  `estimateTotal` + `ageInDays`. الثوابت في موضع واحد: `STALE_PRICE_DAYS = 90` (§89)
  و`INVENTORY_UNIT_LABEL = "وحدة المخزون"` (§139).
- `apps/web/app/lib/__tests__/supplier-price-comparison.test.ts` — **29 اختباراً**
  تغطي معايير §11 كلها: مثال 800/1200 مقابل 1000، عدم الدمج بالاسم، البونص الصفري،
  الدفعة المستهلَكة، التعارض في اللحظة نفسها، القِدم، الوحدة، واستقرار الترتيب.
- `apps/web/app/lib/supplier-price-history.ts` — طبقة البيانات + `resolveHistoryScope`.
- `apps/web/app/api/purchases/supplier-prices/route.ts` — POST المقارنة المجمَّعة.
- `apps/web/app/api/purchases/supplier-prices/history/route.ts` — GET السجل المرقّم.
- `apps/web/app/api/purchases/drug-search/route.ts` — بحث قائمة الاحتياج.

**قرارات وأسبابها**
- **لم يُعَد استخدام `/api/inventory/search`** (§230): يُرشِّح النتائج على الأدوية
  التي لها صف مخزون (`filter(d => invMap.has(d.id))`) ولا يبحث بالباركود — وقائمة
  الاحتياج تحتاج العكس: الدواء النافد أول ما يُطلب (§68)، والباركود مطلوب (§65).
- **لم يُعَد استخدام `getSupplierPriceComparison`** في `ai-data.ts` (§52): يقرأ
  `PurchaseItem` ويجمع **بالاسم**، وكلاهما مخالف لـ§4.1 و§4.2.
- البوابة `canCreatePurchase` على المسارات الثلاثة — لا وجود لـ`canViewPurchases`،
  وهي نفس بوابة `POST /api/warehouses/orders`، فلا تُكشف تكاليف لدور محجوبة عنه (§266).
- النطاق من `resolveHistoryScope` حصراً: المستخدم المقيَّد بفرع تُبنى قائمته من
  `user.branchId` (ويُتحقق أنه يتبع المؤسسة) لا من جسم الطلب (§146/§147).
- **§132 مطبَّقة حرفياً**: السجل الأحدث غير المؤهل يُعاد موسوماً بسببه ولا يُتخطّى
  بصمت لتسمية سجل أقدم «آخر سعر». مغطّى باختبار صريح.
- **§133**: تعارض اللحظة نفسها بقيم مختلفة → `AMBIGUOUS_SAME_INSTANT`، ولا يُحسم
  بترتيب UUID. اختبار يثبت أن أي ترتيب للمعرّفات يعطي التعارض نفسه.
- **§120**: البديل (فاتورة COMPLETED) يُستعمل فقط للأزواج التي **لا دفعة لها**،
  والسجل التفصيلي يُعيد الدفعات والفواتير في قائمتين منفصلتين.
- استُعمل باني استعلامات Prisma لا SQL خام: النطاق يفرضه النوع، وهو أقوى من
  «معاملات مربوطة» يدوياً (§226). استعلامان مجمَّعان لا استعلام لكل صنف (§225).
- `Array.from(map.entries())` لا `for..of` على الـMap — هدف tsc هنا أقل من ES2015.

**تحقق على البيانات الحية (قراءة فقط)**
- «Dologan Denk tab»: 8 موردين و14 دفعة. الناتج مرتّب تصاعدياً وكسر التعادل بالأحدث
  يعمل كما تنص §88 (3800 في 06-08 قبل 3800 في 05-24 قبل 3800 في 05-13).
- **القاعدة تُغيّر الجواب فعلياً في 81 دواءً**: مثلاً `Asparadol 12mg\ml` أقل سعر
  تاريخي 880 بينما أرخص «آخر سعر لكل مورد» 3000 — عرض 880 كان سيَعِد بسعر لم يعد
  أحد يبيع به. هذا إثبات عملي لتعريف §4.1.
- «أرخص قابل للإرسال» = لا أحد اليوم، لأن 0 موردين مربوطين — وهي الخطوة التشغيلية
  المتبقية من المرحلة 1، لا عيب في المنطق.

**الاختبارات**: الحزمة الكاملة **497/497**. `tsc --noEmit` = 0.
طبقة البيانات لا اختبار وحدة لها بطبيعتها (مرتبطة بقاعدة) — تحققت بقراءات حية أعلاه.

## المرحلتان 3 و4 — واجهة الاحتياج والمراجعة والإرسال ✅ (2026-09-10)

**ملفات جديدة**
- `app/dashboard/purchases/warehouse-orders/new/types.ts` — أنواع مشتركة + `priceDrift`
  + `selectedOption` (نقية، ومُختبَرة).
- `.../DrugSearchBox.tsx` — بحث بالاسم/المادة/الباركود، تأخير 300ms، وإهمال استجابة
  قديمة عبر عدّاد تسلسلي (§65/§394). لا اختيار تلقائي (§66)، والنص الحر يبقى بحثاً (§67).
- `.../PriceCompareModal.tsx` — مقارنة الموردين، السجل المرقّم عند الطلب، وفصل
  السجلات غير المؤهلة في قائمة مستقلة (§90).
- `.../NeedListClient.tsx` — المُنسِّق: الفرع، الجدول، المراجعة، الإرسال.
- `.../need-line.test.ts` — 8 اختبارات لـ`selectedOption`/`priceDrift`.
- `app/lib/warehouse-order-grouping.ts` + اختباره — **21 اختباراً**.

**قرار مهم: استُخرج منطق التقسيم والمفاتيح إلى `.ts`**
`vitest.config.ts` يشمل `app/**/*.test.ts` فقط ولا يُحمِّل `.tsx` إطلاقاً. لو بقي
تقسيم المجموعات ومفاتيح منع التكرار داخل المكوّن لبقي **بلا أي تغطية** — وهو أخطر
جزء في الميزة (تكرار طلب حقيقي على مذخر حقيقي). فاستُخرج إلى وحدة نقية يستوردها
المكوّن، وغُطّي بـ21 اختباراً تشمل §189/§191/§192/§193/§195/§197/§383/§386.

**ملفات معدَّلة**
- `.../new/page.tsx` — يحسم النطاق في الخادم ويمرّره: الفرع المفروض والفروع المسموح
  بها و`canCreatePurchase`. العميل لا يستطيع تجاوزها (§64/§149).

**تحقق أمني مطلوب من §149**: `app/api/warehouses/orders/route.ts:98-103` يتحقق فعلاً
أن الفرع المطلوب يتبع مؤسسة المستخدم (`findFirst({ id, organizationId })` → 403).
مستخدم مقيَّد بفرع يتجاهل الخادم قيمة جسمه أصلاً. **آمن.**

**نطاق الأسعار مقابل نطاق الطلب (تسجيل صريح كي لا يُقرأ لاحقاً كتسرّب)**
`resolveHistoryScope` يعطي مستخدم المؤسسة تاريخ **كل** فروعها، بينما الطلب يذهب
لفرع استلام واحد. هذا مقصود (§147/§148)، والواجهة تُظهر «مصدره فرع آخر» عند الاختلاف.

**انحرافان عن الخطة عولجا (كانا سيبقيان حقولاً ميتة توهم بالتطبيق)**
- **§79 النصف الثاني** («لا تغيّر اختيار المستخدم دون إظهار التغيير»): أُضيف
  `chosenPriceAtSelection` كلقطة سعر لحظة الاختيار اليدوي، و`priceDrift` تكشف التغيّر،
  والواجهة تعرض «تغيّر السعر: من ← إلى» بزر قبول. بدون اللقطة لا قيمة يُقارَن بها
  فكان الرقم سيتغيّر بصمت.
- **§81** (اختيار مذخر يدوياً لدواء بلا تاريخ): كان `manualWarehouseId` يُهيّأ ولا
  يُكتب أبداً، فكان كل صنف بلا سعر يسقط في «غير قابل للإرسال». الآن يظهر منتقي
  «اطلب تسعيره من —» بدليل المذاخر النشطة، ويُرسَل الصنف بسعر متوقع 0 = «بلا سعر
  مطلوب» لا عرضاً مجانياً (§383).

**الاختبارات**: الحزمة الكاملة **526/526**. `tsc --noEmit` = 0.

**لم يُحذَف**: `WarehouseOrderClient.tsx` القديم صار يتيماً (لا يستورده أحد) لكنه
**غير متتبع في git**، فحذفه غير قابل للاسترجاع — تُرك للمستخدم عملاً بتحذير §37.

## المرحلة 5 — التحقق والتسليم ✅ (2026-09-10)

**فحص TypeScript**: `npx tsc --noEmit` = **0 أخطاء**.

**البناء**: `NEXT_BUILD_DIR=.next-verify npx next build` = **exit 0**، «Compiled
successfully in 3.0min». شُغِّل على مجلد بناء منفصل كي لا يمسّ `.next` الخاص بخادم
التطوير. والمسارات الخمسة الجديدة ظهرت في جدول المسارات فعلياً:
```
ƒ /dashboard/purchases/warehouse-orders/new          11.2 kB
ƒ /api/purchases/drug-search
ƒ /api/purchases/supplier-prices
ƒ /api/purchases/supplier-prices/history
ƒ /api/admin/warehouses/[id]/supplier-links
```
البناء هو ما يكشف أخطاء حدود الخادم/العميل التي لا يراها `tsc` — وهي الخطر الحقيقي
هنا لأن مكوّنات `'use client'` تستورد من `app/lib/*`. لم يظهر أي خطأ.

**الاختبارات**: الحزمة الكاملة **526/526** في 32 ملفاً. اختبارات هذه الميزة:
| الملف | اختبارات |
|---|---|
| `supplier-price-comparison.test.ts` | 29 |
| `warehouse-order-grouping.test.ts` | 21 |
| `supplier-warehouse-link.test.ts` | 11 |
| `need-line.test.ts` | 8 |
| `drug-scope.test.ts` | 5 |

**ESLint**: لا يوجد إعداد ESLint في المشروع إطلاقاً (رغم ذكر `npm run lint` في
CLAUDE.md) — `npx eslint` يفشل بـ«couldn't find a configuration file». فحص الأنواع
والبناء هما البوابة الفعلية هنا.

**فحص ثبات النطاق**: لا يوجد في المستودع كله أي **قراءة** لـ`organizationId: null`
بلا `warehouseId`. المواضع الأربعة المتبقية: ثلاثة تعريفات (`GLOBAL_DRUG_SCOPE`،
ونوع `PharmacyDrugScope`، وداخل `pharmacyDrugScope` نفسها حيث `warehouseId: null`
على المستوى الأعلى)، وواحد `createMany` في `admin/drugs/import` (كتابة، والعمود
يأخذ افتراضه null). الثبات محفوظ.

**تحقق على البيانات الحية (قراءة فقط)**
- مسارات قرار الربط على موردين حقيقيين: مورد المؤسسة → `LINK`؛ مورد مؤسسة أخرى →
  `WRONG_ORG` (عزل المستأجرين)؛ مورد ثانٍ بعد ربط الأول → `CONFLICT_WAREHOUSE_HAS_SUPPLIER`؛
  إعادة ربط نفسه → `ALREADY_LINKED`. كلها كما تنص §6.2.
- المقارنة على «Dologan Denk tab» (8 موردين، 14 دفعة): الترتيب وكسر التعادل صحيحان.
- القاعدة تُغيّر الجواب في **81 دواءً** فعلياً.

**§149 (أمني)**: `app/api/warehouses/orders/route.ts:98-103` يتحقق أن الفرع المطلوب
يتبع مؤسسة المستخدم ويعيد 403 وإلا. المستخدم المقيَّد بفرع يتجاهل الخادم قيمة جسمه.

---

# ما لم يُختبَر ولا يجوز ادعاء إنجازه (§417)

**مسار الإرسال والاعتماد لم ينفَّذ قط من طرف إلى طرف.** السبب بنيوي لا برمجي:
**0 موردين مربوطين بمذاخر** في القاعدة الحية. وبما أن السطر لا يصير قابلاً للإرسال
إلا عبر مورد مربوط، فإن `classified.sendable` فارغ لكل دواء اليوم — شاشة المراجعة
ستُظهر **0 مجموعة** وكل الأصناف تحت «لم تُرسل إلكترونياً»، حتى ينشئ المشرف أول ربط.

لذلك **لم يُتحقق** من: وصول الطلب للمذخر، تسعيره، اعتماده على المورد المربوط
(§341)، ولا من النجاح الجزئي وإعادة المحاولة على شبكة حقيقية. منطق التقسيم
والمفاتيح مغطّى بـ21 اختبار وحدة، وهذا **ليس** بديلاً عن تجربة فعلية.

**الخطوة التشغيلية اللازمة**: من `/dashboard/admin/warehouses` ← أيقونة الربط ←
اختر المؤسسة ← اختر المورد المقابل للمذخر. بعدها تصبح الميزة قابلة للتجربة كاملة.

# قيود باقية موثّقة

1. **الوحدة غير مضمونة عبر مسارات الإدخال** — تُسمّى «وحدة المخزون» ولا يُدّعى
   شريط ولا باكيت (§139). لا تحويل بين الوحدات في هذه النسخة.
2. **27٪ من الدفعات بلا مورد** (2126 من 7794) — تظهر «مورد غير مسجّل» ولا تُنتج
   اختياراً تلقائياً.
3. **39 دواءً كلفتها متعارضة** في `scripts/cost-conflicts-review.csv`. لم يُوضع
   تحذير لكل دواء مشابه بالاسم لأن §140 تمنع اعتماد الاسم مفتاحاً إنتاجياً، وتعذّر
   بناء قائمة مراجعة بمعرّفات موثوقة من الملف وحده (§141) — يُوثَّق ولا يُدَّعى.
4. **موردون مكرّرون** (`درة الشرق` / `مدخر ادوية درة الشرق`، و`سماء البركة` /
   `مدخر ادوية سماء البركة`) — لا دمج تلقائي (§30). سيظهر المذخر مرتين بسعرين إن
   رُبط أحدهما فقط.
5. **لا فكّ ربط ولا إعادة توجيه** (§176).
6. **لا واجهة ربط من جهة المؤسسة** — اقتراح وجيه (صاحب الصيدلية وحده يعرف أي مورد
   هو أي مذخر) لكنه خارج نطاق الخطة التي حصرته في السوبر أدمن.
7. **`WarehouseOrderClient.tsx` القديم يتيم وغير متتبع في git** — لم يُحذف عملاً
   بتحذير §37؛ حذفه قرار المستخدم.
8. **لا أرشيف أسعار دائم ولا مسودة على الخادم ولا كيان أب للطلبات** (§31).

# ما اختُبر وما لم يُختبَر — تصنيف §411 الثلاثي

**اختُبر محلياً**: 526 اختبار وحدة (منها 74 لهذه الميزة)، `tsc --noEmit` = 0،
و`next build` = exit 0 مع ظهور المسارات الخمسة الجديدة في جدول المسارات.

**اختُبر بقراءات حية (قراءة فقط، بلا كتابة)**: مسارات قرار الربط على موردين
حقيقيين، ترتيب المقارنة على دواء بـ8 موردين، وأثر قاعدة «أرخص آخر سعر» على 81 دواءً.

**لم يُختبَر تكاملياً**: `vitest.integration.config.ts` **لم يُشغَّل** — يشترط
`TEST_DATABASE_URL` يشير إلى قاعدة محلية معزولة اسمها `faramace_readiness`، ولم
تُهيَّأ في هذه الجلسة. ولم تُستعمل قاعدة الإنتاج بديلاً عنها عملاً بـ§409.

**قيد إضافي على النطاق متعدد الفروع**: `resolveHistoryScope` يعيد كل فروع المؤسسة
لمستخدم على مستواها، لكن لا توجد في القاعدة الحية مؤسسة لها فرعان يحملان تاريخ
دفعات — فوسم «مصدره فرع آخر» في نافذة المقارنة **لم يُعرَض قط ببيانات حقيقية**.
النطاق صحيح بحكم بنائه، لكن أول عميل بفرعين هو أول من سيراه فعلياً.


## تصحيح تقرير الإنجاز وإصلاحات المراجعة — 2026-09-10

هذا القسم يصحح وصف المراحل السابقة بأنها مكتملة رغم غياب اختبار دورة الإرسال.

الإصلاحات المنفذة:

- تُحفظ الحمولة الكاملة لكل محاولة قبل الشبكة، بما فيها الفرع والأصناف وترتيبها والكميات والأسعار والملاحظات والمفتاح. تعذر الحفظ يمنع بدء الاتصال.
- استرداد SENDING يحولها إلى UNKNOWN، ويعيد الطلب الأصلي نفسه. لا تلصق حالة SENT قديمة بمجموعة جديدة لمجرد تطابق المذخر.
- تُصنّف أخطاء الخادم والوسيط والتعارض والنجاح ناقص البيانات كحالة غير مؤكدة. فشل المصادقة في محاولة لاحقة لا يمحو غموض محاولة سابقة.
- بعد بدء الإرسال تُقفل القائمة والملاحظات والفرع لمنع تغيير الحمولة. يمكن بدء قائمة جديدة صراحة بعد حسم النتائج. تحتفظ الجلسة بنتائج النجاح إلى ذلك الحين.
- استُبعدت السجلات الصفرية من تسلسل الأسعار المدفوعة، دون تجاوز القيم السالبة/المتعارضة باعتبارها أسعارًا سليمة. يوجد اختبار يثبت فوز 900 أمام 1100 رغم بونص أحدث.
- أصبحت المقارنة والسجل التفصيلي يجمعان النسخ المحلية والعالمية للصنف بعد مطابقة غير غامضة للباركود والاسم التجاري والعلمي، داخل المؤسسة فقط. لا تدمج الباركودات المتعارضة.
- البحث يجمع مخزون الفروع المسموح بها فقط، ويمنع تكرار النتيجة المحلية والعالمية المطابقة. الخادم يرفض تكرار الصنف أو غموض الباركود عند الإرسال أيضًا.
- الأسعار بلا توثيق للوحدة تُعرض كبيانات تاريخية غير مؤهلة للترشيح. أضيف تأكيد صريح لكل صنف بأن المستخدم راجع الأسعار ووحدتها قبل استعمالها للمقارنة؛ التأكيد خاص بهذه القائمة ولا يصحح قاعدة البيانات أو يثبت الوحدة تاريخيًا. يمكن طلب التسعير دون تمرير سعر تاريخي غير مؤكد.
- أصلح الاسترجاع البديل من فواتير COMPLETED عندما لا توجد إلا دفعات صفرية، وكذلك ترقيم السجل واستجابات البحث القديمة والطباعة اليدوية التي كانت تطبع مجموعات الإرسال أيضًا.

التحقق المنفذ على قاعدة PostgreSQL محلية مؤقتة ومعزولة باسم faramace_readiness:

- ربط مورد موجود مع سجل تدقيق واحد، ورفض الربط غير المصرح أو العابر للمؤسسات.
- إنشاء طلب بكتالوج مذخر فارغ، وفقدان الرد بعد حفظه، واسترداد الطلب نفسه دون تكراره.
- نجاح مذخر وفشل آخر، ثم استرداد الجلسة وإرسال المتبقي فقط.
- عرض سعر المذخر، واعتماده، واستلام فاتورة مدفوعة مع بونص، وظهور سعرها تحت النسخة المحلية للدواء أيضًا.
- عزل الفروع والمؤسسات، وتوحيد تاريخ الصنف المطابق، ورفض الغموض وتكرار البنود.

نجحت اختبارات الوحدات الكاملة: 547 اختبارًا في 33 ملفًا. نجحت اختبارات التكامل السابقة البالغ عددها 22، والاختبارات الثمانية الجديدة في tests/need-list.integration.test.ts. نجح فحص TypeScript. اختبار التكامل يستخدم هويات اختبار محقونة، ولا يختبر تسجيل الدخول الحقيقي أو المظهر عبر المتصفح.

لم تُنشأ طلبات أو روابط على قاعدة البيانات الحية، ولم تُصحح أسعارها أو تُدمج موردوها. حالات جلسات الإصدار القديم التي لا تحتوي حمولة أصلية لا تُستأنف بمفتاح جديد تلقائيًا؛ تعرض رسالة مراجعة للطلبات القائمة بدل المخاطرة بتكرارها.


---

## ملحق: ميزة وحدة التسعير (الطبقات الثلاث) — نُفِّذت

**المشكلة الجذرية واحدة:** الوحدة لا تُعلَن في أي مكان. `Batch.costPrice` وحدته
الشريط، والمذخر يسعّر بالباكيت، والحدّ بينهما كان يمرّر الرقم بلا تحويل.

**قرار ملزم من صاحب النظام:** لا تُحفظ أي قيمة لعدد الأشرطة في الباكيت — تُراجَع
معه أولاً. لذلك كل الأعمدة الجديدة NULL بلا استثناء، والسلوك المُنتَج فعلياً
اليوم هو **التوقّف المعلن** لا التخمين.

### الطبقة ١ — تخزين الوحدة (فارغةً)
- `GlobalDrug.unitsPerPack Int?` — مرة واحدة لكل باركود، **بلا** نسخة على
  `Inventory` (تصحيح المستخدم: التعبئة واحدة لكل الموردين، فنسختان = مصدرَا حقيقة).
- `WarehouseCatalogItem.priceUnit String?` + `unitsPerPack Int?`.
- `WarehouseOrder.priceUnit String?` — وحدة أسعار العرض، يعلنها المذخر عند التسعير.
- السكربت: `apps/web/scripts/apply-pack-units-ddl.mjs` (إضافي بالكامل، يتحقق بعد
  التنفيذ أن عدد الصفوف الحاملة لقيمة = صفر).

### الطبقة ٢ — التعبئة التلقائية (لكل دواء لا لكل مورد)
- `GET /api/inventory/last-cost?inventoryId=` — آخر كلفة (سعر شريط) + المورد +
  التاريخ + سعر البيع. مفتاحها **الدواء** لأن المورد لا يُختار تلقائياً عند فتح
  النموذج (تصحيح المستخدم).
- `add-batch-modal.tsx`: شريط «آخر كلفة مسجَّلة» بزر «استخدام» — لا تعبئة صامتة.
  وطريقتا إدخال: من الباكيت (الحالية) أو سعر الشريط مباشرةً؛ المحفوظ في الحالتين
  سعر الشريط. `stripsPerPacket` يبقى **حقلاً لحظياً لا يُحفظ**.

### الطبقة ٣ — حدّ المذخر/الصيدلية
- `app/lib/pack-units.ts` — `toStripPrice()` القاعدة الوحيدة: تحوّل فقط حين تكون
  الوحدة والتعبئة معلنتين ومتّفقتين، وإلا ترفض بسبب صريح
  (`UNDECLARED_PRICE_UNIT` / `UNKNOWN_UNITS_PER_PACK` / `UNITS_PER_PACK_CONFLICT`).
  11 اختباراً في `pack-units.test.ts`.
- اعتماد العرض (`api/warehouses/orders/[id]/route.ts`) يمرّ بها قبل بناء الفاتورة؛
  أي سطر غير محسوم يوقف الاعتماد كله برسالة تسمّي الصنف والسبب (`PRICE_UNIT_UNRESOLVED`).
- شاشة تسعير المذخر تعلن الوحدة؛ الأسعار في صفحة الدفعات وُسمت «للشريط» صراحةً.
- `supplier-prices/route.ts`: تأشيرة `unitVerified` صارت تُستغنى عنها تلقائياً
  لأي دواء سُجِّلت تعبئته — فتتقاعد الآلية كلما اتّسعت البيانات.

**الأثر الحيّ وقت التنفيذ:** طلب مذخر واحد مُعتمَد في تاريخ المنصة، و0 صف كتالوج
مذخر — فالمنع لا يمسّ عميلاً قائماً.

**متبقٍ (يحتاج المستخدم):** تشغيل `node scripts/apply-pack-units-ddl.mjs --apply`
بعد إيقاف خادم التطوير، ثم مراجعة أرقام التعبئة معاً وتعبئتها.


---

## ملحق ٢: تأكيد عدد الأشرطة (unitsPerPack) — نُفِّذ

**قاعدة الاستنتاج:** دفعات «دار التفاؤل» وحدها. في البدايات (حتى 2026-04) كان
يُشترى باكيت واحد لكل دواء، فكمية أول دفعة = عدد الأشرطة. الدفعات اللاحقة تُفحص:
مضاعفات الأساس تؤكّده، وغير المضاعفة تُسقطه.

**الدليل على صحة القاعدة** (وقد كنت أظنها خاطئة ثم أثبتت البيانات العكس): في
نافذة البدايات، الأشكال الوحدوية (قطرة/كريم/شراب) كميتها الأغلب 1 بنسبة 52%،
بينما الأقراص والكبسولات كميتها الأغلب 2 بنسبة 33% ثم 3 و4. اختلاف التوزيعين
حسب الشكل الصيدلاني لا يفسّره «حجم الشراء» — يفسّره أن الكمية تحمل التعبئة.
وفحص الهامش لاحقاً: 96% من المحمَّل هامشه سليم، والأدوية باقتراح «شريط واحد»
أسلم من غيرها (2% منهار مقابل 5%).

**ما نُفِّذ على الإنتاج:**
- عمود `GlobalDrug.unitsPerPackConfirmedAt` (إضافي، NULL للكل).
- حُمِّلت 1,187 قيمة مستنتَجة، ثم أُفرِغت 69 منها (أقراص/كبسولات باقتراح 1)
  بطلب صاحب النظام → **1,118 قيمة، صفر منها مؤكَّد**.

**السكربتات:** `load-units-per-pack.mjs`، `clear-tablet-single-strip.mjs`
(كلاهما بمعاينة/`--apply`، ولا يكتبان فوق قيمة مؤكَّدة أبداً).

**الآلية في الشيفرة:**
- `unitsPerPack` قيمة، و`unitsPerPackConfirmedAt` تحقّق إنسان — لا يُغني أحدهما
  عن الآخر. المشتركان بين كل الصيدليات بقرار صاحب النظام: أول تأكيد يُعتمد للجميع.
- نافذة الدفعة: خانة العدد تبدأ **فارغة** (لا 1) ما لم يكن مؤكَّداً، مع إشارة
  «يرجى التأكد» تختفي نهائياً بعد أول حفظ. الرقم المستنتَج يُعرض كاقتراح بزر
  خارج الخانة لا داخلها.
- أُلغي وضع «سعر الشريط مباشرة» — لم يعد له مبرّر بعد تحميل الأرقام.
- تغيير عدد مؤكَّد سابقاً يطلب إقراراً صريحاً (يمسّ كل الصيدليات).
- `addBatch` يكتب العدد + تاريخ التأكيد بعد نجاح الدفعة، بعد التحقق من أن صف
  المخزون يخصّ مؤسسة الطالب (الكتابة تمسّ صفاً عالمياً).
- بوابة `unitVerified` تقرأ `unitsPerPackConfirmedAt` لا مجرد وجود رقم.

**متبقٍ:** 35 دواءً منهار الهامش تحتاج مراجعة (منها `Angiosar-plus`: كلفة 5,045
مقابل بيع 5,000)؛ وصفحة تعرض غير المؤكَّدة مرتّبة بحركتها؛ وسطح المكتب والهاتف
ما زالا بآلة القسمة القديمة بلا إشارة تأكيد.

### إضافة: شاشة تأكيد التعبئة + حدّ الـ500

- `/dashboard/inventory/pack-units` (+ تبويب في مركز المخزون) — الأدوية غير
  المؤكَّدة مرتّبة بالمبيع خلال 90 يوماً، مع تأكيد فوري لكل صف. الترتيب بالحركة
  هو ما يجعل «المهم» معرَّفاً بالبيع لا بالانطباع؛ الانتظار وحده كان يترك أدوية
  تُطلب كل شهرين بلا تأكيد فصولاً.
- `GET/POST /api/inventory/pack-units` — الكتابة تتحقق أن الدواء في مخزون فروع
  الطالب قبل أي تعديل (الصف عالمي يراه الجميع).
- **حدّ الـ500:** كلفة تفوق سعر البيع بأقل من 500 دينار حالة مشروعة بشهادة صاحب
  النظام (سعر شراء تغيّر ولم يلحقه البيع). التأكيد عند الحفظ صار يقتصر على تجاوز
  الحد، وما دونه سطر إخباري لا يوقف الحفظ — إظهار تأكيد في حالة شائعة مشروعة
  يُعوِّد المستخدم على التخطي بلا قراءة فيضيع التحذير حيث يهمّ.
- أثر الحدّ على المراجعة: قائمة «الكلفة ≥ سعر البيع» 21 دواءً، منها 19 ضمن الحد
  المشروع و**2 فقط** تحتاج نظراً: `lacalte white repaior` (فارق 1,590)
  و`Pioxiga 5mg tab` (فارق 863).

### إصلاح: زمن الفتح + جلب سعر الباكيت تلقائياً

**التشخيص قياساً لا تخميناً:** الاستعلام نفسه 0.116ms، لكن الرحلة الفارغة إلى
Neon 183ms، والنافذة كانت تُطلق استعلامين متتاليين عند الفتح (~700ms) فوق زمن
المسار والمصادقة. البطء شبكة لا قاعدة، فالعلاج إلغاء الرحلة لا تسريعها.

- صفحة المخزون تحمّل `include: { batches: true, drug: true }` — أي أن
  `costPrice`/`createdAt`/`unitsPerPack`/`unitsPerPackConfirmedAt` **في اليد
  أصلاً**. مُرِّرت خصائصَ للنافذة (`lastCostOf` في inventory-table)، فتفتح
  مكتملة بلا انتظار. الجلب بقي مساراً احتياطياً لمستدعٍ لا يملك البيانات
  (الإدخال السريع بالباركود).
- **سعر الباكيت يُملأ تلقائياً** = كلفة الشريط × عدد الأشرطة، وأُزيل زرّ
  «استخدام هذا السعر». لا حقل جديد: الطرفان محفوظان فالضرب يردّ الرقم نفسه،
  وأي تعديل يحفظه الصيدلاني يصير «آخر سعر باكيت» تلقائياً في المرة التالية.
- تغيير عدد الأشرطة يُعيد اشتقاق السعر ما لم يكتبه المستخدم بيده
  (`packetTouched`/`stripsTouched` يحرسان كتابته من الدهس).
- وسم «جُلِب تلقائياً — عدّله إن تغيّر السعر» يميّز الرقم المشتق عن المكتوب.

**سبب وميض التحذير** (أُصلح قبله): `unitsConfirmed=false` كانت تعني «لم يصل
الجواب» لا «غير مؤكَّد»، فيومض التحذير ثانيةً على كل دواء. فُصلت الحالتان
بـ`unitsLoaded`؛ وتحذير يظهر ثم يختفي بلا سبب يُفقد كل التحذيرات مصداقيتها.

### إصلاح: الإدخال السريع بالباركود (المسار الأساسي للإدخال)

صاحب النظام: **كل** الأدوية تُدخَل عبر الإدخال السريع، فالمسار «الاحتياطي» في
الإصلاح السابق هو المسار الفعلي. الحل نفسه: المكوّن يستدعي
`/api/inventory/check-barcode` قبل فتح النافذة، فأُضيفت البيانات إلى جوابه بدل
رحلة ثانية — `unitsPerPack`/`unitsPerPackConfirmedAt` ضمن اختيار الدواء القائم،
وآخر كلفة من جلب الدفعات نفسه (جلب واحد بلا مرشّح رصيد ثم حساب الرصيد وآخر كلفة
في الذاكرة — Prisma لا يسمح باختيار العلاقة نفسها مرتين، والدفعة المنتهية مطلوبة
للسعر وإن لم تعد مطلوبة للرصيد).

**أُغلقت الفجوة:** النوافذ الثلاث للإدخال السريع صارت على المبدأ نفسه.
- `AddToInventoryModal`: خانة فارغة (لا افتراض 1)، إشارة تحقق حين لا تكون التعبئة
  مؤكَّدة، والعدد شرط للحفظ ويُرسَل إلى `add-to-branch` فيُكتب داخل المعاملة نفسها.
- `CreateDrugModal`: دواء جديد بلا رقم مقترَح — الخانة فارغة وموسومة، وما يُكتب
  يُحفظ **مؤكَّداً من لحظة الإنشاء** في `create-quick` (العلبة في يد الصيدلاني).
- كلا المسارين يتجاهل قيمة غير صالحة بدل أن يفسد تعبئة يرثها كل مستعملي الباركود.

### إيصال سطح المكتب والهاتف

**الخادم:** `app/api/inventory/add-batch/route.ts` (مسار المزامنة، يخدم سطح
المكتب والهاتف معاً) صار يقبل `unitsPerPack` ويكتبه داخل معاملة الدفعة، معتمداً
`inventory.drugId` بعد حراسة النطاق لا معرّفاً من جسم الطلب.

**سطح المكتب** (`InventoryPage.tsx` + `electron/main.ts` + `electron/sync.ts`):
خانة العدد تبدأ فارغة في نموذجَي الدفعة وإنشاء الدواء، والحفظ ممنوع بدونها،
والعدد يمرّ عبر IPC إلى حمولتَي المزامنة `add-batch` و`create-drug` (جسم الطلب
في sync.ts قائمة سماح صريحة، فأُضيف الحقل إلى الاثنين).

**الهاتف** (`app/(tabs)/inventory.tsx` + `services/api.ts`): يرسل مباشرة إلى
واجهات الويب، فالتغيير سطحي — خانة فارغة، حارس قبل الحفظ، و`unitsPerPack` في
المسارات الثلاثة (دفعة / إضافة لفرع / دواء جديد).

**فرق مقصود في نصّ الإشارة:** الويب يقول «لم يُراجَع من قبل» لأنه يعرف حالة
التأكيد من الخادم. سطح المكتب يعمل أوفلاين و`GlobalDrug` المحلي بلا عمود
للتعبئة، فلا يستطيع معرفة الحالة — ولذلك نصّه إرشاد («اعدُد أشرطة العلبة») لا
ادّعاء حالة. ادّعاء «غير مؤكَّد» بلا معرفة هو نفس عطب الوميض الذي أُصلح في الويب.

**أُغلقت الفجوة:** سطح المكتب صار يعرف حالة التأكيد أوفلاين.
- `prisma/schema.prisma` المحلي: `unitsPerPack` + `unitsPerPackConfirmedAt`.
- الترحيل عبر `runMigrations()` في `electron/db.ts` (ALTER TABLE ADD COLUMN
  الموجود أصلاً، يعمل عند كل إقلاع ويتجاهل العمود القائم) — فالنسخ المثبَّتة عند
  العملاء تُرقَّى ذاتياً بلا تدخّل.
- `/api/sync/products` صار يرسل الحقلين، و`pullProductsFromCloud` يخزّنهما.
- التمييز بين undefined وnull في السحب مقصود: خادم أقدم لا يرسل الحقل فتبقى
  القيمة المحلية (undefined = «لا تغيّر» في Prisma)، بينما null صريح يمسحها.
  بدونه كانت مزامنة مع نسخة قديمة تمحو تأكيداً وصل سابقاً.
- الإشارة صارت تقول الحقيقة («لم يُراجَع من قبل» + الرقم المقترح) بدل الإرشاد
  العام، وسعر الباكيت يُعبَّأ تلقائياً من كلفة الشريط × العدد للمؤكَّد — نفس دورة
  العمل في الويب.

**الاختبارات:** `inventory-draft.ts` كان يعدّ `stripsPerPacket: 1` قيمة افتراضية
«بلا إدخال»؛ بعد أن صارت الخانة تبدأ فارغة صار 1 رقماً كتبه الصيدلاني بعد عدّ
العلبة، فحُذف من FIELD_DEFAULTS وأُضيف اختبار يحرس ذلك (35 اختباراً تمرّ).

### صفحة الطلب: توثيق التعبئة داخل قائمة الاحتياج

مشكلتان بلّغ عنهما صاحب النظام تبيّن أنهما واحدة: (١) لا سبيل لتوثيق التعبئة من
هنا، (٢) الموردون يظهرون في المقارنة بلا إمكانية اختيار أحدهم.

السبب واحد: كل الأسعار غير مؤهلة بسبب UNVERIFIED_UNIT، وزرّ «اختر» لا يُرسَم إلا
للمؤهّل. فمعالجة السبب تحلّ الاثنتين معاً.

- خانة «عدد أشرطة الباكيت» + زر «توثيق» داخل عمود الكمية لكل دواء غير موثَّق،
  تحفظ عبر `POST /api/inventory/pack-units` ثم تُعيد جلب الأسعار — فتصير مؤهلة
  وقابلة للاختيار فوراً.
- `/api/purchases/supplier-prices` صار يعيد `unitsPerPack` لكل دواء، فتعرض
  الواجهة **سعر الباكيت المشتق** (سعر الشريط × العدد) بجانب سعر الشريط في نافذة
  المقارنة — المذخر يسعّر بالباكيت، فالمقارنة بسعر الشريط وحده تترك المشتري
  يحوّل ذهنياً.
- تأشيرة «لهذا الطلب فقط» بقيت خياراً ثانوياً مصغَّراً لا علاجاً رئيسياً.
- `QUALITY_FIX.UNVERIFIED_UNIT` عُدِّل ليدلّ على العلاج الدائم بدل التأشيرة.

**ما لا يُحلّ بهذا (وهو صحيح):** سطر `UNKNOWN_SUPPLIER` يبقى غير قابل للاختيار
لأن الدفعة غير منسوبة إلى مورد أصلاً — لا يوجد مورد ليُختار، وعلاجه في صفحة
الدفعات. ولمثل هذه الحالات تبقى قائمة «اختر جهة الطلب» متاحة.

### رفع اشتراط الترقية المسبقة قبل الإرسال للمذخر

**سبب التغيير (معرفة سوق من صاحب النظام):** الرمز ثابت في السوق العراقي ولا
يتكرر لدواءين، و21% من الكتالوج العالمي أصلاً برموز غير GTIN (`86019`،
`QR-3103-2013-1`، بل أسماء أدوية كاملة). فاشتراط وجود صفّ عالمي مسبقاً كان
ترتيباً إدارياً يمنع الصيدلاني من طلب دواء يحتاجه اليوم، لا ضرورة تقنية:
تسعير المذخر يدوي، والشحن يطابق **بالباركود** ويتخطّى غير المتتبَّع بدل أن يفشل.

- `POST /api/warehouses/orders`: حين لا يوجد صفّ عالمي بالباركود، يُنشأ من صفّ
  المؤسسة نفسه عند أول إرسال (مع التقاط تسابق الإنشاء وإعادة الجلب). فالكتالوج
  يتوحّد من العمل اليومي: أول صيدلية ترسل الصنف تُنشئ الصفّ المشترك، ومن بعدها
  يُطابَق عليه بالباركود تلقائياً عبر `MAPPED_BY_BARCODE`.
- `classifyNeedLines`: المنع صار مقصوراً على مانعَي الهوية الحقيقيين — **بلا
  باركود** (لا مفتاح تعريف)، و**مطابقة غامضة** (صفّان عالميان بنفس الرمز — وهو
  خطأ بيانات يحتاج دمجاً، لا حالة طبيعية، وإرساله قد يصيب الصنف الخطأ).
- خلية الحالة وقائمة «جهة الطلب» تعتمدان الباركود لا وجود الصفّ العالمي، و
  `NO_GLOBAL_MATCH` لم يعد يحمل نصّ تحذير.
- 3 اختبارات جديدة تحرس القاعدة الجديدة ومانعَيها.

**التكلفة المتبقية المعلومة:** أول من يرسل يثبّت الاسم التجاري المشترك (مسألة
جودة تسمية لا هوية)، وخطأ مطبعي في الرمز صار يُطابق دواءً حقيقياً آخر بدل أن
يُرفض — علاجه المقترح سطر تأكيد قبل الإرسال يعرض الاسم المفهوم من الرمز.

### تغيير جهة الطلب متاح دائماً

كان مُنتقي جهة الطلب يُرسم بشرط `!opt?.comparable` — أي يظهر فقط حين لا سعر
مؤهل. فوجود السعر يصير سبباً لإخفاء أداة القرار، وهو خلط بين سؤالين:
«بكم اشتريتُ وممّن؟» تاريخ يُقيَّد بمن له سعر بحق، و«إلى أين أرسل هذا الطلب؟»
قرار تجاري لا سبب لتقييده بالتاريخ — المذخر يحمل آلاف الأصناف، وعدم شرائك صنفاً
منه من قبل لا يقول شيئاً عن توفّره عنده، بل طلب التسعير منه هو جوهر البوابة.

- زرّ «تغيير جهة الطلب» تحت المورد المشتقّ يفتح نفس المنتقي (مذاخر تتعامل معها /
  مذاخر أخرى / موردون محليون) بصرف النظر عن وجود سعر.
- «إلغاء والعودة إلى الأرخص» يمسح الاختيار اليدوي ويعيد الجهة المشتقّة.
- اختيار جهة بلا سعر لهذا الصنف يُفرِغ السعر من السطر ويخرجه من الإجمالي — وهو
  صحيح لا ناقص: الطلب حينها طلب تسعير، والرقم الحقيقي يأتي من المذخر. والعنوان
  «إجمالي الأصناف المسعّرة تقديرياً» يغطّي ذلك أصلاً (§82).

### إصلاح P2028: انتهاء مهلة معاملة إنشاء الطلب

**العطب:** `Transaction already closed ... timeout 5000 ms, however 5393 ms passed`
عند إرسال الطلب. السبب عدد الرحلات لا ثقل الاستعلامات.

- **داخل المعاملة:** حلقة تولّد رقم الطلب `WO-${uuid}` وتفحص تفرّده حتى خمس مرات
  — خمس رحلات متتالية إلى Neon بلا فائدة: الاحتمال معدوم، والعمود فريد في
  المخطط فالقاعدة ترفض التكرار وتتراجع المعاملة ويعيد العميل بنفس مفتاح
  idempotency. حُذفت الحلقة، وسقط معها حارس `WO_NUMBER_GENERATION_FAILED`
  لأن القيمة لم تعد قد تكون null.
- **مهلة المعاملة** رُفعت إلى 20 ثانية (maxWait 10) — المعاملة قفل + فحص +
  إنشاءان على قاعدة بعيدة، و5 ثوانٍ ضيّقة على اتصال بطيء. القفل مفتاحه
  idempotencyKey فلا تزاحم بين الطلبات المختلفة.
- **قبل المعاملة:** حلقة تحليل الأصناف كانت تستعلم مرة أو مرتين **لكل صنف**
  بالتتابع (طلب من 20 صنفاً = 40 رحلة). استُبدلت باستعلامين مجمَّعين
  (الصفوف العالمية + صفوف المؤسسة لكل الباركودات) وخريطتين في الذاكرة؛ ولا يبقى
  استعلام داخل الحلقة إلا لإنشاء صفّ عالمي مفقود، وهي حالة تحدث مرة واحدة لكل
  دواء في تاريخ المنصة. والخريطة تُحدَّث بعد الإنشاء كي تراه بقية الأسطر.

### حسم الأشكال الوحدوية دفعةً واحدة

**سياق:** ظننتُ أن حجم العبوة مكتوب في أسماء الأدوية (`4tab`، `30sach`) كما رأيته
في فواتير المذاخر، فصحّح صاحب النظام. والقياس أثبته: 212 من 4,120 فقط (5.1%)
يطابق أي نمط، وأغلبها إيجابيات كاذبة — `Adol 125 supp` جرعة لا عدد،
`Vitamin B6 amp` اسم فيتامين، `wound dressing 10*20` أبعاد بالسنتيمتر. والسبب أن
المذخر يسمّي الصنف كما يستلمه من المستورد (بحجم العبوة) والصيدلاني كما ينطقه على
الرف (اسم وجرعة) — فتعميمي من ثلاث فواتير على 4,120 صفاً كان استقراءً خاطئاً.

**البديل المُنفَّذ:** `app/lib/dosage-form.ts` — `classifyDosageForm` يميّز
SINGLE_UNIT (زجاجة/أنبوب/قطرة، تعبئتها 1 يقيناً) عن MULTI_UNIT (أقراص/كبسول/
أمبول/أكياس) عن UNKNOWN. `amp` و`vial` و`supp` و`sachet` مستبعدة من الوحدوي
عمداً: `Methycobal 500mcg 10amp` علبة فيها عشر أمبولات. والتداخل يُرجَّح للمتعدد
لأن خطأ التعبئة يضخّم الكلفة بينما غيابها يوقف التسجيل للمراجعة فقط.

**عطب كشفته الاختبارات:** `` لا يطابق بين رقم وحرف، فـ`100cap` و`10amp`
و`3sachets` كانت تفلت من التصنيف — وهي أكثر الصيغ وروداً. استُبدل الحدّ الأيسر
بـ`(?<![a-z])`، فانتقل 158 صنفاً من «غير مصنّف» إلى «متعدد» حيث مكانها الصحيح.

**الأرقام بعد التصحيح** (من 4,111 غير مؤكَّدة): 922 وحدوي · 1,145 متعدد ·
2,044 غير مصنّف.

**الواجهة:** مرشّح «شكل وحدوي (التعبئة 1)» في `/dashboard/inventory/pack-units`،
وخانات تحديد، و«تأكيد المحدد كتعبئة 1». التحديد يبدأ **فارغاً** عمداً: خطوتان
(تحديد ثم تأكيد) لأن الرقم معروف سلفاً لكن التأكيد مشترك بين كل الصيدليات ولا
يُسأل عنه أحد بعده. والكتابة الجماعية مشروطة بـ`unitsPerPackConfirmedAt: null`
فلا تدهس تأكيداً سبقها، وتتحقق من ملكية المخزون باستعلام واحد وتتجاهل ما ليس
للطالب بدل إفشال الدفعة كلها.

## المرحلة: وقف اختلاق رقم الدفعة وتاريخ الانتهاء عند الاستلام

خرج هذا من سؤال «تعبئة رقم الدفعة والصلاحية من شحنة المذخر». وقبل بناء التعبئة
المسبقة تبيّن أن الوصف الأولي للفجوة كان **أخف من الواقع**: لم تكن المشكلة أن
الصيدلاني يكتب القيمتين بيده، بل أن شاشة الاستلام كانت **تخترعهما**.

`apps/web/app/dashboard/purchases/[id]/receive/page.tsx` كانت تبدأ بـ
`BAT-<تاريخ اليوم>-<رقم>` وتاريخ انتهاء = اليوم + سنة، ثم في `handleConfirm`
مولّد احتياطي ثانٍ (`'BATCH-' + yyyyMMdd`). ومن يضغط «تأكيد» بلا تعديل يكتب
تاريخ صلاحية مُختلَقاً في `Batch.expiryDate` — الحقل الذي يحكم الصرف (أول منتهٍ
أول مصروف) وتقرير قرب الانتهاء. والحارس في `purchase-receipt.ts` يشترط الحقلين
فعلاً، لكن «اليوم + سنة» تاريخ مستقبلي صالح شكلاً فيمرّ منه — أي أن الواجهة كانت
تُبطل حارس الخادم بقيمة تُرضيه.

**قياس الأثر في الإنتاج** — البحث عن `BAT-` وحده غير كافٍ: من ينسخ رقم الدفعة
الحقيقي ويترك التاريخ الافتراضي يُنتج صفاً برقم واقعي وتاريخ مُختلَق. فالتوقيع
الصحيح على التاريخ لا على الاسم: `expiryDate = createdAt + سنة بنفس الشهر واليوم`.
النتيجة: **26 دفعة من 7,980** (دار التفاؤل 15، البكرلي 4، تجريبية 7). ومعظم
الـ15 مستحضرات تجميل بأرقام دفعات عشوائية من `generateBatchNumber()` لا من هذه
الشاشة، فبعضها قد يكون صلاحية سنة حقيقية — لكن السبعة في «تجريبية» من هذه الشاشة
بالضبط، وخمسة منها من تجربة طلب مذخر يوم 2026-09-17.

**التغيير:** الحقلان يُعبّآن من `PurchaseItem.batchNumber/expiryDate` حصراً (وهما
موجودان في المخطط اختياريين) وإلا يبقيان فارغين؛ حدّ أحمر على الحقل الناقص؛
`incompleteCount` يعطّل زر التأكيد ويشرح السبب؛ والمولّد الاحتياطي في
`handleConfirm` حُذف واستُبدل بحارس ثانٍ.

**التطبيقات الأخرى:** لا شاشة استلام في سطح المكتب. وفي الهاتف
(`apps/mobile/app/purchases/[id]/receive.tsx`) كان التاريخ سليماً ابتداءً —
يبدأ فارغاً ومُتحقَّقاً منه («no assumed expiry» في توثيقه) — لكن رقم الدفعة كان
يُولَّد `B<ts><nn>`، وهو ما كان **يُبطل مُتحقِّقه الخاص** `batch: !item.batchNumber.trim()`
فلا يمكن أن يُطلِق أبداً. صار يُبذَر من بند الشراء أو فارغاً، فعاد المتحقق عاملاً.

`tsc` نظيف في الويب والهاتف، و617 اختباراً تمرّ.

**لم يُنفَّذ بعد (بانتظار قرار):** التعبئة المسبقة من شحنة المذخر نفسها. المعطيات
جاهزة: `WarehouseStockMove{batchId, orderId, type:'SHIPMENT'}` يربط الطلب بدفعات
`WarehouseBatch` التي تحمل `batchNumber`/`expiryDate` **إلزاميين**. القيد الذي
يجب احترامه: `allocateFEFO` قد يخصم سطراً واحداً من دفعتين بتاريخَي انتهاء
مختلفين، بينما `purchase-receipt.ts` ينشئ دفعة واحدة لكل سطر — فالتعبئة المسبقة
تصحّ **فقط** حين يؤول التخصيص إلى `(batchNumber, expiryDate)` واحد؛ وما عداه
يُعرَض للصيدلاني بلا تعبئة. والرابط من الفاتورة إلى الطلب مدفون في
`WarehouseOrderEvent(APPROVED).payload.purchaseId`، فقد يلزم `Purchase.warehouseOrderId`
اختيارياً (إضافة آمنة على قاعدة حيّة).

## المرحلة: وحدة الكمية عند حدّ المذخر/الصيدلية (تصحيح مالي)

**الإقرار الذي حسم الأمر (صاحب النظام):** «المذخر دائما يكون باكيت في البيع الخاص
به». فـ`WarehouseOrderItem.quantity` عدد **باكيتات** و`quotedPrice` سعر **باكيت**،
بينما `Batch.quantity` عدد **أشرطة** و`Batch.costPrice` سعر **شريط** في كل بقية
النظام.

**العطب:** مسار الاعتماد كان يحوّل نصف المعادلة — يقسم السعر على عدد الأشرطة
ويمرّر الكمية كما هي:
`pricedItems.push({ ...it, effectivePrice: conversion.stripPrice })`.
فلصنف باكيته شريطان، طلبُ 9 باكيتات بسعر 500 كان ينزل للرفّ 9 أشرطة بدل 18،
ويُفوتَر 2,250 بدل 4,500 — بينما `WarehouseOrder.totalAmount` (المحسوب بـ
`effectiveLine` من سعر الباكيت) يبقى 4,500. رقمان لنفس الطلب. والشيفرة نفسها
تدّعي في تعليقها أن `plan.total` «الثابت الذي يبقي فاتورة الشراء وفاتورة المذخر
متفقتين ماليّاً دوماً» — وهو ادّعاء كان كاذباً كلما كان عدد الأشرطة > 1.

**التصحيح:** تحويل مزدوج متلازم في نفس حلقة الحارس — الكمية تُضرَب والسعر يُقسَم
بنفس `conversion.unitsPerPack` (لا `drug.unitsPerPack` مباشرة: `toStripPrice` هو
من سوّى التعارض بين تعبئتنا وتعبئة المذخر، واستخدام رقم آخر يفكّ الزوج بصمت):
`(N × U) × (P ÷ U) = N × P`. و`bonusQuantity` يُضرَب بنفس المعامل — وهو المصيدة
الحقيقية: سطر البونص يُبنى بكلفة صفر فلا يمرّ بمسار تحويل السعر إطلاقاً، فلولا
ضربه صراحةً لوصل البونص للرفّ منقوصاً بنفس المعامل.

**ما لم يُمسّ عمداً:** خصم مخزون المذخر (`deductStockForShipment` يخصم من
`WarehouseBatch` بالباكيت، ومخزون المذخر بالباكيت — الطرف متسق)، و`effectiveLine`،
و`WarehouseOrder.totalAmount`، وشاشة «طلب حسب الاحتياج».

**الأثر الظاهر للصيدلاني:** شاشة الاستلام ستعرض الكمية المتوقَّعة `N × عدد الأشرطة`
(18 بدل 9 في المثال). هذا هو السلوك الصحيح بعد التصحيح، لكنه تغيّر رقم مرئي قد
يُقرأ خطأً كعطب جديد.

**الإثبات:** `warehouse-money-path.test.ts` — لـ U ∈ {1, 2, 10} يثبت
`quantity === N×U` و`cost === P÷U` و`total === N×P`، والأهم
`expect(plan.total).toBe(quoteTotal)` حيث `quoteTotal` محسوب استقلالاً بمسار
`effectiveLine`. 629 اختباراً تمرّ (من 624).

**حدّ الإثبات (بصراحة):** الاختبار يعيد بناء خط أنابيب المسار في دالة مساعدة لأن
مسار الـAPI غير قابل للاختبار الوحدوي. فهو يوثّق القاعدة ويحرسها، لكنه لن يلتقط
انحرافاً لو عُدِّل المسار وحده دون الاختبار.

**متبقٍّ بانتظار قرار:** شاشة «طلب حسب الاحتياج» تعرض إجمالي السطر بـ«سعر الشريط
من تاريخ الصيدلية × الكمية»، والكمية باكيتات — فالإجمالي المعروض أصغر من الفاتورة
بمقدار عدد الأشرطة. تصحيحه يتطلب الضرب بعدد أشرطة غير مؤكَّد في أغلب الأصناف
(1,126 لها رقم، 9 مؤكَّدة)، أي معالجة رقم خاطئ برقم مظنون. لم يُنفَّذ.

**بقايا عشرية:** حين لا يقسم عدد الأشرطة سعر الباكيت بلا باقٍ (U=3, P=100) يصير
سعر الشريط كسراً دورياً ويفترق الإجمالي بمقدار epsilon (~1e-13). سلوك سابق لهذا
التصحيح آتٍ من قسمة السعر وحدها، ولم يُضَف تقريب.

## المرحلة: الاعتماد الآلي عند تطابق العرض (المهمة ٣)

**القرار:** الخيار «أ» — اعتماد تلقائي حين لا يتغيّر شيء. آلة الحالات لم تُمسّ:
`QUOTED → APPROVED` يقع كما هو، والفاتورة والذمة وفحص حدّ الائتمان في نفس اللحظة.
ما اختفى هو النقرة البشرية في الحالة المتطابقة وحدها.

**الشرط (`shouldAutoApprove` في warehouse-quote.ts، دالة نقية مُختبَرة):**
`partial === 0 && outOfStock === 0 && changedPrices === 0 && available === judgedItemCount && judgedItemCount > 0`.

**التحفّظ مقصود:** `changedPrices` يقارن `quotedPrice !== ctx.unitPrice`، و`unitPrice`
صفرٌ حين لا تاريخ أسعار، وقد يكون سعر شريط مقابل عرض بالباكيت. الحالتان تُحسبان
«تغيّراً» فيسقط الاعتماد الآلي للقرار اليدوي. يتّسع نطاق الأتمتة تدريجياً مع
اكتمال تأكيد التعبئة وتراكم التاريخ — وهذا الاتجاه أأمن من عكسه.

**الاستخراج (الخطوة 1، إعادة هيكلة بلا تغيير سلوكي):** كل منطق الاعتماد نُقل من
جسم `POST` في `api/warehouses/orders/[id]/route.ts` إلى
`app/lib/warehouse-order-approval.ts`:
`approveWarehouseOrder({orderId, actorType, actorName}) → ApprovalOutcome`.
لا يرمي للرفض التجاري بل يُرجع `{ok:false, code, message, status}`؛ والمسار
يعيد بناء نفس شكل الاستجابة ورموزها الأصلية بالحرف. أُثبت مرور 635/42 على
الاستخراج وحده قبل إضافة أي جديد — كي يُعرَف مصدر أي خلل لاحق.

**ثلاث قواعد أمان في مسار التسعير:**
1. معاملة منفصلة **بعد** التزام معاملة التسعير — فشل الاعتماد لا يُسقط عرضاً سُجِّل.
2. الاعتماد الآلي لا يُفشل طلب المذخر أبداً: أي `ok:false` أو استثناء يُسجَّل
   ويُترك الطلب `QUOTED` وتُعاد استجابة التسعير الناجحة.
3. حدّ الائتمان لا يُخترَق: صيدلية متجاوِزة لا تُعتمَد آلياً بل ترجع للقرار اليدوي.
وحتى إعادة قراءة الطلب بعد الاعتماد مُغلَّفة بحمايتها، فلا تحوّل نجاحاً إلى 500.

**صدق السجلّ والرسائل:** الحدث يُسجَّل بـ`actorType: 'SYSTEM'` مع
`actorName: 'اعتماد آلي — عرض المذخر طابق طلب الصيدلية تماماً'`
(تحقّق أن `OrdersTrackClient.tsx` يعرض أي قيمة غير PHARMACY/WAREHOUSE كـ«النظام»).
وصُحِّحت رسالتان كانتا ستكذبان: توست المذخر «أُرسل العرض للصيدلية»، وإشعار
«اعتمدت الصيدلية…» المُرسَل لمن يعلم أن لا صيدلاني لمسه.

**تصحيح بعد المراجعة (توسيع صلاحية غير مقصود):** الاستخراج بدّل مصدر المنظمة من
`tenantCtx.organizationId` إلى `order.branch.organizationId` (وجب ذلك لأن بوابة
المذخر بلا سياق صيدلية). لكن `warehouseOrderScope` يعيد `{}` لـ`SUPER_ADMIN` —
كل الطلبات — فصار بوسع مدير منصة بلا منظمة اعتماد طلب أي صيدلية وإنشاء فاتورة
وذمة عليها، وهو ما كان `BLOCKED_NO_ORG` يمنعه. أُعيد الحارس صراحةً في المسار
(`if (!tenantCtx.organizationId) → 403`) فبقيت الدالة صالحة للمسارين دون أن
يتسرّب توسيع صلاحية كأثر جانبي لإعادة هيكلة.

641 اختباراً تمرّ (من 635)، `tsc` نظيف.

## المرحلة: صدق شاشة الكتالوج تجاه المذخر غير المتتبِّع

**السياق:** بوابة المذاخر ستُقدَّم مجاناً ليطلب الصيدلانيون إلكترونياً. فأغلب
المذاخر ستُدخل الأسعار ولن تُدخل مخزوناً — ربما أبداً.

**قاعدة الشحن (سليمة ولم تُمسّ):** `decideStockTracking` ذاتية الضبط **لكل صنف**:
صنف بلا أي صف دفعة = لم يبدأ التتبّع ← يُشحَن بلا خصم وبلا تسجيل حركة إطلاقاً.
فالشحنات السابقة **لا تترك أثراً** في دفتر المخزون، وحين يُدخل المذخر رصيده
الافتتاحي لاحقاً يُؤخذ كما هو بلا طرح رجعي — وهو أصلاً سيجرد رفّه اليوم، أي بعد
ما شحنه، فلا ازدواج. (حالة الإنتاج عند الكتابة: 4 مذاخر، صفر كتالوج/دفعات/حركات.)

**العطب:** شاشة الكتالوج تحسب `deriveAvailability = isAvailable && sellable > 0`،
فمذخر أدخل أسعاره بلا دفعات يرى **كل** أصنافه «غير متوفر» ويستنتج أن حسابه معطّل
أو أن الصيدليات لا تراه. والاستنتاج كاذب: `isAvailable` لا يحرس شيئاً في مسار
الطلب عند الصيدلية (تُحقّق منه في `warehouses/orders/route.ts` و`supplier-prices`).

**العلاج — على مستوى الصنف لا الشاشة**، لأن العمود هو ما يكذب لا الصفحة:
- لم يبدأ التتبّع → شارة محايدة «غير محسوب» مع `title` يشرح أنها لا تمنع الطلب.
- بدأ التتبّع → «متوفر فعلياً»/«غير متوفر» كما هو تماماً بلا تغيير.
- لافتة إعلامية تظهر فقط حين لا صنف واحد بدأ تتبّعه، تقول: التتبّع لم يبدأ؛ و«غير
  محسوب» لا يخفي شيئاً والطلبات تصل وتُشحن؛ والتتبّع يبدأ **لكل دواء** بمجرد
  إدخال دفعته بلا مفتاح تفعيل؛ وتحذير بأن نقص الرصيد بعد بدء التتبّع **يُفشل
  الشحنة كاملة** لا السطر وحده، فليطابق الرصيد الافتتاحي الرفّ.

**التفصيلة الحاسمة:** الإشارة تُشتقّ من `summary.batchCount` (العدد الخام
`batches.length`) عبر `decideStockTracking` نفسها — لا من `sellableQuantity`.
لو اشتُقّت من الكمية لانقلب كل صنف نفد رصيده إلى «غير محسوب»، فتحوّل إصلاح رسالة
مضلِّلة إلى **إخفاء نفاد حقيقي** — أسوأ من الأصل. وباستدعاء الدالة ذاتها لا
تستطيع الشاشة الانحراف عن قاعدة الشحن مهما عُدِّلت لاحقاً.

**اتساق مسار إعادة التحميل:** `/api/warehouse-portal/stock` كان يُرجع `isTracked`
بنفس الاشتقاق أصلاً، فتحقّق التطابق ولا يقفز الصفّ من «غير محسوب» إلى «غير متوفر»
بعد الإضافة/الاستيراد.

641 اختباراً تمرّ (بلا تغيير — منطق `.tsx` خارج تغطية vitest)، `tsc` نظيف.

## إصلاح: هوية الصيدلية في شاشة طلبات المذخر

**العطب:** كل طلب وارد كان يُعرَض بـ`من: {o.branch.name}` فقط. وفي الإنتاج
**الستّ مؤسسات كلها** لفرعها نفس الاسم «الفرع الرئيسي» (دار التفاؤل، تجريبية،
صيدلية القرنة، نظام تعريفي، البكرلي النموذجية، أديم الحياة) — فالمذخر يقرأ سطراً
واحداً حرفياً على كل طلب، ويشحن بضاعة ويمنح ائتماناً دون معرفة العميل. السبب أن
`branch: { select: { name: true } }` لا يجلب المؤسسة أصلاً.

**النمط الصحيح كان موجوداً مرتين** ولم يُخترَع جديد: `print/picking/[orderId]`
و`print/invoice/[id]` يعرضان «العميل» من المؤسسة و«الفرع» من الفرع. (الفاتورة
المطبوعة تأخذ العميل من `invoice.organization` مباشرة، فسليمة بلا تغيير.)

**التصحيح:** وُسِّعت المصادر الثلاثة التي تغذّي `warehouse/orders/OrdersClient.tsx`
— `api/warehouse-portal/orders/route.ts`، و`api/warehouse-portal/orders/[id]/route.ts`،
و`warehouse/orders/page.tsx` — إلى `branch: { select: { name: true, organization:
{ select: { name: true } } } }` بنص متطابق حرفياً في الثلاثة. والعرض: اسم المؤسسة
بارزاً (`text-foreground`) والفرع ثانوياً بين قوسين، في صفّ القائمة وعنوان نافذة
المراجعة ورأسها.

**لماذا اشتُرط تطابق المصادر الثلاثة:** الواجهة تعيد الجلب بعد التسعير وبعد الشحن.
لو وُسِّع اثنان ونُسي ثالث لظهر اسم الصيدلية عند الفتح ثم اختفى بعد التسعير — عطب
وميض من نفس العائلة المتكررة في هذا المستودع.

**ملاحظة على حدود التحقق:** `initialOrders` و`refreshOne` يمرّان عبر
`JSON.parse(JSON.stringify(...))`، فالأنواع تُمحى و`tsc` **لا يلتقط** مصدراً
منسيّاً. التحقق الفعلي كان بالمقارنة النصية للمصادر الثلاثة وبمسح كل استعمالات
`branch.` في الواجهة (أربعة، كلها عبر `?.organization?.name ?? "—"`).

641 اختباراً تمرّ، `tsc` نظيف.

## المرحلة: رقم الدفعة وتاريخ الانتهاء يُعلنهما المذخر عند التسعير

**الفجوة:** المذخر المتتبِّع لمخزونه تُشتقّ دفعته وصلاحيته من تخصيص FEFO عند
الشحن وتصل الصيدلية معبّأة. أما غير المتتبِّع — الحالة الغالبة، فالبوابة تُعطى
مجاناً ومعظم المذاخر لن تُدخل دفعات — فلا وسيلة لديه إطلاقاً لإبلاغ الصيدلية، إذ
`WarehouseOrderItem` بلا هذين العمودين ومسار الشحن يرسل الحالة وحدها.

**قرار صاحب النظام:** الإدخال **عند التسعير** مع السعر والبونص (لا عند الشحن)،
مع علمه بأن ما يُكتب حينها وعدٌ قبل انتقاء البضاعة من الرفّ.

**الأسبقية عند الاستلام (الأهمّ):** الأصدق أولاً —
1. `shippedPrefill.kind === 'SINGLE'` (تخصيص FEFO الفعلي) — ما خرج من الرفّ.
2. `PurchaseItem.batchNumber/expiryDate` — وعد المذخر عند التسعير.
3. فارغ.
وحين `MULTIPLE` يبقى الحقلان **فارغين** ولا يُستعاد الوعد: معرفتنا أن البضاعة من
دفعتين تجعل الرقم الواحد خاطئاً يقيناً لبعض العلب. والفاتورة العادية بلا
`shippedPrefill` أصلاً فيبقى `PurchaseItem` مقدَّماً فيها كما كان.

**العلامتان تفرّقان بين الادّعاءين:** «من شحنة المذخر الفعلية» مقابل «من تصريح
المذخر عند التسعير (وعد لا إثبات شحن)». ومشروطتان بـ`supplier.warehouseId` —
وإلا نُسِب رقمٌ كتبه الصيدلاني بيده في فاتورة مورّد عادية إلى مذخر لا علاقة له.

**قيود التنفيذ:**
- الحقلان اختياريان بالكامل؛ مذخر يتركهما فارغين يعمل كما اليوم.
- تاريخ ماضٍ/غير صالح **يُرفَض عند التسعير** برسالة تسمّي الصنف، لا يُقبل صامتاً:
  `purchase-receipt.ts` سيرفضه عند الاستلام على أي حال، فقبوله هنا يؤجّل الفشل
  إلى لحظة أسوأ (بعد وصول البضاعة).
- `OUT_OF_STOCK` يُصفَّر الحقلان له بلا شرط، كما `quotedPrice`/`bonusQuantity`.
- **سطر البونص يرث نفس القيمتين** — المصيدة نفسها التي ظهرت في تحويل الكمية:
  يُبنى سطراً منفصلاً بكلفة صفر فلا يمرّ بأي مسار يخصّ السطر المدفوع.
- **الكتابة استبدال كامل لا دمج**، فالواجهة ترسل القيمتين مع كل سطر في كل إرسال
  ولو لم تُمسّا؛ الإرسال عند التعديل فقط كان سيمحو تصريحاً سابقاً بصمت.

**قيد معروف:** اختيار تاريخ **اليوم** يُرفَض (`>` لا `>=`، والتاريخ النصّي يُفسَّر
منتصف ليل UTC). مقصود لتاريخ انتهاء، لكن المستخدم سيصطدم به أول مرة يجرّب «اليوم».

**تحذير نشر:** مسار التسعير صار يكتب في عمودين غير موجودين في الإنتاج بعد —
`20260919000000_order_item_batch_expiry` **لم تُطبَّق**. النشر قبل الهجرة يُفشل كل
تسعير بـP2022. الترتيب إلزامي: الهجرة ثم النشر.

655 اختباراً تمرّ (من 641)، `tsc` نظيف.

## المرحلة: مرجع الشحنة المُولَّد + إعادة تصميم صفوف التسعير

**قرار صاحب النظام:** «رقم الدفعة» لم يعد يكتبه المذخر — يُصدره النظام.
عُرضت عليه بدائل ثلاثة (مرجع مُولَّد + حقل يدوي لرقم المصنّع / توليد كامل يحلّ
محلّ رقم المصنّع / اقتراح قابل للتعديل) مع بيان ما يُفقَد في كل خيار، فاختار
**التوليد الكامل** وهو عالمٌ أن الاستدعاء والإرجاع لوجبة بعينها يفقدان مرجعهما.

**ما بُني ضمن ذلك القرار للحفاظ على أكبر قيمة ممكنة:**
`buildShipmentRef` في `app/lib/shipment-ref.ts` — نقية ومُختبَرة (11 اختباراً):
`WO-51045DA4-L1` مشتقّ من الجزء المميِّز من رقم الطلب + ترتيب السطر.
- **حتمي بالكامل** — بلا `Date.now()`/`Math.random()`/`randomUUID()`. السبب غير
  تجميلي: PATCH .../quote استبدال كامل وقد يُعاد التسعير مراراً، فأي عشوائية
  تولّد رقماً جديداً كل مرة فتتفرّق `PurchaseItem` المنسوخة عند الاعتماد عن
  `WarehouseOrderItem` المُستبدَلة — نفس خلل التاريخ المُختلَق لكن في الرقم.
- **الترتيب من فرز ثابت لمعرّفات الأسطر** لا من موضعها في المصفوفة، فإعادة
  التسعير لا تُعيد ترقيم الأسطر.
- **يُولَّد خادمياً حصراً**؛ المتصفح لا يرسل الحقل إطلاقاً (تُحقِّق: لا وجود لـ
  `input.batchNumber` في المسار). العرض في النافذة يستدعي نفس الدالة للمعاينة.
- **تاريخ الانتهاء لم يُولَّد ولم يُفترَض** — توليده يكتب معلومة كاذبة في الحقل
  الذي يحكم الصرف وتنبيه الانتهاء، وهو ما أُزيل من شاشة الاستلام أول اليوم.
- **أسبقية FEFO محفوظة:** المذخر المتتبِّع يبقى رقمه الحقيقي من `WarehouseBatch`
  متقدّماً على المُولَّد عند الاستلام؛ التوليد يملأ فراغ غير المتتبِّع فقط.
- وسم الصف في شاشة الاستلام يُركَّب من شرطَي الرقم والتاريخ منفصلين (OR)، فلا
  يُنسَب حقلٌ إلى مصدر لم يأتِ منه حين يفرغ أحدهما.

**التصميم:** كل سطر ثلاث طبقات — ترويسة (الاسم/الباركود/المطلوب)، ثم القرار
وحده، ثم شبكة مصطفّة بعناوين فوق الحقول، والملاحظة بعرض كامل. الشبكة
`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (المراجعة أمسكت أن النسخة الأولى
عمودان حتى في أضيق شاشة). و`dir="ltr"` على حقل التاريخ وحده لا على الشبكة:
منتقي المتصفح يعرض mm/dd/yyyy دوماً ولا يُعاد ترتيبه من CSS، لذلك يذكر العنوان
الترتيب صراحة بدل ترك المستخدم يخمّنه.

**توحيد الارتفاع:** كانت الحقول بلا ارتفاع صريح (`py-1.5` فقط)، فارتفاع كلٍّ
ناتجٌ عن محتواه — و`input[type=date]` يحمل منتقياً أصلياً بارتفاع جوهري لا
تتحكّم به الحشوة، فيخرج أطول بصورة تختلف بين المحرّكات. العلاج `h-9` صريح على
الحقول الستة + `flex h-9 items-center` على مربع المرجع (span للقراءة فقط).

**أثر يجب تذكّره:** `isLineIncomplete` في شاشة الاستلام كان يمنع التأكيد ما دام
رقم الدفعة فارغاً — وكان فارغاً غالباً فيُجبِر الصيدلاني على النظر إلى العلبة.
الآن الرقم يُملأ آلياً دائماً، فلم يبق حارساً إلا تاريخ الانتهاء الآتي من تصريح
المذخر. مسؤولية صحة التاريخ انتقلت عملياً من الصيدلاني إلى المذخر. عُرض على
صاحب النظام إضافة خانة تأكيد صريحة لكل سطر جاءت قيمه من تصريح لا من شحنة فعلية
— لم يُبَتّ فيها بعد.

666 اختباراً تمرّ (من 655)، `tsc` نظيف.

## إصلاح: حلّ التعبئة بالباركود لا بـ drugId (حجب اعتماد كاذب)

**البلاغ:** اعتماد عرض يفشل بـ`PACK_UNITS_UNRESOLVED` رغم أن الصيدلاني أكّد عدد
الأشرطة للصنفين.

**السبب المُثبَت من الإنتاج:** `GlobalDrug` مُنطَّق بثنائية
`(organizationId, warehouseId)`، فللباركود الواحد عدة صفوف. في طلب «الشمس»:

```
6291001000027  6c02193e…  org=الشمس  upp=1     مؤكَّد    ← الصيدلاني أكّد هنا
               455de2ca…  org=null   upp=null  غير مؤكَّد ← الطلب يشير إلى هذا
2931523        9723d0f7…  org=الشمس  upp=1     مؤكَّد
               ce4451fe…  org=null   upp=null  غير مؤكَّد ← الطلب يشير إلى هذا
```

صفحة التأكيد تعمل على أدوية مخزون الصيدلية (صفوفها الخاصة)، بينما مسار الإرسال
للمذخر يحلّ الباركود إلى الصفّ المشترك كي يراه المذخر. فالحارس يقرأ `drugId`
المكتوب على الطلب → الصفّ العالمي → `null` → يمنع. **13 باركوداً في الإنتاج لها
صفوف مكرّرة**، فالخلل متكرر لا فردي.

**لماذا القراءة نفسها خاطئة:** مبدأ صاحب النظام المعلَن سابقاً —
«`unitsPerPack` خاصية بالباركود لا بالمورّد». التعبئة خاصية العلبة الفيزيائية،
لا خاصية أيّ صفّ يصادف أن يشير إليها. فقراءتها عبر `drugId` واحد مخالفة للمبدأ.

**الإصلاح (جهة القراءة وحدها — بلا هجرة ولا دمج صفوف):**
`app/lib/resolve-pack-units.ts` نقية ومُختبَرة (10 اختبارات):
- القيم الصالحة فقط (عدد صحيح موجب).
- المؤكَّدة أولاً: قيمة واحدة → `RESOLVED confirmed:true`؛ أكثر → `CONFLICT`.
- لا مؤكَّد؟ يُرجَع لغير المؤكَّدة بنفس المنطق (إبقاءً لسلوك اليوم).
- **المؤكَّد يغلب غير المؤكَّد دوماً ولو اختلفا** — تأكيد إنسان يغلب رقماً
  مستنتَجاً، وليس تعارضاً. أما مؤكَّدان مختلفان فيحسمهما إنسان لا خوارزمية.

**الحارس النطاقي إلزامي ولا يُوسَّع:**
`organizationId ∈ {null, org الطلب}` و`warehouseId ∈ {null, مذخر الطلب}`.
بدونه تقود تعبئةٌ خاصة بصيدلية أخرى حسابَ الكلفة في فاتورة هذه — أي تحويل إصلاح
إلى تسريب بين المستأجرين. والباركود يُطبَّع بـtrim كما في
`supplier-price-history.ts` تحسّباً لصفّ يختلف بفراغ فقط.

**التمييز في الرسائل:** تعارض داخلي بين صفوفنا يُمنَع قبل `toStripPrice` برسالة
تسمّي القيمتين؛ والمجهول الحقيقي يسقط إلى رسائل `toStripPrice` القائمة كما هي.

**التحقق على بيانات الإنتاج فعلياً** (لا بالاختبارات وحدها): تشغيل نفس الاستعلام
والقواعد على الطلب المحجوب أعطى `RESOLVED 1 (مؤكَّد)` للصنفين. والأثر المالي:
التعبئة 1، فالكمية ×1 والسعر ÷1، والإجمالي 5×450 + 6×300 = **4,050** مطابقاً
لـ`totalAmount` المعروض — الثابت الحسابي قائم.

**لم يُغيَّر `supplier-prices`:** تُتُبِّعت السلسلة كاملة (drug-search →
NeedListClient → pack-units POST → supplier-prices) وكلها مفاتيحها نفس `drugId`
الآتي من مخزون الصيدلية، فالتأكيد والقراءة يقعان على الصفّ ذاته. بقي أثر تجميلي
فقط: لو ملكت صيدلية مخزوناً على الصفّين معاً، فأيّهما ينجو في نتيجة البحث
اعتباطي — لا تمرّ منه كلفة فاتورة.

**متبقٍّ للمنبع:** مسار الإرسال ينشئ صفّاً عالمياً للباركود إن لم يجده بينما
المخزون على صفّ خاص، فالعدد يستمر في النمو. يُعالَج لاحقاً كعمل منفصل
(وسكربت `merge-duplicate-global-drugs.mjs` ما زال غير مُشغَّل).

676 اختباراً تمرّ (من 666)، `tsc` نظيف، و`warehouse-money-path.test.ts` يمرّ بلا مساس.
