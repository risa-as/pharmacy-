# دليل أوامر التشغيل والبناء (Faramace)

هذا الدليل يلخص جميع الأوامر التي تحتاجها لتشغيل أو بناء أجزاء نظام Faramace الثلاثة: (تطبيق سطح المكتب، تطبيق الهاتف، وموقع الويب).

> [!IMPORTANT]  
> النظام مبني باستخدام **pnpm** بدلاً من npm لإدارة الحزم عبر بيئة الـ Monorepo. تأكد من استخدام `pnpm` دائماً لتفادي مشاكل توافقية الحزم.

---

## 1. تطبيق الويب والواجهة الخلفية (Web & API)

هذا الجزء (الموجود في `apps/web`) يحتوي على الموقع الإلكتروني (Dashboard) والـ API الذي تتصل به باقي التطبيقات (الهاتف وسطح المكتب).

### التشغيل في بيئة التطوير (Development)

```bash
pnpm --filter web dev
```

- **الشرح:** يشغّل خادم التطوير لـ Next.js. يمكنك الوصول للموقع عبر `http://localhost:3000`. يقوم بتحديث التغييرات في الكود فوراً (Hot Reload).

### البناء لبيئة الإنتاج (Production)

```bash
pnpm --filter web build
```

- **الشرح:** يجمع الكود ويجهزه للرفع على السيرفر (مثل Vercel أو الخادم الخاص بك). يُنشئ نسخة سريعة ومُحسّنة.

### تشغيل النسخة المبنية (بعد البناء)

```bash
pnpm --filter web start
```

- **الشرح:** يشغل النسخة الجاهزة للإنتاج التي تم بناؤها في الخطوة السابقة.

---

## 2. تطبيق الهاتف (Mobile App)

هذا الجزء (الموجود في `apps/mobile`) مبني باستخدام React Native و Expo.

### التشغيل في بيئة التطوير (Development)

```bash
pnpm --filter faramace-mobile start
```

- **الشرح:** يفتح لوحة تحكم Expo في المتصفح، ويظهر لك رمز QR. يمكنك مسحه بتطبيق Expo Go لتجربة التطبيق على هاتفك الفعلي أثناء التطوير.
  > [!NOTE]
  > اسم حزمة الموبايل في `package.json` هو **`faramace-mobile`** (وليس `mobile`)، لذا يجب استخدام هذا الاسم بالضبط مع `--filter`. (بينما الويب اسمه `web` وسطح المكتب اسمه `desktop`.)

### بناء نسخة APK قابلة للتثبيت (مُستضافة على EAS)

```bash
# يمكنك الانتقال إلى مجلد الموبايل لتنفيذ أوامر EAS
cd apps/mobile
npx eas-cli build --profile preview --platform android
```

- **الشرح:** يرسل الكود إلى خوادم Expo (EAS) لبناء ملف `.apk`. هذه العملية تأخذ (10-20 دقيقة) وبعدها يعطيك رابطاً لتحميل التطبيق وتثبيته.

### إرسال تحديث هوائي (OTA Update)

```bash
cd apps/mobile
npx eas-cli update --channel production --message "رسالة التحديث"
```

- **الشرح:** يرسل التحديثات الجديدة إلى التطبيقات المُثبتة مسبقاً (عبر EAS Build) دون الحاجة لإعادة بناء وتثبيت ملف APK جديد.

### 🔄 التبديل بين حسابات EAS (عند نفاد الحصة المجانية)

ارتباط التطبيق بحساب EAS محدّد بقيمة **`projectId`** في `apps/mobile/app.json`:

```json
"extra": { "eas": { "projectId": "<معرّف المشروع>" } }
```

الحساب الذي تبني به = الحساب المُسجَّل دخوله (`eas login`) **ويجب** أن يملك صلاحية على ذلك `projectId`. إن لم يطابقا يظهر الخطأ: `Entity not authorized` / `You don't have the required permissions`.

**معرّفات المشاريع (احتفظ بها):**

| الحساب                 | projectId                              |
| ---------------------- | -------------------------------------- |
| الحساب الأول (القديم)  | `89cd48b4-b1ea-49b7-8811-d1ceda7edac7` |
| الحساب الثاني (الحالي) | `808c2b2a-ec42-46c1-8a3a-0e2054b7ea40` |

**خطوات التبديل لأي حساب** (شيئان معاً — الحساب + المعرّف):

1. سجّل دخول الحساب المطلوب:
   ```powershell
   npx eas-cli login
   ```
2. ضع `projectId` المقابل لذلك الحساب في `app.json` (من الجدول أعلاه).

3. ابنِ كالمعتاد:
   ```powershell
   npx eas-cli build --profile preview --platform android
   ```
   > لا تحتاج `eas init` عند العودة لمشروع موجود — فقط استعِد معرّفه وسجّل دخول حسابه. (`eas init` يُستخدم مرّة واحدة لإنشاء مشروع جديد تحت حساب لا يملك معرّفاً بعد.)

> [!WARNING]
> **مفتاح التوقيع (Keystore) مختلف لكل مشروع/حساب.** نسخة مبنية بحساب لا يمكن تثبيتها فوق نسخة مبنية بحساب آخر على نفس الجهاز (تعارض توقيع → «لم يتم تثبيت التطبيق»). عند التبديل، **احذف التطبيق القديم من الجهاز ثم ثبّت الجديد**. وللنشر على Google Play يجب الالتزام بنفس المفتاح دائماً — لا تبدّل الحساب بعد النشر على المتجر دون نقل الـ keystore.

---

## 3. تطبيق سطح المكتب (Desktop App)

هذا الجزء (الموجود في `apps/desktop`) مبني باستخدام Electron ليعمل كنظام Point of Sale (POS) اوفلاين/اونلاين، ويدعم **التحديث التلقائي** عبر GitHub Releases.

### التشغيل في بيئة التطوير (Development)

```bash
pnpm --filter desktop dev
```

- **الشرح:** يشغل تطبيق سطح المكتب محلياً. يفتح نافذة التطبيق ويتيح لك رؤية التعديلات البرمجية فوراً.
  > [!NOTE]
  > في وضع التطوير يكون **التحديث التلقائي معطّلاً عمداً** (يعمل فقط في النسخة المثبّتة). فلا تتوقع ظهور شريط التحديث أثناء `dev`.

### البناء المحلي (إنشاء ملف EXE بدون نشر)

```bash
pnpm --filter desktop build
```

- **الشرح:** ينفّذ السلسلة كاملة: تنظيف مجلد الإخراج ← توليد Prisma Client ← فحص TypeScript ← بناء Vite ← تغليف عبر electron-builder. يُنتج المثبّت والنسخة المحمولة **بدون رفعها** لأي مكان. مناسب للاختبار المحلي.
- **مكان الملف الناتج:** خارج المستودع في مجلد مجاور:
  `D:\Programming\FaramaceBuilds\desktop\`
  وتجد فيه:
  - `Faramace POS Setup x.x.x.exe` ← **المثبّت** (هو الذي يدعم التحديث التلقائي).
  - `Faramace POS x.x.x.exe` ← نسخة محمولة (Portable) **لا تدعم** التحديث التلقائي.

> [!NOTE]
> لا يوجد أمر `make`؛ كل التغليف يتم داخل سكربت `build` نفسه عبر electron-builder. ومجلد الإخراج خارج المستودع عمداً (يتجنّب قفل ملفات البناء من VS Code).

---

### 🚀 نشر تحديث جديد للصيدليات (Auto-Update عبر GitHub)

عند إصدار نسخة جديدة، تتحدّث كل الصيدليات تلقائياً: يُنزَّل التحديث بالخلفية، ويظهر شريط أخضر بزر **"إعادة التشغيل الآن"** (وإن لم يُعد المستخدم التشغيل، يُثبَّت تلقائياً عند الإغلاق التالي).

#### تجهيز لمرة واحدة: توكن GitHub

يحتاج النشر إلى توكن GitHub في ملف `apps/desktop/.env` (المحمي من git):

```
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
```

- أنشئ التوكن من: GitHub ← Settings ← Developer settings ← Personal access tokens (classic) ← صلاحية `public_repo` فقط.
- **لا تلصق التوكن في أي رسالة أو ملف يُرفع لـ git.** سكربت `release` يحمّل `.env` تلقائياً قبل electron-builder.

#### خطوات إصدار نسخة (في كل مرة)

**1. ارفع رقم الإصدار** في `apps/desktop/package.json` (يجب أن يرتفع دائماً):

```jsonc
"version": "1.0.2"  // ← مثلاً 1.0.2 ثم 1.0.3 ...
```

**2. ابنِ وارفع** المثبّت + `latest.yml` كمسودّة إصدار على GitHub:

```bash
pnpm --filter desktop release
```

- **الشرح:** نفس بناء الإنتاج، لكنه يرفع الملفات تلقائياً إلى GitHub Releases. علامة النجاح ظهور أسطر `publishing` و`uploading`، ووصولها 100%.

**3. انشر المسودّة:** افتح صفحة Releases في المستودع ← الإصدار الجديد (مثل `v1.0.2`) يكون **Draft** ← اضغط **Edit** ← **Publish release**.

> [!IMPORTANT]
> هذه الخطوة إلزامية: المُحدِّث **يتجاهل المسودّات تماماً**. لن تصل التحديثات للصيدليات حتى تنشر الإصدار فعلياً.

**4. (أول مرة فقط):** النسخ المثبّتة قبل دعم التحديث التلقائي لا تُحدّث نفسها — ثبّت `Faramace POS Setup x.x.x.exe` يدوياً على كل صيدلية **مرة واحدة**. بعدها تتحدّث كل الإصدارات تلقائياً.

#### شروط عمل التحديث (للتذكير عند أي خلل)

- رقم الإصدار الجديد **أعلى** من المثبّت.
- الإصدار **منشور** على GitHub (ليس Draft) ويحتوي ملف **`latest.yml`**.
- النسخة المُختبَرة هي **المثبّتة** (Setup) وليست `dev` ولا النسخة المحمولة.
- المستودع **عام (Public)** — كي يُنزّل العملاء بلا توكن.

> [!NOTE]
> بدون شهادة توقيع كود (Code Signing) سيظهر تحذير Windows SmartScreen عند **التثبيت اليدوي الأول** فقط؛ التحديث التلقائي بعدها يعمل بصمت. الشهادة تحسين اختياري لاحق.

---

### 🔏 توقيع الكود (Code Signing) — إزالة تحذير SmartScreen

التوقيع اختياري لكنه يزيل تحذير «Windows protected your PC» عند التثبيت، ويزيد ثقة الصيدليات.

**ما تحتاجه:** شهادة توقيع كود من جهة معتمدة (مثل Sectigo أو DigiCert). الأنسب للتوقيع الآلي هي شهادة **OV (Organization Validation)** بصيغة ملف `.pfx`. (شهادات **EV** تعطي سمعة فورية لدى SmartScreen لكنها أغلى وتتطلب توكن أجهزة (USB)، مما يعقّد التوقيع الآلي.)

**كيف تُفعّلها:** أضف سطرين إلى `apps/desktop/.env` (المحمي من git):

```
CSC_LINK=C:\\path\\to\\certificate.pfx
CSC_KEY_PASSWORD=كلمة_سر_الشهادة
```

- لا حاجة لأي تعديل في الكود — سكربت `release` يحمّل `.env` تلقائياً، وelectron-builder يكتشف هذين المتغيّرين ويوقّع المثبّت **تلقائياً** أثناء البناء.
- ثم شغّل كالمعتاد: `pnpm --filter desktop release`.

**التحقق من نجاح التوقيع:** انقر يميناً على ملف `Faramace POS Setup x.x.x.exe` ← **Properties** ← تبويب **Digital Signatures** — يجب أن تظهر شهادتك.

> [!NOTE]
> شهادات OV تبني سمعتها لدى SmartScreen تدريجياً مع تراكم التنزيلات؛ لذا قد يستمر التحذير لفترة قصيرة في البداية حتى لو كان التوقيع صحيحاً. التحديث التلقائي يستفيد من التوقيع أيضاً (يتحقق electron-updater من سلامة المثبّت).

---

## 4. أوامر عامة ومفيدة للـ Database (Prisma)

يحتوي النظام على **مخطّطين منفصلين**: مخطط الويب السحابي في `apps/web/prisma/schema.prisma` (PostgreSQL)، ومخطط سطح المكتب المحلي في `apps/desktop/prisma/schema.prisma` (SQLite). لذا **نفّذ أوامر Prisma من داخل مجلد التطبيق المقصود** كي تستهدف القاعدة الصحيحة.

### تحديث الـ Prisma Client (بعد أي تعديل في schema.prisma)

```bash
# لقاعدة الويب
pnpm --filter web exec prisma generate
# لقاعدة سطح المكتب
pnpm --filter desktop exec prisma generate
```

- **الشرح:** يولد الواجهات (Types) اللازمة للتعامل مع قاعدة البيانات في الكود.

### رفع التعديلات على قاعدة البيانات الفعلية (Migration)

```bash
# من داخل apps/web (يستهدف PostgreSQL السحابية عبر DATABASE_URL)
pnpm exec prisma db push
```

- **الشرح:** يطبق التغييرات الموجودة في `schema.prisma` مباشرة على قاعدة البيانات الفعلية.
  > [!IMPORTANT]
  > عند تعديل مخطط الويب: شغّل `prisma db push` على قاعدة الإنتاج **قبل أو مع** نشر كود الويب — وإلا قد ينكسر النظام إذا قرأ الكود عموداً لم يُضَف بعد. وتأكّد أن `DATABASE_URL` يشير لقاعدة الإنتاج الصحيحة.
  > سطح المكتب لا يحتاج هذا الأمر: ترحيلات SQLite المحلية تُطبَّق تلقائياً عند إقلاع التطبيق.

---

## 5. مفاهيم متقدمة: لماذا نستخدم `--filter`؟

تطبيقك ليس تطبيقاً منفرداً، بل هو مجموعة من التطبيقات (الويب، الهاتف، سطح المكتب) تعيش جميعها داخل مجلد واحد رئيسي. هذه المعمارية البرمجية تُسمى **Monorepo**.

بفضل أداة `pnpm` يمكننا إدارة كل هذه المشاريع معاً لتتشارك الموارد (مثل قاعدة البيانات) دون تكرار الأكواد.

### وظيفة `--filter`

عندما تكون في المجلد الرئيسي للمشروع وتكتب أمراً مثل `pnpm run dev`، لن يعرف pnpm أي التطبيقات الثلاثة تريد تشغيلها.

- **الحل:** نستخدم `--filter` للقول: _"من بين كل المشاريع، قم بتصفية مشروع كذا، ونفّذ الأمر داخله فقط"_.
- **الفائدة:** لا تحتاج لكتابة `cd apps/mobile` للدخول إلى المجلد ثم تشغيل الأمر هناك. يمكنك تنفيذ كل أوامرك من مكان واحد (المجلد الرئيسي).

### أوامر وأعلام مشابهة في الـ Monorepo:

1. **البناء والموازاة (`-r`):**
   - `pnpm -r run build` (اختصار لـ recursive أي متكرر/شامل). هذا الأمر يدخل إلى جميع التطبيقات والمكاتب في النظام ويبنيها جميعاً بضغطة واحدة.
2. **أمر `exec`:**
   - مثل `pnpm exec prisma db push`. كلمة `exec` (Execute) تعني تنفيذ أداة أو سكريبت تم تثبيته مسبقاً في مجلد `node_modules` دون الحاجة لتثبيت الأداة على جهازك بشكل كامل.
3. **التثبيت العام (`install`):**
   - عند تحميل المشروع أول مرة، تكتب `pnpm install` في المجلد الرئيسي فقط. `pnpm` ذكي بما يكفي لقراءة متطلبات جميع التطبيقات (الهاتف، الويب، الخ) وتثبيت وربط كل المكاتب المطلوبة معاً بكفاءة عالية وتوفير لمساحة التخزين.
