diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..7735b0df1a019c0094ed01dafb16facde4ac9f36 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -64,10 +64,16 @@ const ICON_TYPE_SIZE = {
     ic10: 1024,
 };
 function readImageHeader(input, imageOffset) {
+    if (imageOffset < SIZE_HEADER || input.length - imageOffset < SIZE_HEADER)
+        throw new TypeError('Invalid ICNS entry header');
     const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
+    const length = (0, utils_1.readUInt32BE)(input, imageLengthOffset);
+    const fileLength = (0, utils_1.readUInt32BE)(input, FILE_LENGTH_OFFSET);
+    if (length < SIZE_HEADER || length > fileLength - imageOffset)
+        throw new TypeError('Invalid ICNS entry size');
     return [
         (0, utils_1.toUTF8String)(input, imageOffset, imageLengthOffset),
-        (0, utils_1.readUInt32BE)(input, imageLengthOffset),
+        length,
     ];
 }
 function getImageSize(type) {
@@ -78,7 +84,11 @@ exports.ICNS = {
     validate: (input) => (0, utils_1.toUTF8String)(input, 0, 4) === 'icns',
     calculate(input) {
         const inputLength = input.length;
+        if (inputLength < SIZE_HEADER * 2)
+            throw new TypeError('Invalid ICNS file header');
         const fileLength = (0, utils_1.readUInt32BE)(input, FILE_LENGTH_OFFSET);
+        if (fileLength < SIZE_HEADER * 2)
+            throw new TypeError('Invalid ICNS file length');
         let imageOffset = SIZE_HEADER;
         let imageHeader = readImageHeader(input, imageOffset);
         let imageSize = getImageSize(imageHeader[0]);
diff --git a/dist/types/jxl.js b/dist/types/jxl.js
index f557b0aea7e679d2cb84d9915e957dd2254eba97..4b699fd88977074bab6ccb364e600957323774ae 100644
--- a/dist/types/jxl.js
+++ b/dist/types/jxl.js
@@ -23,6 +23,8 @@ function extractPartialStreams(input) {
         const jxlpBox = (0, utils_1.findBox)(input, 'jxlp', offset);
         if (!jxlpBox)
             break;
+        if (jxlpBox.size < 12)
+            throw new TypeError('Invalid JXL partial codestream box');
         partialStreams.push(input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size));
         offset = jxlpBox.offset + jxlpBox.size;
     }
diff --git a/dist/types/utils.js b/dist/types/utils.js
index 5224bbafe87551ac415cb3de234820ccc0ff6e2c..57c246e0bac4bec643b595483d61a44bd7609040 100644
--- a/dist/types/utils.js
+++ b/dist/types/utils.js
@@ -49,9 +49,13 @@ function readUInt(input, bits, offset, isBigEndian) {
 }
 exports.readUInt = readUInt;
 function readBox(input, offset) {
-    if (input.length - offset < 4)
-        return;
+    if (!Number.isSafeInteger(offset) || offset < 0 || input.length - offset < 8)
+        throw new TypeError('Invalid image box header');
     const boxSize = (0, exports.readUInt32BE)(input, offset);
+    // This reader supports ordinary 32-bit boxes only. Reject zero/extended
+    // sizes before returning a matching box: callers also advance by size.
+    if (boxSize < 8)
+        throw new TypeError('Invalid image box size');
     if (input.length - offset < boxSize)
         return;
     return {
@@ -67,9 +71,7 @@ function findBox(input, boxName, offset) {
             break;
         if (box.name === boxName)
             return box;
-        // Fix the infinite loop by ensuring offset always increases
-        // If box.size is 0, advance by at least 8 bytes (the size of the box header)
-        offset += box.size > 0 ? box.size : 8;
+        offset += box.size;
     }
 }
 exports.findBox = findBox;
