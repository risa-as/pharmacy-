# مواصفات حمايات الإدخال السريع — صفحة المخزون

هذا الملف يلخّص الميزات المضافة إلى نماذج الإدخال السريع (قارئ الباركود) في صفحة المخزون، لتطبيقها على نظام آخر.

**السياق:** النظام يسجّل الأسعار والكميات **للشريط الواحد** (البيع بالشريط)، بينما الموظفون يفكرون بأسعار الباكيت. خطآن متكرران: (1) إدخال سعر الباكيت في حقل سعر الشريط، (2) كتابة سنة الصلاحية برقمين "27" بدلاً من "2027".

تُطبَّق الحمايات على **النماذج الثلاثة** التي تُفتح من شريط مسح الباركود:

1. إضافة دفعة لدواء موجود في المخزون
2. إضافة دواء موجود في النظام إلى مخزون الفرع
3. تسجيل دواء جديد + الدفعة الأولى

---

## الميزة 1: التكلفة تُدخل بسعر الباكيت وتُقسم تلقائياً

- حقلان: **سعر شراء الباكيت** + **عدد الأشرطة في الباكيت** (الافتراضي 1، الحد الأدنى 1، أعداد صحيحة فقط)
- تكلفة الشريط المحسوبة = سعر الباكيت ÷ عدد الأشرطة، تُعرض مباشرة أثناء الكتابة في صندوق ملخص:
  `سعر التكلفة للشريط: 1500 ÷ 6 = 250`
- **القيمة المحفوظة في قاعدة البيانات = تكلفة الشريط المحسوبة** (وليس سعر الباكيت)

## الميزة 2: سعر البيع يُدخل للشريط مباشرة

- حقل واحد: **سعر بيع الشريط (الجمهور)** — إدخال مباشر بدون قسمة (الصيدلاني يعرف سعر بيع الشريط)
- ملاحظة ثابتة تحت الحقل: «سعر الشريط الواحد — وليس الباكيت»
- ملاحظة أعلى قسم الأسعار: «التكلفة تُدخل بسعر الباكيت وتُقسم تلقائياً، أما سعر البيع فأدخله للشريط الواحد مباشرة»

## الميزة 3: فحوصات عند الحفظ (تأكيدات)

كلها تأكيدات ناعمة (يمكن المتابعة بعد التأكيد) **ما عدا** الكمية صفر فهي منع كامل:

| # | الشرط | النوع | العنوان |
|---|-------|-------|---------|
| 1 | الكمية ≤ 0 | **منع كامل** | «لا يمكن الحفظ: الكمية يجب أن تكون أكبر من صفر» |
| 2 | سعر الباكيت < 125 دينار | تحذير | «سعر الباكيت منخفض» |
| 3 | عدد الأشرطة > 20 **أو** (الكمية > 10 **و** عدد الأشرطة ≥ الكمية) | تحذير | «عدد الأشرطة يبدو غير صحيح» — الرسالة توضح أن الحقل يعني عدد الأشرطة داخل الباكيت الواحد وليس الإجمالي، وتعرض ناتج القسمة |
| 4 | سعر بيع الشريط ≤ تكلفة الشريط | **خطر (أحمر)** | «سعر البيع أقل من التكلفة» |
| 5 | سعر بيع الشريط > 2 × تكلفة الشريط | تحذير | «سعر البيع مرتفع جداً» — «تأكد أنك أدخلت سعر الشريط وليس سعر الباكيت» |
| 6 | (نموذج إضافة دفعة فقط — لا يحتوي حقل بيع) تكلفة الشريط ≥ سعر البيع الحالي المخزَّن | **خطر (أحمر)** | «التكلفة أعلى من سعر البيع» — «غالباً أُدخل سعر الباكيت دون تحديد عدد الأشرطة الصحيح». وإذا كان الفارق ≥ **500 دينار** يصير العنوان «التكلفة أعلى من سعر البيع بأكثر من 500 دينار» وتعرض الرسالة مقدار الفارق. الثابت `COST_OVER_PRICE_GAP = 500` |

- الفحص 5 هو صمام الأمان الأساسي لمشكلة الباكيت/الشريط: من يُدخل سعر بيع الباكيت في حقل الشريط سيتجاوز الضعف غالباً.
- الفحص 6 يظهر **نافذة واحدة فقط**؛ عتبة الـ500 دينار تغيّر العنوان/الرسالة إلى صيغة أشدّ ولا تضيف نافذة ثانية.
- تظهر أيضاً **تحذيرات فورية** (نصوص صغيرة ملونة) تحت الحقول أثناء الكتابة لنفس الشروط 2–6 مع عبارة «سيظهر تأكيد عند الحفظ».

## الميزة 4: حماية تاريخ الصلاحية (تصحيح السنة)

دالة `checkExpiry` تعمل على القيمة "YYYY-MM-DD" عند مغادرة الحقل (blur) **وعند الحفظ**:

- سنة من 1 إلى 99 ← تُضاف 2000 تلقائياً (27 ← 2027) مع ملاحظة خضراء: «✓ تم تصحيح السنة تلقائياً إلى 2027»
- سنة من 100 إلى 999 (رقم ناقص) ← تصحيح تقديري: 2000 + (السنة % 100)
- التاريخ (بعد التصحيح) **في الماضي** ← تحذير فوري أحمر تحت الحقل: «⚠ هذا التاريخ منتهي بالفعل — تأكد من السنة (2027 وليس 27)» + تأكيد خطر عند الحفظ: «تاريخ الانتهاء المدخل (…) منتهي بالفعل! تأكد من السنة — هل تقصد <السنة القادمة> بدلاً من <السنة المدخلة>؟»
- السنة > السنة الحالية + 15 ← تحذير: «بعيد جداً — تأكد من صحة السنة»
- **القيمة المصحَّحة هي التي تُحفظ** في قاعدة البيانات
- لحقول التاريخ النصية المقنَّعة (كما في الهاتف): تاريخ غير مكتمل/غير صالح ← تحذير: «التاريخ غير مكتمل أو غير صحيح — المطلوب: سنة-شهر-يوم (مثال: 2027-05-01)»

## الميزة 5: نافذة تأكيد مخصصة (بدل تأكيد المتصفح)

استبدال `window.confirm` بنافذة مصممة بنمط النظام:

- ألوان مسطّحة بدون تدرجات، زوايا صغيرة، RTL
- أيقونة تحذير: كهرماني للتحذير / أحمر للخطر
- عنوان + رسالة متعددة الأسطر
- زران: «إلغاء وتصحيح» (المركَّز افتراضياً — الخيار الآمن) و«متابعة على أي حال» (بلون التحذير/الخطر)
- Escape أو النقر على الخلفية = إلغاء
- تُعرض فوق نوافذ الإدخال (z-index أعلى) مع عزل النقرات كي لا يُغلق النموذج خلفها
- واجهة برمجية بوعد (Promise) قابلة للانتظار داخل معالج الحفظ:
  `if (!(await confirm({ title, message, variant }))) return;`

---

## ملاحظات تنفيذية مهمة

1. **ترتيب الفحوصات عند الحفظ:** كمية ← سعر باكيت منخفض ← عدد أشرطة ← فحوصات البيع/التكلفة ← تاريخ الصلاحية. أي إلغاء يوقف الحفظ.
2. **التاريخ المصحَّح يجب تمريره لدالة الحفظ مباشرة** وليس عبر تحديث الحالة (state) فقط — تحديث الحالة قد لا يصل قبل تنفيذ الحفظ (مشكلة قيمة قديمة/stale).
3. الفحص 6 يحتاج تمرير **سعر البيع الحالي** للدواء إلى نموذج إضافة الدفعة — من نتيجة فحص الباركود ومن صف جدول المخزون أيضاً عند فتح النموذج بزر «إضافة دفعة».
4. في تطبيق الهاتف استُخدمت سلسلة تنبيهات نظامية (Alert) متتابعة بدل النافذة المخصصة — أي «إلغاء» يوقف السلسلة.
5. كل الفحوصات من جهة الواجهة (client-side) — لا تغييرات على قاعدة البيانات أو الـ API.
