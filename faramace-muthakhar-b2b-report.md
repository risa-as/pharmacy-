# تقرير مفصل — ميزة «المذاخر» B2B في نظام Faramace
**ربط واجهة الصيدلاني بواجهة المذخر (المستودع) داخل النظام نفسه**

> تاريخ التقرير: 2026-09-02
> **مراجعة وتصحيح: 2026-09-02 (النسخة 2)** — بعد تدقيق كل ادعاءات التقرير مقابل الكود فعليًا. التصحيحات الجوهرية: (1) فرضية توحيد الكتالوج بالباركود كانت **خاطئة** بسبب `@@unique([barcode, organizationId])` — القسم 4.1 تغيّر؛ (2) جسر فواتير الشراء يحتاج ربط `Supplier ↔ Warehouse` ولم يكن مجانيًا؛ (3) طبقة الهوية هي المسار الحرج الحقيقي وليست بندًا من ستة؛ (4) الثغرة الأمنية أوسع مما وُصف (كتابة عابرة للمستأجرين لا قراءة فقط). التقدير الزمني عُدّل إلى 5–7 أسابيع.
> نطاق الفحص: `D:\Programming\Faramace` (Monorepo: apps/web + apps/mobile + apps/desktop + packages)
> الهدف: تقييم الوضع الحالي للميزة «المستودعات» المبنية جزئيًا، وتصميم كامل لميزة «المذاخر» بحيث يمتلك صاحب المذخر حسابًا بدور خاص، يرفع أدويته، ويستقبل طلبات الصيدليات مباشرة، ويؤكد التفاصيل (التوفر والأسعار) ثم يعيد إرسالها للصيدلاني الذي يتابع حالة طلباته في صفحة مخصصة.

---

## 1) الملخص التنفيذي

الميزة المطلوبة هي تحويل النظام من أداة إدارة داخلية للصيدلية إلى **شبكة توريد B2B**: كل مذخر (مستودع أدوية) يحصل على حساب بدور جديد `WAREHOUSE`، يدير كتالوج أدويته وأسعاره داخل النظام، وعندما ينشئ الصيدلاني طلب شراء ويختار المذخر يُرسل الطلب **فورًا** إلى حساب المذخر، الذي يراجع الأصناف (متوفر / غير متوفر / السعر تغير)، يكمل تفاصيل الفاتورة، ويعيد إرسالها للصيدلية للاعتماد، مع صفحة متابعة حالات لدى الصيدلي.

**الخلاصة الأساسية:**
- الميزة **مبنية جزئيًا فعلًا** باسم «المستودعات» (Warehouses): يوجد نموذج بيانات (`Warehouse`, `WarehouseOrder`, `WarehouseOrderItem`)، وواجهتا API، وصفحة إدارة بسيطة، وحالة طلبات أولية. لكنها **غير مكتملة ومعطّلة عن المستخدم** (مدخل القائمة الجانبية معطّل بكومنت صريح).
- الفجوة الجوهرية: **لا يوجد دور «مذخر» ولا حساب دخول للمستودع إطلاقًا** — المستودع حاليًا مجرد سجل بيانات يدخله أدمن الصيدلية. كما لا يوجد كتالوج أدوية للمذخر، ولا مسار تأكيد/تعديل الأسعار من طرف المذخر، ولا صفحة متابعة طلبات للصيدلي، ولا حتى endpoint لتحديث حالة الطلب.
- البنية العامة للنظام (تعدد المستأجرين، الصلاحيات، الإشعارات، فواتير الشراء والاستلام، بوابة الخطط SaaS) **صالحة للبناء فوقها**، لكن **ليست جاهزة كما وصفت النسخة الأولى**: حساب المذخر لا ينتمي إلى Organization ولا إلى Branch، وهذا يكسر افتراضًا مبثوثًا في طبقة المصادقة كلها (القسم 4.2)، كما أن كتالوج الأدوية غير موحّد فعليًا (القسم 4.1-أ).
- تقديريًا: MVP عملي كامل (ويب فقط) يحتاج **5–7 أسابيع** لفريق من مطوّرَين. الكتلة الأثقل ليست الميزات بل **طبقة الهوية**: النظام الحالي يفترض في كل مكان أن كل مستخدم ينتمي إلى Organization/Branch، وحساب المذخر يكسر هذا الافتراض في خمسة مواضع محددة (القسم 4.2).
- **قراران يجب حسمهما قبل كتابة أي migration** (وإلا يُعاد بناء المخطط في منتصف الطريق): كيف يُطابَق صنف الكتالوج بين المذخر والصيدلية (القسم 4.1-أ)، وكيف يُربط المذخر بـ `Supplier` المطلوب في فاتورة الشراء (القسم 4.1-ب).
- المخاطرة الرئيسية خارج التقنية: **مشكلة الإطلاق البارد** (بدون مذاخر مسجّلة لا قيمة للصيدليات، وبدون صيدليات لا قيمة للمذاخر) — الكود نفسه يحذّر من هذا في كومنتات القائمة الجانبية. هذه ليست حاشية: المحاولة السابقة ماتت عليها بالضبط.

---

## 2) فهم النظام الحالي (الأدلة من الكود)

### 2.1 البنية العامة
| العنصر | التفاصيل |
|---|---|
| Monorepo | pnpm + Turbo: `apps/web` (Next.js 14 App Router — لوحة التحكم + كل الـ API)، `apps/mobile` (Expo)، `apps/desktop` (Electron + SQLite محلي)، `packages/shared`, `packages/ui` |
| قاعدة البيانات | Prisma 5 — PostgreSQL للويب (سحابي) + SQLite لسطح المكتب |
| المصادقة | **نظامان متوازيان** يخدمهما `getTenantContext()` معًا: (1) **NextAuth (Auth.js) بمزوّد Credentials + bcrypt** عبر كوكي جلسة — هذا ما تستخدمه لوحة الويب (`apps/web/auth.ts`, `auth.config.ts`)؛ (2) **JWT موقّع بـ jose** عبر `/api/auth/login` بحمولة `userId, email, role, branchId, organizationId` — مسار Bearer للموبايل/سطح المكتب (`tenant-utils.ts:60-75`). **بوابة المذخر يجب أن تختار أحدهما صراحة**، وأي دعم موبايل لاحق يحتاج الاثنين |
| الأدوار الحالية | `enum Role { ADMIN, SUPER_ADMIN, PHARMACIST, CASHIER }` — **لا يوجد دور مذخر** |
| تعدد المستأجرين | `Organization → Branch → User`، وسياق المستأجر عبر `getTenantContext()` في كل الـ API |
| الدفع والخطط | خطط اشتراك + Feature Flags لكل خطة (`warehouseManagement` و`marketplace` من ضمن أعلام خطة ENTERPRISE) |
| الإشعارات | نموذج `Notification` (للمستخدم أو الفرع، مع `data: Json`) + `expoPushToken` على المستخدم = بنية جاهزة للإشعارات الداخلية والـ Push |
| الكتالوج | `GlobalDrug` (barcode, tradeName, scientificName) + أدوية خاصة بـ Organization عبر `organizationId String?`. ⚠️ **القيد الحاسم: `@@unique([barcode, organizationId])` (`schema.prisma:176`)** — أي أن الدواء الفيزيائي الواحد قد يوجد كـ **عدة صفوف**: صف عالمي (`organizationId: null`) + صف خاص لكل صيدلية أنشأت نسختها. الباركود **ليس** معرّفًا فريدًا عالميًا. هذا يبطل الفرضية الساذجة لكتالوج المذاخر — انظر 4.1-أ |

### 2.2 ما هو مبني فعلًا باسم «المستودعات» (الحالة الجزئية)

**نماذج البيانات** (`apps/web/prisma/schema.prisma`):
```prisma
model Warehouse {
  id            String   @id @default(uuid())
  name          String
  code          String?  @unique
  phone         String?
  address       String?
  city          String?
  contactPerson String?
  email         String?
  apiEndpoint   String?   // ← مؤشر على نية ربط أنظمة خارجية مستقبلًا
  apiKey        String?
  isActive      Boolean  @default(true)
  notes         String?
  orders        WarehouseOrder[]
}

model WarehouseOrder {
  id          String               @id @default(uuid())
  warehouseId String
  branchId    String
  orderNumber String?              @unique
  status      WarehouseOrderStatus @default(PENDING)
  totalAmount Float                @default(0)
  notes       String?
  expectedDate DateTime?
  // علاقات: warehouse + branch + items
}

model WarehouseOrderItem {
  id               String @id @default(uuid())
  warehouseOrderId String
  drugId           String      // مرتبط بـ GlobalDrug
  quantity         Int
  unitPrice        Float  @default(0)
  totalPrice       Float  @default(0)
}

enum WarehouseOrderStatus {
  PENDING  CONFIRMED  SHIPPED  DELIVERED  CANCELLED
}
```

**الـ API الموجود:**
- `GET/POST /api/warehouses` — عرض وإنشاء المستودعات (الإنشاء للأدمن فقط، ومحمي بعلم الخطة `warehouseManagement`).
- `GET/POST /api/warehouses/orders` — عرض الطلبات (فلترة warehouseId/branchId/status) وإنشاء طلب برقم تلقائي `WO-000001`.
- **ملاحظتان أمنيتان (الثانية أخطر):**
  1. **تسريب قراءة:** `GET /api/warehouses/orders` غير محمي بعلم الخطة وغير مقيّد بسياق المستأجر — شرط `where` يُبنى حصريًا من باراميترات الرابط (`orders/route.ts:18-20`)، فأي مستخدم مسجّل يقرأ طلبات أي مستودع/فرع في المنصة كلها.
  2. **كتابة عابرة للمستأجرين:** جدول `Warehouse` **لا يملك `organizationId` إطلاقًا** — إنه جدول عالمي مشترك. ومع ذلك `POST /api/warehouses` (`warehouses/route.ts:37-67`) يسمح لأي `ADMIN` في أي صيدلية على باقة ENTERPRISE بإدراج صفوف فيه، وحقل `code` فريد عالميًا (`@unique`) فيمكن لمستأجر أن يحجز أكواد مستأجر آخر.
  
  **الأثر على الخطة وليس على قائمة الإصلاحات فقط:** تسجيل المذاخر يجب أن يصبح وظيفة `SUPER_ADMIN` حصرًا، و**نموذج «إضافة مستودع» الحالي المتاح لأدمن الصيدلية يُحذف، لا يُطوَّر**.
- **لا يوجد** `[id]/route.ts` لأوامر المستودعات — أي **لا توجد طريقة لتحديث حالة الطلب** (CONFIRMED/SHIPPED…) من أي واجهة. المسار ميت بعد الإنشاء.

**الواجهة الموجودة:**
- صفحة `/dashboard/warehouses` (`page.tsx` + `WarehousesClient.tsx`): بطاقات المستودعات + نموذج إضافة مستودع فقط. **لا توجد واجهة لإنشاء أو متابعة الطلبات.**
- مدخل القائمة الجانبية **معطّل بكومنت صريح** في `apps/web/app/ui/dashboard/sidenav.tsx`:
  > `FUTURE: Iraqi Warehouses — requires Kimadia/national warehouse API integration. Zero value without supplier-side onboarding.`
  
  وهذا يؤكد أن فريق التطوير أدرك أن قيمة الميزة كلها مرهونة بوجود **حسابات حقيقية للموردين/المذاخر** — وهو بالضبط ما تطلبه الآن.

**بما أن التطبيقات الأخرى لا تعرف الميزة:** لا يوجد أي ذكر لـ `WarehouseOrder` في `apps/mobile` ولا `apps/desktop`.

### 2.3 منظومة «المورد» الحالية (المرتبطة مباشرة)
- `Supplier` نموذج بسيط داخل Organization (اسم/هاتف/رصيد) — يُستخدم في **فواتير الشراء اليدوية**: صفحة «إنشاء فاتورة شراء» `(sales-hub)/invoices/create` يختار فيها الصيدلي المورد من قائمة ويدخل الأصناف يدويًا.
- يعني: «المورد» اليوم مجرد **اسم في فاتورة**، لا كيان يتفاعل. ميزة المذاخر ستجعل المورد **طرفًا حيًا داخل النظام**.
- يوجد `/api/smart-order`: يقترح كميات إعادة الطلب من تحليل المخزون (أقل من الحد الأدنى) وسرعة البيع (30 يومًا) وزمن التوريد (14 يومًا + 7 أمان). **هذا المدخل الذكي يمكن ربطه مباشرة بطلب المذخر** — اقتراح جاهز يتحول لطلب بضغطة زر.
- يوجد أيضًا «سوق B2B» جزئي (`MarketplaceListing/MarketplaceOrder`): بيع فائض مخزون بين فروع الصيدليات نفسها (معلّق هو الآخر بسبب مشكلة الإطلاق البارد). مفاهيميًا مختلف عن المذاخر لكنه يشاركه نفس نمط الحل.

---

## 3) تحليل الفجوة — الموجود مقابل المطلوب

| # | المتطلب من طلبك | الحالة في الكود | الفجوة |
|---|---|---|---|
| 1 | حساب لصاحب المذخر بدور «مذخر» يدخل للنظام | لا يوجد دور ولا ربط `User ↔ Warehouse` | **إضافة دور `WAREHOUSE` + ربط المستخدم بالمستودع + مسار دخول وصلاحيات** |
| 2 | المذخر يرفع الأدوية الخاصة به (كتالوج + أسعار + توفر) | لا يوجد أي كتالوج للمستودع؛ فقط `apiEndpoint/apiKey` | **نموذج `WarehouseCatalogItem` + واجهة رفع (يدوي/Excel) + بحث للصيدلي** + ⚠️ **طبقة مطابقة بالباركود** لم تكن في التقدير الأصلي (4.1-أ) |
| 3 | الصيدلاني ينشئ طلبًا واختيار المذخر → يُرسل فورًا | يوجد POST لطلب مستودع لكن دون واجهة ولا ربط بشاشة إنشاء الفاتورة/الشراء | **تكامل نقطة البيع/المخزون مع إنشاء الطلب + إشعار للمذخر** — ⚠️ «لحظي» غير متاح: لا بنية realtime في الويب، البديل polling (4.6) |
| 4 | المذخر يدخل تفاصيل الفاتورة (توفر كل صنف، الأسعار نفسها أم تغيرت) ثم يرسل للعميل | الحالة تنتهي عند PENDING ولا يوجد endpoint تحديث إطلاقًا | **آلة حالات جديدة + مراجعة صنف-بصنف (متوفر/جزئي/نافد + سعر مقترح) + إعادة الإرسال للصيدلية** |
| 5 | صفحة مخصصة للصيدلاني لحالة الطلبات والتفاصيل | لا توجد صفحة طلبات للمستودعات في الواجهة | **صفحة «طلبات المذاخر» للصيدلية (Timeline + تفاصيل) + صفحة صندوق وارد للمذخر** |
| 6 | تسمية «المذاخر» بدل «المستودعات» | التسمية الحالية «المستودعات» | **إعادة تسمية في كل نصوص الواجهة (العربية)، والإبقاء على المعرّفات البرمجية Warehouse أو توحيدها على Distributor** |
| 7 | تحويل الطلب المعتمد إلى فاتورة شراء ومخزون | فواتير الشراء موجودة (`Purchase/PurchaseItem`) لكنها منفصلة تمامًا عن أوامر المستودعات | **جسر: اعتماد الطلب → إنشاء Purchase مسودة → استلام → إضافة Batch للمخزون (وفلترة 007-inventory-supplier)** — ⚠️ يتطلب أولًا `Supplier.warehouseId` ومورد مرآة لأن `Purchase.supplierId` إلزامي (4.1-ب) |

**خلاصة الفجوة:** تقريبًا 60% من بنية البيانات الأولية موجودة، و5% من الواجهة، و0% من تدفق الطرفين الحي (حساب المذخر + دورة التفاوض على الطلب). المبنى حاليًا هو «دليل عنواني للمستودعات»، والمطلوب هو «شبكة توريد تفاعلية».

**تصحيح النسخة 2:** نسبة الـ60% تصف **وجود الجداول** لا صلاحيتها للاستخدام. عمليًا يحتاج مخطط البيانات ثلاثة تغييرات لم ترد في النسخة الأولى — مفتاح كتالوج بالباركود، `Supplier.warehouseId`، و`User.warehouseId` مع سياق مصادقة مستقل — فالرقم الواقعي للجاهزية أقرب إلى **35–40%** على مستوى البيانات، و0% على مستوى الهوية.

---

## 4) التصميم المقترح للميزة

### 4.1 نموذج البيانات (تغييرات Prisma)

```prisma
// 1) دور جديد
enum Role {
  ADMIN
  SUPER_ADMIN
  PHARMACIST
  CASHIER
  WAREHOUSE        // جديد: صاحب المذخر / موظفه
}

enum WarehouseUserType { OWNER  STAFF }   // مستقبلًا: عدة مستخدمين لنفس المذخر

// 2) ربط المستخدم بالمذخر (branchId يبقى null لحسابات المذاخر)
model User {
  // ... الحقول الحالية
  warehouseId  String?
  warehouse    Warehouse? @relation(fields: [warehouseId], references: [id])
  warehouseUserType WarehouseUserType?
}

// 3) كتالوج المذخر — انظر التحذير 4.1-أ أدناه قبل تنفيذ هذا النموذج
model WarehouseCatalogItem {
  id          String   @id @default(uuid())
  warehouseId String
  drugId      String                    // ⚠️ يشير حصرًا لصف GlobalDrug العالمي (organizationId: null)
  barcode     String                    // ⚠️ مكرَّر عمدًا: هو مفتاح المطابقة عبر المستأجرين
  price       Float                     // سعر البيع للصيدلية
  isAvailable Boolean  @default(true)
  lastPriceUpdate DateTime @updatedAt
  warehouse   Warehouse @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  drug        GlobalDrug @relation(fields: [drugId], references: [id])
  @@unique([warehouseId, barcode])       // ⚠️ ليس [warehouseId, drugId] — السبب في 4.1-أ
  @@index([barcode])
  @@index([drugId])
}

// 3-ب) ربط المذخر بسجل المورد المحلي لكل صيدلية (لازم لجسر فواتير الشراء — انظر 4.1-ب)
model Supplier {
  // ... الحقول الحالية +
  warehouseId String?                    // إذا امتلأ: هذا المورد هو مرآة محلية لمذخر على المنصة
  warehouse   Warehouse? @relation(fields: [warehouseId], references: [id])
  @@unique([organizationId, warehouseId])  // مورد مرآة واحد لكل (صيدلية، مذخر)
}

// 4) تطوير حالة الطلب إلى دورة تفاوض كاملة
enum WarehouseOrderStatus {
  DRAFT         // سلة الصيدلي قبل الإرسال
  SENT          // أُرسل للمذخر
  UNDER_REVIEW  // المذخر يراجع/يعدّل
  QUOTED        // المذخر أكمل التفاصيل وأعاد الإرسال (عرض سعر)
  APPROVED      // الصيدلي اعتمد العرض
  REJECTED      // الصيدلي رفض العرض
  SHIPPED
  DELIVERED
  CANCELLED
}

enum WarehouseOrderItemStatus {
  REQUESTED     // طلب أولي من الصيدلي
  AVAILABLE     // متوفر بنفس السعر أو بسعر محدث
  PARTIAL       // متوفر بكمية أقل (quotedQuantity < quantity)
  OUT_OF_STOCK  // نافد
}

model WarehouseOrderItem {
  // ... الحقول الحالية +
  requestedPrice   Float?                     // ما ي توقعه الصيدلي (اختياري)
  quotedPrice      Float?                     // سعر المذخر النهائي
  quotedQuantity   Int?                       // الكمية المتاحة فعلًا
  status           WarehouseOrderItemStatus @default(REQUESTED)
  note             String?                    // "بدل آخر قادم بعد أسبوع" …
}

// 5) سجل أحداث الطلب (Timeline — أساس صفحة المتابعة)
model WarehouseOrderEvent {
  id        String   @id @default(uuid())
  orderId   String
  actorType String   // PHARMACY | WAREHOUSE | SYSTEM
  actorName String?
  type      String   // SENT, UNDER_REVIEW, QUOTED, APPROVED, SHIPPED ...
  payload   Json?    // ملخص التغييرات (مثلاً: الأصناف النافدة)
  createdAt DateTime @default(now())
  order     WarehouseOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)
  @@index([orderId, createdAt])
}
```

ملاحظات تصميمية:
- **حل التعارض التسمية/المفهوم:** `Supplier` الحالي (سجل داخلي) يبقى كما هو للفواتير اليدوية القديمة، ويُضاف نوع جديد من الموردين «مذخر على المنصة». في شاشة اختيار المورد تظهر قائمتان: مورّدون يدويون + مذاخر المنصة (بحسابات حقيقية). لاحقًا يمكن الترحيل/الدمج بقرار إداري.
- **لماذا `WarehouseCatalogItem` بدل تخزين السعر في الطلب فقط؟** لأن الصيدلي يجب أن يرى سعر المذخر وتوفره **قبل** إنشاء الطلب (بحث/إضافة سريعة)، والمذخر يدير أسعاره مرة واحدة بدل تعديلها في كل طلب.
- `WarehouseOrderEvent` هو ما يجعل صفحة «حالة الطلبات» عند الصيدلي حقيقية (من أرسل؟ متى؟ ماذا عدّل؟) ويغني عن تخمين المستخدم.
- ترقيم الطلبات `WO-{count+1}` الحالي فيه سباق محتمل (عدّاد عام) — الأفضل `@@default(cuid())` أو تسلسل per-warehouse.
- `WarehouseOrder.branchId` مفتاحه **الفرع لا المنظمة**. لذا صفحة متابعة الصيدلي يجب أن تستعلم بـ `tenantBranchWhere` وليس `tenantWhere`، وإلا رأى أدمن صيدلية متعددة الفروع نطاقًا خاطئًا.

#### ⚠️ 4.1-أ) مشكلة هوية الصنف — قرار حاسم قبل الـ migration

النسخة الأولى من هذا التقرير افترضت أن «`GlobalDrug` موحّد بالباركود فالكتالوج المشترك جاهز مفاهيميًا». **هذا غير صحيح.** القيد الفعلي في الكود:

```prisma
// apps/web/prisma/schema.prisma:172-176
organizationId String?
organization   Organization? @relation("OrganizationCustomDrugs", ...)
@@unique([barcode, organizationId])
```

المعنى: الدواء الفيزيائي الواحد يوجد كـ **عدة صفوف مختلفة الـ id**: صف عالمي (`organizationId: null`) + صف خاص لكل صيدلية أنشأت نسختها المحلية.

**الفشل الصامت الناتج:** المذخر يبني كتالوجه على الصف العالمي، والصيدلية تبحث انطلاقًا من صفها الخاص → `drugId` مختلف → **لا تطابق، بلا أي رسالة خطأ**: الصيدلي يرى «الصنف غير متوفر لدى هذا المذخر» بينما هو متوفر. والأسوأ: `WarehouseOrderItem.drugId` سيحمل معرّفًا لا يفهمه الطرف الآخر.

**القرار المطلوب (اختر أحدهما في المرحلة 0):**
| الخيار | الوصف | المقايضة |
|---|---|---|
| **أ-1 (موصى به)** | الكتالوج والطلبات تُطابَق على **`barcode`** لا `drugId`؛ `drugId` يبقى مرجعًا للصف العالمي فقط للعرض | يعمل عبر المستأجرين فورًا؛ يتطلب دالة `resolveToGlobalDrug(barcode)` تُستدعى عند الإضافة للسلة، وسياسة واضحة للأصناف بلا باركود |
| أ-2 | حصر كتالوج المذاخر بصفوف `organizationId: null` فقط، وإجبار الصيدلية على «ترقية» أدويتها الخاصة للصف العالمي قبل الطلب | أنظف بنيويًا لكنه يفرض عملية ترحيل بيانات على كل صيدلية قائمة — احتكاك تبنٍّ عالٍ |

في الحالتين: الأصناف بلا باركود (أو بباركود داخلي مخترع) لن تُطابَق. يجب تحديد سلوك واضح لها (استبعادها من الطلب مع رسالة صريحة).

⚠️ **تحذير مصاحب لخيار أ-1:** حقل `barcode` المكرَّر في `WarehouseCatalogItem` هو **نسخة من حقل قابل للتعديل** — لا يوجد اليوم ما يمنع أدمن من تعديل `GlobalDrug.barcode`، وعندها تصبح كل صفوف الكتالوج الحاملة للقيمة القديمة يتيمة **بصمت**، وهو بالضبط نمط الفشل الذي يحذّر منه هذا القسم. أحد حلّين إلزامي: (1) عدم تكرار الحقل والمطابقة عبر `drug.barcode` وقت الاستعلام (أبطأ قليلًا، آمن)، أو (2) اعتبار باركود الصف العالمي **غير قابل للتعديل بعد إدراجه في أي كتالوج مذخر** وفرض ذلك في مسار تعديل الأدوية.

📌 **ملاحظة للمنفّذ:** كتل Prisma أعلاه **مقتطفة** (`// ... الحقول الحالية +`) ولن تمر بـ `prisma validate` كما هي — تحتاج العلاقات العكسية على الطرف الآخر: `users User[]` و`suppliers Supplier[]` و`catalogItems WarehouseCatalogItem[]` على `Warehouse`، و`warehouseCatalogItems WarehouseCatalogItem[]` على `GlobalDrug`.

#### ⚠️ 4.1-ب) جسر فاتورة الشراء ليس مجانيًا

`Purchase.supplierId` حقل **إلزامي** (`schema.prisma:361`)، و`Supplier` مرتبط بالمنظمة (`schema.prisma:351`)، بينما `Warehouse` **جدول عالمي بلا `organizationId`**. أي أن خطوة «الطلب المعتمد ← فاتورة شراء مسودة باسم المذخر» لا يمكن تنفيذها كما هي.

**الحل المعتمد أعلاه:** حقل `Supplier.warehouseId` + **إنشاء تلقائي** لصف `Supplier` مرآة عند أول طلب معتمد بين صيدلية ومذخر (داخل نفس الـ transaction). يترتب على ذلك أن أرصدة/ديون المذخر تُدار عبر `SupplierPayment` القائم بلا بناء موازٍ — وهي فائدة مجانية. لكن يجب منع المستخدم من تعديل/حذف الموردين المرآة يدويًا.

### 4.2 الهوية والصلاحيات (Auth & Guards) — **المسار الحرج للمشروع**

> هذا ليس بندًا من ستة في المرحلة 1؛ هذا هو مصدر معظم مخاطر التقدير الزمني. النظام الحالي **يفترض في كل طبقة أن كل مستخدم ينتمي إلى Organization/Branch**، وحساب المذخر يكسر هذا الافتراض في خمسة مواضع محددة يجب تعديلها جميعًا قبل أن تعمل أي شاشة في البوابة:

| # | الموضع في الكود | السلوك اليوم | المطلوب |
|---|---|---|---|
| 1 | `tenant-utils.ts:97-99` | `getTenantContext()` يُرجع **403 «Branch not assigned»** لأي مستخدم غير أدمن بلا `branchId` | حساب المذخر (`branchId: null`) سيُرفض من **كل** مسار قائم. هذا سلوك مرغوب أمنيًا، لكنه يعني أن البوابة **لا يمكنها إعادة استخدام سطر واحد** من هذا الـ helper → تُكتب `getWarehouseContext()` مستقلة تمامًا |
| 2 | `auth.config.ts:13` | `authorized()` يحرس المسارات التي تبدأ بـ `/dashboard` **فقط** | مساحة `/warehouse/*` الجديدة ستكون **بلا أي حراسة** افتراضيًا. يجب إضافة فرع `isOnWarehouse` صريح + منع دور `WAREHOUSE` من `/dashboard/*` والعكس |
| 3 | `auth.config.ts:34` + `middleware.ts:23` | إعادة التوجيه بعد الدخول ومن الجذر `/` إلى `/dashboard` **بلا شرط** | توجيه حسب الدور في **الموضعين معًا** (نسيان أحدهما = المذخر يهبط على لوحة صيدلية فارغة) |
| 4 | `auth.config.ts:41-58` | callbacks الـ jwt/session لا تحمل `warehouseId` إطلاقًا | تمريره عبر السلسلة كاملة: `authorize()` ← `jwt` ← `session` ← كل مسار في البوابة |
| 5 | `tenant-utils.ts:60-75` | نفس الـ helper يخدم **نظامَي مصادقة**: كوكي NextAuth (ويب) و Bearer JWT بـ jose (موبايل) | بوابة المذخر تختار أحدهما صراحة في MVP (التوصية: NextAuth فقط)، وأي دعم موبايل لاحق للمذخر يفرض تنفيذ المسارين |

1. **دخول نفسه:** نفس صفحة `/login` ونفس آلية NextAuth — لكن بمجرد أن يكون `role=WAREHOUSE` يُحوّل المستخدم إلى مساحة المذخر `/warehouse/...` بدل لوحة الصيدلية (البندان 2 و3 في الجدول أعلاه).
2. **سياق جديد موازٍ لسياق المستأجر:** `getWarehouseContext()` تُرجع `warehouseId` من الـ session وتُستخدم في كل مسارات `/api/warehouse-portal/*` — هذا أهم قرار أمني: **عزل بيانات المذخر عن بيانات الصيدليات تمامًا** (يرى فقط طلباته وكتالوجه، ولا يرى قط مخزون الصيدلية ولا مبيعاتها ولا أسعار بيعها للجمهور).
3. **حراسة الصلاحيات:**
   - مسارات الصيدلية (إنشاء طلب، اعتماد عرض): `ADMIN/PHARMACIST` (وبصلاحية جزئية اختيارية `orders.warehouse` ضمن نظام `permissions` JSON الموجود أصلًا في `User`).
   - مسارات المذخر (كتالوج، مراجعة طلبات): `WAREHOUSE` فقط.
   - إصلاح الثغرتين الحاليتين قبل أي إطلاق: تقييد `GET /api/warehouses/orders` بالسياق + علم الخطة، و**نزع صلاحية إنشاء المستودعات من أدمن الصيدلية** (القسم 2.2).
4. **تسجيل المذاخر:** مرحلة أولى يدويًا — **`SUPER_ADMIN` حصرًا** ينشئ حساب المذخر من لوحة الإدارة (يضمن التحقق الحقيقي من المذخر، ويغلق ثغرة الكتابة العابرة للمستأجرين في آن واحد). يترتب على ذلك أن **صفحة `/dashboard/warehouses` بنموذج «إضافة مستودع» الحالي تُحذف من جانب الصيدلية** وتتحول إلى «دليل المذاخر» للقراءة فقط. مرحلة لاحقة: صفحة تسجيل عامة مع موافقة إدارية (KYC بسيط: اسم تجاري، رقم ترخيص، هوية).
5. **الخطط والتسعير:** إرسال الطلبات للمذاخر يمكن أن يكون علم خطة (`warehouseOrders`) أو مجانيًا للكل في البداية لدفع الشبكة؛ حساب المذخر نفسه يُقترح أن يكون **مجانيًا** في مرحلة الإطلاق (حل مشكلة cold-start المذكورة في كومنتات الكود).

### 4.3 رحلة الاستخدام الكاملة (Flows)

**أ) الصيدلي (الطلب):**
1. من صفحة الطلبات الجديدة أو من شاشة نقص المخزون/الطلب الذكي → «إرسال طلب لمذخر».
2. يختار المذخر (قائمة المذاخر النشطة + عرض حالة توفر وسعر كل صنف من `WarehouseCatalogItem` أثناء التجميع).
3. يضيف الأصناف والكميات (بحث بالاسم/الباركود من `GlobalDrug`) → إنشاء الطلب بحالة `SENT` → **إشعار لحساب المذخر** (صف في `Notification` يلتقطه polling البوابة خلال ≤60 ثانية + Push عبر expoPushToken + اختياريًا WhatsApp لاحقًا — لا توجد قناة لحظية حقيقية، انظر 4.6).
4. تظهر له حالة الطلب في «صفحة الطلبات» المخصصة: Timeline بالأحداث، وحين تعود الحالة `QUOTED` يرى تفاصيل العرض: كل صنف متوفر/جزئي/نافد + السعر النهائي + الإجمالي.
5. اعتماد (`APPROVED`) أو رفض (`REJECTED`) بملاحظة. عند الاعتماد تُنفَّذ **transaction واحدة**: (أ) البحث عن `Supplier` مرآة لهذا المذخر داخل منظمة الصيدلية، وإنشاؤه إن لم يوجد (`Supplier.warehouseId` — انظر 4.1-ب، فحقل `Purchase.supplierId` إلزامي ولا يقبل مذخرًا مباشرة)؛ (ب) إنشاء **Purchase مسودة** بنفس الأصناف والأسعار المعتمدة مربوطة بذلك المورد؛ (ج) تسجيل حدث في `WarehouseOrderEvent`. بعدها يكمل الصيدلي الاستلام العادي الحالي (`purchases/[id]/receive`) فتتضاف الدفعات للمخزون بنفس منطق `007-inventory-supplier` (المورد يُسجل على الـ Batch تلقائيًا). **فائدة جانبية:** ديون الصيدلية للمذخر تُدار تلقائيًا عبر `SupplierPayment` القائم بلا بناء موازٍ.

**ب) صاحب المذخر (التنفيذ):**
1. يدخل بحسابه → لوحة المذخر: بطاقات (طلبات جديدة بحاجة مراجعة، طلبات قيد التسليم، إجمالي مبيعات الشهر، أكثر الأصناف طلبًا).
2. «رفع الأدوية»: إدارة الكتالوج — إضافة صنف بالبحث من `GlobalDrug` (**الصفوف العالمية فقط**، ومطابقة الطلبات على `barcode` لا `drugId` — انظر 4.1-أ) + السعر + التوفر، أو **رفع ملف Excel** (نمط الرفع موجود أصلًا: مكتبة `xlsx` + صفحات استيراد أدوية أدمن — إعادة استخدام). يحتاج الاستيراد تقرير مطابقة صريحًا: كم صنفًا طوبق بالباركود، وكم صنفًا فشل ولماذا.
3. «الطلبات الواردة»: يفتح الطلب → لكل صنف يحدد: متوفر (نفس السعر) / تعديل السعر / جزئي بكمية / نافد (+ بديل اختياري) → «إرسال العرض للصيدلية» → الحالة `QUOTED` + إشعار للصيدلية.
4. بعد الاعتماد: يحدّث الحالة `SHIPPED` (مع رقم قسيمة التسليم اختياريًا) ثم `DELIVERED` — والصيدلية ترى كل خطوة في صفحة المتابعة عند أول تحديث (polling ≤60 ثانية)، لا لحظيًا بالمعنى الحرفي.

**ج) آلة الحالات (State Machine):**
```
DRAFT → SENT → UNDER_REVIEW → QUOTED → APPROVED → SHIPPED → DELIVERED
                        ↘ REJECTED (نهائي مع إمكانية إعادة فتح طلب جديد)
DRAFT → CANCELLED ، SENT/APPROVED → CANCELLED (بقيد: قبل SHIPPED فقط، مع إشعار للطرف الآخر)
```

### 4.4 عقود الـ API المقترحة

**مسارات الصيدلية (تحت سياق المستأجر الحالي):**
| Endpoint | الوظيفة |
|---|---|
| `GET /api/warehouse-directory?search=` | قائمة المذاخر النشطة + عدد أصنافها (استبدال/تطوير GET الحالي) |
| `GET /api/warehouse-directory/[id]/catalog?search=&barcode=` | كتالوج مذخر معين بأسعاره وتوفره (للواجهة عند التجميع). ⚠️ **البحث يُطابق على `barcode` لا `drugId`** (4.1-أ)، ويجب أن يُرجع صراحة الأصناف التي تعذّرت مطابقتها بدل حذفها بصمت |
| `POST /api/warehouse-orders` | إنشاء الطلب (تطوير POST الحالي + عزل المستأجر + علم الخطة) |
| `GET /api/warehouse-orders?status=&mine=1` | طلبات صيدليتي (صفحة المتابعة) |
| `GET /api/warehouse-orders/[id]` | تفاصيل + أحداث الطلب (Timeline) |
| `POST /api/warehouse-orders/[id]/approve` / `reject` / `cancel` | قرارات الصيدلي |
| `GET/POST /api/warehouse-orders/[id]/events` | سجل الأحداث / إضافة ملاحظة |

**مسارات بوابة المذخر (تحت سياق المذخر الجديد):**
| Endpoint | الوضيفة |
|---|---|
| `GET /api/warehouse-portal/orders?status=` | الطلبات الواردة على مذخري |
| `GET /api/warehouse-portal/orders/[id]` | تفاصيل طلب + أصنافه |
| `PATCH /api/warehouse-portal/orders/[id]/quote` | إرسال المراجعة: لكل صنف {status, quotedPrice, quotedQuantity, note} + تحويل الحالة QUOTED |
| `PATCH /api/warehouse-portal/orders/[id]/shipping` | SHIPPED / DELIVERED |
| `GET/POST/PATCH /api/warehouse-portal/catalog` + `POST /api/warehouse-portal/catalog/import` | إدارة الكتالوج + رفع Excel |
| `GET /api/warehouse-portal/stats` | لوحة معلومات المذخر |

### 4.5 الواجهات الجديدة/المعدلة

**للصيدلية (تطبيق الويب أولاً):**
1. صفحة «**طلبات المذاخر**» جديدة (`(purchases-hub)/warehouse-orders`): جدول بالطلبات مع شارات حالة ملوّنة + فلترة + صفحة تفاصيل بالـ Timeline وبطاقة العرض (Quotation) وأزرار اعتماد/رفض.
2. تطوير «إنشاء فاتورة شراء» الحالية: زر/تبويب «اطلب من مذخر على المنصة» يفتح نفس نموذج الأصناف مع بحث في كتالوج المذخر المختار.
3. ربط صفحة «نقص المخزون» و«الطلب الذكي» الحاليتين بزر «أرسل كمطلب لمذخر» (تحويل الاقتراحات الجاهزة إلى طلب).
4. ⚠️ **صفحة `/dashboard/warehouses` الحالية تُقلَّص لا تُطوَّر:** يُحذف منها نموذج «إضافة مستودع» بالكامل (`WarehousesClient.tsx`) لأن إنشاء المذاخر ينتقل إلى `SUPER_ADMIN` حصرًا — الجدول عالمي بلا `organizationId` والصلاحية الحالية ثغرة كتابة عابرة للمستأجرين (القسمان 2.2 و4.2). تبقى الصفحة **دليل مذاخر للقراءة فقط** مع زر «أرسل طلبًا».
5. شاشة إدارية جديدة لدى `SUPER_ADMIN`: إنشاء حساب المذخر (بيانات المذخر + المستخدم المالك) — هي البديل عمّا حُذف في البند 4.
6. إعادة تسمية كل ما يظهر للمستخدم من «المستودعات» إلى «**المذاخر**»، وإعادة تشغيل مدخل القائمة الجانبية المعطّل **بعد** إنجاز البندين 4 و5 (وإلا فُتحت الثغرة للمستخدمين).

**للمذخر (مساحة جديدة داخل نفس النظام):**
1. `/warehouse` لوحة رئيسية + `/warehouse/orders` (الوارد/قيد التنفيذ/المنجزة) + `/warehouse/orders/[id]` (شاشة التسعير والمراجعة — قلب الميزة) + `/warehouse/catalog` (إدارة الأدوية والأسعار + استيراد Excel).
2. تستفيد الواجهة من نظام التصميم الموحد الموجود (`packages/shared` + `packages/ui` + Glass tokens) فتبدو جزءًا طبيعيًا من النظام.

**تطبيقا الموبايل وسطح المكتب:** خارج نطاق MVP كـ**واجهات كاملة** — الويب أولًا قرار صحيح لأن فواتير الشراء موجودة على الويب حصرًا ولا أثر لها في تطبيق سطح المكتب (POS/مخزون/ديون فقط). الموبايل يكتفي بعرض حالات الطلبات واعتمادها (قراءة + قرار) في مرحلة ثانية.
**لكن استثناء واحد يستحق التقديم:** سطح المكتب هو السطح اليومي الفعلي للصيدلاني، فبقاء متابعة الطلبات على الويب وحده يعني اعتماد الميزة على أن يتذكر المستخدم فتح صفحة أخرى. **شارة إشعار بسيطة في تطبيق Electron** (عدد العروض المنتظرة + رابط يفتح الصفحة في المتصفح) تكفي، ولا تحتاج طبقة المزامنة — انظر 4.6.

### 4.6 الإشعارات
- إعادة استخدام `Notification` (نوع جديد `WAREHOUSE_ORDER`) + Push (expoPushToken):
  - للمذخر: «طلب جديد WO-000123 من صيدلية كرادة».
  - للصيدلية: «عرض السعر جاهز لطلبك»، «طلبك قيد التسليم»، «صنف طلبك نافد».
- الإشعار يعتمد إنشاء الأحداث في `WarehouseOrderEvent` ليقرأ الطرفان نفس القصة.
- ⚠️ **تصحيح: لا توجد بنية لحظية في تطبيق الويب إطلاقًا** — لا WebSocket ولا SSE ولا حتى polling (فحص: صفر نتائج لـ `EventSource|WebSocket|text/event-stream` في `apps/web`). نموذج `Notification` مجرد جدول يُقرأ عند تحميل الصفحة. فكلمة «فوري» في هذا التقرير تعني عمليًا أحد خيارين في MVP: **polling كل 30–60 ثانية** في هيكل بوابة المذخر (الأبسط والموصى به)، أو Push فقط عبر Expo لمن نصّب تطبيق الموبايل. أي وعد بـ «ثوانٍ» يحتاج بنية جديدة خارج نطاق MVP.
- **ملاحظة سطح المكتب:** السطح اليومي للصيدلاني هو تطبيق Electron (POS/مخزون/ديون) لا لوحة الويب — فواتير الشراء أصلًا على الويب فقط. لذا صفحة متابعة الطلبات على الويب تعني أن على الصيدلي أن **يتذكر** فتحها. يستحق شارة إشعار في تطبيق سطح المكتب أبكر مما اقتُرح (المرحلة 1 أو بداية 2 لا نهايتها).

### 4.7 نقاط القوة الحالية التي تسرّع التنفيذ
1. ~~`GlobalDrug` موحد بالباركود → الكتالوج المشترك جاهز مفاهيميًا.~~ **صُحّح:** التوحيد غير موجود بسبب `@@unique([barcode, organizationId])`. تبقى نقطة قوة **مشروطة**: الباركود موجود ومفهرس (`@@index([barcode])`) في كل الصفوف، فالمطابقة عليه ممكنة بلا هجرة بيانات — لكنها تحتاج طبقة `resolveToGlobalDrug()` مكتوبة عمدًا (4.1-أ) لا مجرد مفتاح أجنبي.
2. بنية صلاحيات JSON جزئية على `User` + نظام حراسة صفحات (`page-guards`, `requireFeature`) جاهز لإضافة حراسة الدور الجديد.
3. تدفق مشتريات/استلام/دفعات مكتمل (`Purchase → receive → Batch` + مورّد على الدفعة من ميزة 007) → الطلب المعتمد يسقط مباشرة في مسار موجود بدل بناء مخزن موازٍ.
4. «الطلب الذكي» يحوّل الميزة من مجرد «إرسال طلب» إلى **إعادة طلب ذكية** — ميزة تنافسية قوية للتسويق.
5. بنية الإشعارات **المخزَّنة** جاهزة (`Notification` + `expoPushToken`) — لكن **بلا قناة تسليم لحظية في الويب**؛ يلزم إضافة polling في MVP (4.6).
6. حقلا `apiEndpoint/apiKey` في `Warehouse` يدعمان مستقبلًا المذاخر التي تملك نظامًا خاصًا (تكامل API بدل اليدوي).

---

## 5) خطة التنفيذ المرحلية

### المرحلة 0 — قرارات قبل الكود (5–7 أيام)

**قراران حاجزان — لا يبدأ أي migration قبل حسمهما (كلاهما يغيّر المخطط نفسه):**
1. **هوية الصنف (4.1-أ):** المطابقة على `barcode` (خيار أ-1، موصى به) أم حصر الكتالوج بالصفوف العالمية مع ترحيل بيانات (خيار أ-2)؟ وما سلوك الأصناف بلا باركود؟
2. **ربط المورد (4.1-ب):** حقل `Supplier.warehouseId` مع إنشاء تلقائي للمورد المرآة — تأكيد أن هذا مقبول محاسبيًا لدى الصيدليات (سيظهر مورد جديد في قوائمهم دون أن ينشئوه يدويًا).

**قرار حاجز ثالث — غير تقني:**
3. **التحقق من الإطلاق البارد:** تأكيد وجود **3–5 مذاخر حقيقية** في مدينة واحدة وافقت شفهيًا على الاستخدام، **قبل** كتابة الـ migration. الكود فشل في هذا الاختبار مرة سابقة — كومنت القائمة الجانبية هو شاهد قبر المحاولة الأولى. بناء الميزة كاملة ثم البحث عن مذاخر هو تكرار الخطأ نفسه.

**قرارات تُحسم بالتوازي (لا تحجب البدء):**
- تثبيت التسمية: «المذاخر» في الواجهة، ومعرّف برمجي موحد (نقترح إبقاء `Warehouse` في الكود لتفادي refactor ضخم، مع توثيق ذلك).
- نموذج التسعير: هل إرسال الطلبات مقيّم بخطة الصيدلية؟ وهل حساب المذخر مجاني؟ (التوصية: مجاني للطرفين في الإطلاق).
- قنوات الإشعار في MVP: التوصية بعد تصحيح 4.6 — **polling كل 30–60 ثانية** + Push للموبايل، وعدم الوعد بأقل من ذلك.
- من يتحقق من هوية المذاخر عند التسجيل؟ (الجواب المعماري محسوم: `SUPER_ADMIN` — انظر 4.2 بند 4).

### المرحلة 1 — MVP (الويب فقط، **5–7 أسابيع** لمطوّرَين)

> **سبب رفع التقدير من 3–5:** التقدير الأول عامل طبقة الهوية كبند واحد من ستة. عمليًا هي كتلة عمل مستقلة تمس خمسة مواضع في `auth.config.ts` و`middleware.ts` و`tenant-utils.ts` (جدول 4.2)، ولا يعمل أي شيء آخر قبل اكتمالها، ويصعب تقسيمها بين مطوّرَين. أضف إليها بندًا جديدًا لم يكن في التقدير الأصلي أصلًا (طبقة مطابقة الباركود).

0. **إصلاح الثغرات الأمنية أولًا** (يوم–يومان، مستقل عن كل ما بعده): عزل `GET /api/warehouses/orders`، ونزع `POST /api/warehouses` من أدمن الصيدلية. هذه ديون قائمة تُسدَّد بغض النظر عن مصير الميزة.
1. **طبقة الهوية (المسار الحرج — تُنجز أولًا وبالكامل):** دور `WAREHOUSE` + `User.warehouseId` + `getWarehouseContext()` + حراسة `/warehouse/*` في `authorized()` + التوجيه حسب الدور في `auth.config.ts` و`middleware.ts` + تمرير `warehouseId` عبر jwt/session. **معيار الخروج: اختبار عزل آلي أخضر قبل بناء أي شاشة.**
2. Schema migration: `WarehouseCatalogItem` (بمفتاح `barcode`) + `Supplier.warehouseId` + تطوير الحالات + `WarehouseOrderEvent` + **دالة `resolveToGlobalDrug(barcode)` واختباراتها** (+ بذر بيانات تجريبية: مذخر + كتالوج + صيدلية بدواء خاص لاختبار المطابقة).
3. بوابة المذخر: لوحة + كتالوج (CRUD + Excel import مع تقرير مطابقة) + صندوق الطلبات + شاشة التسعير/المراجعة.
4. جانب الصيدلية: دليل المذاخر (قراءة فقط) + إنشاء الطلب (من الشاشة الجديدة ومن الطلب الذكي) + صفحة المتابعة بالتفاصيل (بـ `tenantBranchWhere`) + اعتماد/رفض.
5. الجسر للمشتريات: APPROVED → إنشاء/جلب المورد المرآة → Purchase مسودة → استلام عادي.
6. الإشعارات: `Notification` + polling في هيكل البوابة + Push.
7. اختبارات: vitest موجودة أصلًا في المشروع — تغطية إلزامية لـ **آلة الحالات**، **عزل المذخر عن الصيدليات**، و**مطابقة الباركود عبر المستأجرين** (الثلاثة هي مواضع الفشل الصامت).

### المرحلة 2 — بعد أول شبكة حية (3–4 أسابيع)
- طلبات جزئية (شحن ما توفر)، بدائل الأصناف، فواتير PDF للطلب/العرض، تقارير للمذخر، دعم الموبايل (عرض + اعتماد)، تفعيل الإشعار عبر WhatsApp للطلبات الحرجة.
- **شارة إشعار في تطبيق سطح المكتب** (Electron) لتنبيه الصيدلاني بعرض سعر جاهز — لأن سطحه اليومي ليس لوحة الويب (انظر 4.6). يُفضّل تقديمها لنهاية المرحلة 1 إن سمح الوقت.
- ملاحظة: أرصدة وديون المذخر **لم تعد بندًا مستقلًا** — تأتي مجانًا مع المورد المرآة و`SupplierPayment` القائم (4.1-ب).

### المرحلة 3 — التوسّع
- تسجيل مذاخر ذاتي مع موافقة إدارية (KYC)، تكامل API بالمفاتيح (`apiEndpoint/apiKey`) للمذاخر ذات الأنظمة الخاصة، تقارير أسعار سوقية مجهولة (benchmark)، الالتقاء مع Marketplace (المذخر كـ seller نوع جديد).

---

## 6) المخاطر والمعالجات

| الخطر | الأثر | المعالجة |
|---|---|---|
| الإطلاق البارد (كومنت الكود يقولها صراحة: قيمة صفرية دون onboarding للموردين) | الميزة ميتة ولو بُنيت كاملة | حسابات مذاخر مجانية + onboarding يدوي لأول 3–5 مذاخر في مدينة واحدة + إبقاء الموردين اليدويين يعملون بالتوازي |
| مساس سياق المستأجر (`getTenantContext`) بإضافة طرف رابع لا ينتمي لـ Organization | خلط بيانات/ثغرة أمنية | سياق `getWarehouseContext()` منفصل + اختبارات عزل إلزامية لكل endpoint |
| ازدواجية المفهوم Supplier/Warehouse لدى المستخدمين | ارتباك | دمج الاختيارين في شاشة «المورد» بقائمتين واضحتين، ثم ترحيل لاحق بقرار إداري |
| **[جديد — الأخطر تقنيًا] فرضية توحيد الكتالوج خاطئة** (`@@unique([barcode, organizationId])`) | **فشل صامت**: الصيدلي يرى «غير متوفر» لصنف متوفر فعلًا، وبنود الطلب تحمل `drugId` لا يفهمه الطرف الآخر. يُكتشف بعد الإطلاق ويُفقد الثقة | حسم 4.1-أ في المرحلة 0؛ المطابقة على `barcode`؛ اختبار عبر-مستأجرين إلزامي (صيدلية بدواء خاص ← مذخر بصف عالمي) |
| **[جديد] `Purchase.supplierId` إلزامي و`Supplier` مرتبط بالمنظمة بينما `Warehouse` جدول عالمي** | جسر «الاعتماد ← فاتورة شراء» لا يعمل كما وُصف؛ يُكتشف في منتصف التنفيذ | `Supplier.warehouseId` + إنشاء تلقائي للمورد المرآة داخل نفس الـ transaction (4.1-ب) + منع تعديله يدويًا |
| **[جديد] كتابة عابرة للمستأجرين في `POST /api/warehouses`** (جدول عالمي بلا `organizationId`، و`code` فريد عالميًا) | أي أدمن على ENTERPRISE يكتب في جدول مشترك ويحجز أكواد غيره | نقل الإنشاء إلى `SUPER_ADMIN` حصرًا + **حذف** نموذج الإضافة من واجهة الصيدلية (المرحلة 1 بند 0) |
| **[جديد] لا بنية لحظية في الويب** (لا SSE/WebSocket/polling) | وعد «إشعار خلال ثوانٍ» غير قابل للتحقيق؛ معيار قبول يسقط | polling 30–60 ثانية في MVP + Push للموبايل، وتعديل صياغة معايير القبول (تم) |
| **[جديد] طبقة الهوية أكبر مما قُدّر** (5 مواضع في auth.config/middleware/tenant-utils) | انزلاق جدولي في منتصف المشروع | إنجازها أولًا وبالكامل مع اختبار عزل أخضر كمعيار خروج قبل أي شاشة (المرحلة 1 بند 1) |
| تسريب قراءة في `GET /api/warehouses/orders` (بدون عزل/بدون حماية خطة) | تسريب بيانات بين المستأجرين | تُصلح في بداية المرحلة 1 قبل أي إطلاق |
| سباق ترقيم الطلبات `WO-{count+1}` | أرقام مكررة | تسلسل آمن (unique + retry) أو cuid |
| غياب endpoint تحديث الحالة اليوم يعني أن أي بناء فوق الحالي بدون Phase تصميم سيتجاهل دورة التفاوض | ميزة ناقصة | اعتماد آلة الحالات المقترحة كعقد أساسي قبل كتابة الواجهات |
| أداء بحث الكتالوج (مذاخر × أصناف) | بطء الواجهة | فهارس `@@index([barcode])` و`@@index([drugId])` معًا (البحث يمر على الباركود — 4.1-أ) + بحث نصي موجود بالنمط (Fuse.js مستخدم أصلًا) + pagination |

---

## 7) معايير القبول (Acceptance Criteria)

1. صاحب مذخر بدور `WAREHOUSE` يسجّل الدخول **فيهبط على `/warehouse` لا `/dashboard`**، ولا يرى أي شاشة من لوحات الصيدليات، ويُرفض من كل مسار صيدلية بـ 403 (اختبار عزل مؤتمت يغطي المسارات الخمسة في جدول 4.2). والعكس: مستخدم صيدلية يُرفض من كل `/api/warehouse-portal/*`.
2. المذخر يرفع 50 صنفًا عبر Excel ويظهر فورًا في كتالوجه بأسعاره.
3. الصيدلي ينشئ طلبًا من شاشة الطلب الذكي لمذخر معين → يظهر الطلب في صندوق المذخر **خلال دورة polling واحدة (≤60 ثانية) دون إعادة تحميل يدوي**، ويصله إشعار Push إن كان مسجّلًا على الموبايل. (صيغة «خلال ثوانٍ» في النسخة الأولى غير قابلة للتحقيق — لا توجد بنية لحظية في الويب، انظر 4.6.)
3-ب. **[معيار جديد — الأهم] المطابقة عبر المستأجرين:** صيدلية أنشأت صفًا خاصًا لدواء له باركود موجود في كتالوج المذخر → يظهر الصنف **متوفرًا** عند التجميع، ويصل لبند الطلب معرّف يفهمه الطرفان. واختبار معاكس: صنف بلا باركود يُرفض برسالة صريحة لا بصمت.
4. المذخر يعدّل صنفين (واحد نافد وواحد بسعر جديد) ويعيد الإرسال → الصيدلي يرى الحالة `QUOTED` مع التفاصيل في صفحة المتابعة و Timeline كامل لمن فعل ماذا ومتى.
5. اعتماد العرض ينشئ فاتورة شراء مسودة بنفس الأسعار — **مع إنشاء المورد المرآة تلقائيًا في أول تعامل مع هذا المذخر** — واستلامها يضيف دفعات للمخزون مرتبطة بالمذخر كمورد. تكرار الاعتماد لطلب ثانٍ من نفس المذخر **لا** ينشئ موردًا مكررًا.
5-ب. صفحة متابعة الصيدلي تعرض طلبات **فرعه** فقط (`tenantBranchWhere`)، وأدمن صيدلية متعددة الفروع يرى فروعه هو لا غيرها.
6. لا يمكن تحويل طلب إلى SHIPPED قبل APPROVED، ولا إلغاؤه بعد SHIPPED (قواعد آلة الحالات مختبرة).
7. كل نصوص الواجهة بالعربية تستخدم «المذاخر»، والصفحة تظهر في القائمة الجانبية مفعلة.

---

## 8) أسئلة مفتوحة تحتاج قرارك قبل البدء

> **أضيف في النسخة 2:** السؤالان 0-أ و0-ب أدناه **حاجزان** — إجابتهما تغيّر مخطط قاعدة البيانات، فلا يبدأ أي كود قبلهما. أما السؤال 1 فأصبح محسومًا معماريًا بحكم أن `getTenantContext` يرفض أي مستخدم بلا فرع (4.2).

**0-أ. هوية الصنف:** المطابقة بين كتالوج المذخر وبحث الصيدلية على `barcode` (خيار أ-1، **موصى به**) أم حصر الكتالوج بصفوف `GlobalDrug` العالمية مع ترحيل بيانات الصيدليات (خيار أ-2)؟ وماذا نفعل بالأصناف بلا باركود؟
**0-ب. المورد المرآة:** هل يقبل عملاؤك ظهور مورد جديد في قوائمهم يُنشأ **تلقائيًا** عند أول طلب معتمد من مذخر (اللازم لأن `Purchase.supplierId` إلزامي)؟
**0-ج. الإطلاق البارد:** هل لديك 3–5 مذاخر حقيقية في مدينة واحدة وافقت على الاستخدام؟ إن لم يكن — هذا هو العمل الأول، لا الكود.

1. هل بوابة المذخر داخل نفس تطبيق الويب بدور جديد (التوصية) أم موقع/دومين منفصل؟
2. هل يعتمد الصيدلاني العرض قبل الشحن إلزاميًا (التوصية: نعم — حماية الطرفين)، أم يُشحن مباشرة بعد تأكيد المذخر؟
3. هل تريد دمج `Supplier` اليدوي الحالي في حسابات المذاخر لاحقًا، أم إبقاؤهما منفصلين دائمًا؟
4. من يدفع؟ (اشتراك صيدلية أعلى؟ عمولة على المذخر؟ مجاني للنمو؟)
5. هل المذخر الواحد يملك حسابًا واحدًا أم مستخدمين متعددين بصلاحيات مختلفة؟ (التصميم أعلاه يدعم الاثنين).
6. التسعير داخل النظام: هل يظهر الصيدلي أسعار عدة مذاخر للمقارنة قبل الطلب (توصية: نعم في المرحلة 2) أم يختار مذخرًا واحدًا أولًا؟

---

## 9) الخلاصة
الأساس الجزئي الموجود («المستودعات»: نماذج + API أولي + صفحة إدارة معطلة) يوفر نحو ثلث العمل التحضيري على مستوى البيانات فقط، لكن **قلب الميزة — حساب المذخر بدور خاص، كتالوج أدويته، دورة التفاوض على الطلب، وصفحة متابعة الصيدلي — غير موجود إطلاقًا ويتطلب بناءً جديدًا**.

البنية التحتية للنظام (تعدد المستأجرين، الأدوار، الإشعارات، فواتير الشراء والاستلام، الطلب الذكي) مناسبة فعلًا لبناء الميزة فوقها، لكن **بعد التدقيق التفصيلي لم تعد «منخفضة المخاطرة» بالإطلاق الذي وصفته النسخة الأولى**. ثلاثة أمور تقرر نجاح المشروع، بهذا الترتيب:

1. **هوية الصنف (4.1-أ)** — الخطر الأكبر لأنه يفشل **صامتًا**: لا استثناء ولا رسالة خطأ، فقط أصناف «غير متوفرة» تُفقد ثقة الطرفين. يُحسم في المرحلة 0 ويُغطّى باختبار عبر-مستأجرين.
2. **طبقة الهوية (4.2)** — كتلة عمل مستقلة تمس خمسة مواضع في الكود، لا يعمل شيء قبلها، وهي سبب رفع التقدير إلى 5–7 أسابيع. تُنجز أولًا مع اختبار عزل أخضر كمعيار خروج.
3. **الإطلاق البارد** — ليس مخاطرة تقنية بل شرط وجود. المحاولة الأولى ماتت عليه بالضبط، وكومنت القائمة الجانبية يقولها صراحة: «Zero value without supplier-side onboarding». تأكيد 3–5 مذاخر حقيقية موافقة **يسبق** أول migration.

ما عدا ذلك، التصميم المقترح (آلة الحالات، حالة الصنف المفصّلة، سجل الأحداث، الجسر لفواتير الشراء) هو الشكل الصحيح للميزة كما وُصفت. بحسم قرارَي المرحلة 0 وتأكيد المذاخر، يمكن البدء بالمرحلة 1 وفق الترتيب المعدّل أعلاه.

---

## 10) سجل تنفيذ المراحل (Progress Log)

> يُحدَّث هذا القسم بعد إتمام كل مرحلة. المراحل وفق الخطة المعدّلة في القسم 5 أعلاه.

### ✅ المرحلة 0 — التحصينات الأمنية (منجزة 2026-09-03)

- إغلاق تسريب القراءة العابر للمستأجرين في `GET /api/warehouses/orders` عبر `warehouseOrderScope()`.
- قصر إنشاء المستودعات على `SUPER_ADMIN` حصرًا (`canCreateWarehouse()`) وإغلاق الكتابة العابرة للمستأجرين في إنشاء الطلبات.
- منطق الوصول مستخرج كدوال نقية في `app/lib/warehouse-access.ts` مع اختبارات (قابلة للتشغيل دون قاعدة بيانات).
- التحقق: vitest 36/36، tsc نظيف، prisma validate ناجح. (نفّذها وكيل مرحلة مع مراجعة بشرية).
- ملاحظة مسجلة للمرحلة 4: نموذج «إضافة مستودع» في `/dashboard/warehouses` أصبح يعيد 403 دائمًا ويجب حذفه من الواجهة.

### ✅ المرحلة 1 — طبقة الهوية (منجزة 2026-09-03)

- دور `WAREHOUSE` في `enum Role` + `User.warehouseId` + `WarehouseUserType (OWNER/STAFF)` + فهرس وعلاقة.
- `getWarehouseContext()` في `app/lib/warehouse-context.ts` بعزل كامل عن سياق الصيدليات (fail-closed).
- حراسة `/warehouse/*` في `middleware.ts` و`auth.config.ts` + توجيه حسب الدور (WAREHOUSE يهبط على `/warehouse`).
- تحصين إضافي: `getTenantContext()` يرفض دور `WAREHOUSE` قبل أي منطق فروع (سدّ أرضية الزحف العكسية).
- ترحيل يدوي `20260903000000_add_warehouse_role_and_user_link` (غير مطبق بعد على أي قاعدة بيانات — ترويسة الملف توثق خطوات ما قبل التطبيق).
- تصحيح منفّذ بشرٍ أثناء المراجعة: `prisma format` الذي شوّش المخطط أُلغي وأعيد بناء الملف من HEAD بتعديلات موضعية (14 إضافة فقط).
- التحقق: vitest 54/54، tsc نظيف، prisma validate + generate ناجحان.
- ⚠️ قيد مستمر: قاعدة البيانات الحية لم تُلمس (6 ترحيلات معلقة سابقة تتطلب حسمًا بشريًا أولًا) — كل التحقق ساكن.

### ✅ المرحلة 5 — جسر المشتريات (منجزة 2026-09-03)

**اعتماد العرض يُنشئ فاتورة شراء مسودة بالمورد المرآة تلقائياً — معيار القبول 5 و5-مكرر محقق.**

- **منطق نقي جديد** `app/lib/warehouse-purchase-bridge.ts`: `decideMirrorSupplier` (مورد موجود → USE بلا إنشاء؛ غير موجود + منظمة → CREATE؛ SUPER_ADMIN بلا منظمة → رفض 403 صريح) و`buildDraftPurchasePlan` (الأصناف النافدة تُستثنى، PARTIAL بكميته المتاحة، السعر الفعلي = quotedPrice ← requestedPrice ← unitPrice، والكميات/الأسعار غير الصالحة تُرفض مفهرسة) — مع **6 اختبارات** تغطي لا-تكرار المورد والإجماليات والرفضات.
- **مسار القرار طُوّر** (`POST /api/warehouses/orders/[id]`): الرفض/الإلغاء كما كانا (حدث Timeline فقط)، أما **الاعتماد** فيُنفّذ داخل transaction واحدة: قرار المورد المرآة → إنشاء `Supplier` باسم «مذخر على المنصة: {اسم المذخر}» مرتبط بـ `warehouseId` و`organizationId` (القيد الفريد `@@unique([organizationId, warehouseId])` يحرس التسابق وP2002 يُعاد له برسالة إعادة محاولة) → إنشاء `Purchase` مسودة (status PENDING) ببنود العرض المعتمد → حدث Timeline محمّل بـ `purchaseId` — والاستلام اللاحق يمر بالمسار العادي الحالي فيتضمن المورد على الدفعات تلقائياً (ربط ميزة 007).
- استجابة الاعتماد تعيد `purchaseId` للواجهة (تمهيداً لتوجيه اختياري لفاتورة الشراء في لاحقاً).
- **التحقق**: `prisma validate` ✅، **vitest 84/84** (78 → 84)، `tsc --noEmit` exit 0 (بعد إصلاح تمرير `organizationId ?? null` لنوع القرار النقّي). ساكن بالكامل دون لمس قاعدة البيانات.
- ملاحظة للمرحلة 6: الإشعارات سترتكز على `WarehouseOrderEvent` المكتوبة الآن في كل خطوة (SENT/UNDER_REVIEW/QUOTED/APPROVED/REJECTED/SHIPPED/DELIVERED) — polling 30–60 ثانية في بوابة المذخر وصفحة متابعة الصيدلية + Push للموبايل حيث expoPushToken موجود.
### ✅ المرحلة 4 — جانب الصيدلية (منجزة 2026-09-03)

**دليل المذاخر + إنشاء الطلب (شاشة مخصصة + تكامل الطلب الذكي) + صفحة المتابعة بالقرارات — كاملة.**

- **API جديد**: `GET /api/warehouses/directory` (دليل المذاخر النشطة + كتالوج مذخر محدد بالبحث — قراءة فقط)، `POST /api/warehouses/orders` **أعيد بناؤه**: مطابقة الأصناف عبر الباركود على الصفوف العالمية حصراً (`resolveToGlobalDrug`) فيحمل بند الطلب `drugId` يفهمه الطرفان (§4.1-أ)، رفض صريح لكل صنف غير قابل (بلا باركود/غير مسجل/كمية أو سعر غير صالح) مع تقرير أخطاء مفهرس، حالة ابتدائية `SENT` مباشرة، `requestedPrice` مسجل، رقم طلب آمن ضد السباق (بدل عدّاد count+1)، وحدث Timeline عند الإرسال، `POST /api/warehouses/orders/[id]`: قرار الصيدلي (APPROVED/REJECTED من QUOTED حصراً، CANCELLED قبل SHIPPED) عبر `assertTransition` (409 عند المخالفة) + حدث Timeline + سبب الرفض في payload.
- **صفحة «طلب جديد من مذخر»** (`/dashboard/purchases/warehouse-orders/new`): اختيار مذخر → كتالوجه بأسعاره المتوفرة → سلة بكميات قابلة للتعديل وملاحظات → إرسال. محمية بعلم الخطة `warehouseManagement` (UpgradeRequired تلقائي).
- **تكامل الطلب الذكي**: زر «إرسال لمذخر» في `smart-order-client` يفتح مربعاً يعرض المذاخر، يجلب كتالوج المذخر المختار ويطابق باركودات البنود المختارة، **يستثني غير المتوفر بإشعار صريح**، ويسمح بضبط الكميات حسب أسعار المذخر قبل الإرسال (تحويل الاقتراح إلى طلب بضغطة زر).
- **صفحة «طلبات المذاخر» للمتابعة** (`/dashboard/purchases/warehouse-orders`): قائمة بفلترة الحالات التسع + بطاقات شارات ملوّنة، نطاق القراءة عبر `warehouseOrderScope` (فرع المستخدم؛ أدمن المؤسسة يرى فروع مؤسسته — معيار القبول 5-ب)، ونافذة تفاصيل تعرض **بطاقة العرض** (لكل صنف: الحكم/الكمية النهائية/السعر النهائي/ملاحظة المذخر + الإجمالي) و**Timeline كامل** (من فعل ماذا ومتى بالعربية) وأزرار اعتماد/رفض/إلغاء حسب الحالة.
- **القائمة الجانبية**: قسم «المذاخر» حي الآن (مدخل «طلبات المذاخر») محل قسم FUTURE الميت — مع الحفاظ على تعليق الأرشيف لسياق القرار الأصلي.
- **تنظيف ملاحظة المرحلة 0**: حذف نموذج «إضافة مستودع» من `WarehousesClient` (كان يعيد 403 دائماً بعد قصر الإنشاء على SUPER_ADMIN) — الملف أصبح عرض قراءة فقط.
- **التحقق**: `prisma validate` ✅، **vitest 78/78**، `tsc --noEmit` exit 0 (بعد إصلاحين: استيراد افتراضي لمكوّن المربع الجانبي، وتداخل JSX في ذيل الملف). ساكن بالكامل دون لمس قاعدة البيانات.
- ملاحظة للمرحلة 5: الاعتماد (APPROVED) يُنشئ حالياً حدث Timeline فقط — إنشاء فاتورة الشراء المسودة عبر المورد المرآة هو عمل المرحلة 5 (داخل نفس الـ transaction).
### ✅ المرحلة 3 — بوابة المذخر (منجزة 2026-09-03)

**لوحة المذخر + الكتالوج + صندوق الطلبات + شاشة التسعير/المراجعة — كاملة وتعمل على حساب دور `WAREHOUSE`.**

- **الهيكل**: `app/warehouse/layout.tsx` — تبويبات (الرئيسية / أدويتي / الطلبات) مع حراسة الدور في كل صفحة (defense-in-depth فوق middleware).
- **اللوحة** `/warehouse`: بطاقات (طلبات جديدة بانتظار المراجعة، قيد التنفيذ، مكتملة، أصناف الكتالوج المتوفرة) + مبيعات الشهر الحالي (الطلبات المعتمدة وما بعدها).
- **الكتالوج** `/warehouse/catalog`: قائمة ببحث فوري + إضافة صنف بالباركود (عبر `resolveToGlobalDrug` — الصفوف العالمية حصراً ورفض صريح لغير المبارَز) + تعديل سعر بالنقر + تبديل التوفر + حذف + **استيراد Excel** (قالب قابل للتنزيل، قراءة محلية بمكتبة xlsx، تقرير مطابقة كامل: جديد/محدّث/فاشل مع أرقام الصفوف وأسبابها).
- **صندوق الطلبات** `/warehouse/orders`: قائمة بفلترة الحالة (لا تظهر DRAFT إطلاقاً) + بدء المراجعة (SENT→UNDER_REVIEW) + **شاشة التسعير**: لكل صنف متوفر/جزئي/نافد + السعر النهائي + الكمية الجزئية + ملاحظة للصيدلية، وإجمالي العرض حي، ثم «إرسال العرض» (QUOTED) مع كتابة الحدث في الـ Timeline.
- **API** `/api/warehouse-portal/*`: `orders` (GET/POST)، `orders/[id]` (GET)، `orders/[id]/quote` (PATCH)، `catalog` (GET/POST/PATCH/DELETE)، `catalog/import` (POST). كل مسار محمي بـ `getWarehouseContext()` (رفض 401/403 لأي طالب غير WAREHOUSE بحساب مذخر صالح) + كل كتابة داخل transaction + كل تغيير حالة عبر `assertTransition` (409 عند الانتقال غير الشرعي) + كل عملية مراجعة عبر `buildQuoteDecision` (رفض 400 مع تفاصيل عربية عند حكم ناقص/أسعار غير صالحة).
- **منطق نقي جديد**: `app/lib/warehouse-quote.ts` (قواعد الحكم الثلاثة + حساب الإجماليات + منع الحكم الناقص/المكرر) مع **9 اختبارات** تغطي المسار السعيد وتغيير الأسعار والجزئي والنافد والرفضات.
- **تصحيح أثناء العمل**: إصلاح خطأ TS في مسار quote (Map من `Object.entries` بدل `.entries()` على Record) — `tsc` صفر أخطاء بعده.
- **التحقق (ساكن، دون لمس قاعدة البيانات)**: `prisma validate` ✅، **vitest 78/78** (69 → 78 بإضافة اختبارات التسعير)، `tsc --noEmit` exit 0.
- ملاحظة للمرحلة 4: مسارات الصيدلية الجديدة ستحتاج نفس النمط (`getTenantContext` + `tenantBranchWhere` لطلبات الفرع) + إشعار `Notification` عند كل QUOTED/حالة جديدة.
### ✅ المرحلة 2 — مخطط B2B الكامل (منجزة 2026-09-03)

- **`WarehouseCatalogItem`**: كتالوج المذخر بمفتاح مطابقة `barcode` (الخيار أ-1) + `drugId` للصف العالمي فقط + فهارس باركود/دواء + `@@unique([warehouseId, barcode])`.
- **`Supplier.warehouseId`**: المورد المرآة لمذخر على المنصة + `@@unique([organizationId, warehouseId])` (لا يؤثر على الموردين اليدويين — NULLs خارج القيد).
- **آلة الحالات**: `WarehouseOrderStatus` أصبح (DRAFT, SENT, UNDER_REVIEW, QUOTED, APPROVED, REJECTED, SHIPPED, DELIVERED, CANCELLED) والافتراضي DRAFT؛ و`WarehouseOrderItemStatus` جديد (REQUESTED, AVAILABLE, PARTIAL, OUT_OF_STOCK) مع أعمدة التفاوض: `requestedPrice/quotedPrice/quotedQuantity/status/note`.
- **`WarehouseOrderEvent`**: سجل أحداث الطلب (actorType/actorName/type/payload) بفهرس (orderId, createdAt) — أساس صفحة المتابعة.
- **المنطق النقي**: `app/lib/warehouse-order-state.ts` (جدول الانتقالات + assertTransition برسائل عربية) و`app/lib/warehouse-catalog.ts` (`resolveToGlobalDrug` تطابق الصفوف العالمية حصرًا + `barcodeChangeDecision`).
- **حارس الباركود مطبق فعليًا** في `updateDrug` و`updateGlobalDrug` (`app/lib/actions/drug.ts`): منع تعديل باركود أي دواء مدرج في كتالوج مذخر (سدّ الفشل الصامت §4.1-أ).
- **ترحيل يدوي** `20260904000000_warehouse_b2b_schema` (غير مطبق على أي قاعدة): إعادة بناء نوع الحالة مع تحويل بيانات آمن (PENDING→SENT، CONFIRMED→UNDER_REVIEW)، الجداول والقيود الجديدة كاملة بأسلوب ترحيلات Prisma الفعلية.
- **بذر تجريبي** `prisma/seed-warehouse-demo.ts` (ملف جاهز، يُشغّل يدويًا بعد الترحيلات فقط): مذخر + حساب WAREHOUSE + 5 أدوية عالمية + كتالوج + صيدلية بصنف خاص يحمل نفس باركود عالمي (اختبار المطابقة عبر المستأجرين 3-ب).
- **التحقق (ساكن بالكامل، دون لمس قاعدة البيانات)**: `prisma validate` ✅، `prisma generate` ✅، **vitest 69/69** (أضيف 15: آلة الحالات 8 + المطابقة/الحارس 7)، `tsc --noEmit` exit 0، فرق schema موضعي نظيف (80 إضافة / 3 حذوف).
- ملاحظة: أُنجزت المرحلة 2 تنفيذًا مباشرًا بعد تعذر إطلاق وكيل التنفيذ (خطأ مزوّد النموذج 403 مرتين)؛ جميع القيود احتُرمت (لا migrate/db push، لا prisma format، لا commit).
