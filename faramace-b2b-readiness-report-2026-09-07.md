# تقرير التحقق وجاهزية إطلاق Faramace B2B

> تحديث: راجع `release-readiness-2026-09-08.md` لنتائج التحقق الأحدث؛ التقييمات العددية أدناه تاريخية ولا تمثل موافقة إطلاق حالية.

**التاريخ:** 2026-09-08 (مراجعة مستقلة ثانية)
**النطاق:** تطبيق الويب، مسار المذاخر B2B، قاعدة البيانات، وتغليف سطح المكتب.

## الخلاصة

خضع هذا التقرير لتحقّق مستقل ثانٍ. **كل ادّعاء قابل للفحص فيه ثبتت صحته** (Next 15.5.25، إزالة `xlsx`، أعداد الاختبارات، الترحيلات، حماية حزمة سطح المكتب).

الاستثناء الوحيد هو **بند التدقيق الأمني**: لم يكن «ناقصاً» كما وُصف، بل **لم يُنفَّذ إطلاقاً**. نُفِّذ الآن، فكشف **٦٩ تحذيراً بينها ثغرتان حرجتان** في شجرة الإنتاج. عولجت **٦٢ منها**، وبقيت ٧ موثّقة بأسبابها.

النتيجة بعد المعالجة: **9.3/10**. لا ترتفع إلى 10/10 قبل نشر canary وتمرين rollback فعليين.

## ما تم إصلاحه (من المراجعة الأولى — تم التحقق منه)

- استلام الشراء ذرّي ومقفل على مستوى الصف؛ يمنع التكرار والكميات غير الصحيحة والدواء المجهول والصلاحية الماضية وتكرار تسجيل الدين/المصروف.
- كل عمليات المذاخر (مراجعة، تسعير، اعتماد، إلغاء، شحن، تسليم، دفع) تتحقق من انتقال الحالة داخل معاملة وتمنع سباقات الاعتماد/الدفع.
- عزل tenant/branch إلزامي في المشتريات والموردين والطلبات؛ التوكن القديم لا يمنح صلاحيات بعد تعطيل المستخدم أو المذخر.
- صلاحيات JSON صارت allowlist لقيم Boolean فقط.
- صندوق الطلبات لا يعرض DRAFT غير المرسل، مع حدود pagination والتحقق من المدخلات.
- تحديث Next.js من 14.2.29 إلى **15.5.25** — مؤكَّد.
- `idempotencyKey` فريد للطلبات والمدفوعات؛ migration `20260913000000_add_b2b_idempotency_keys` مطبَّقة.
- استُبدل `xlsx` بـ ExcelJS بالكامل — **مؤكَّد**: صفر استيراد لـ`xlsx` في المصدر، وغياب تام من `pnpm-lock.yaml` والـmanifests، وExcelJS مستخدم في ١٢ ملف مصدر.
- حزمة سطح المكتب لا تنسخ قاعدة بيانات العميل — **مؤكَّد وأقوى ممّا وُصف**: يبني hook الـ`beforePack` قاعدة من المخطط عبر `migrate diff --from-empty` (لا ينسخ قاعدة قائمة أبداً)، ثم يمرّ على كل جدول ويرمي خطأً إذا كان عدد صفوفه ≠ صفر.

## ما أُصلح في هذه المراجعة

### ١. التدقيق الأمني — نُفِّذ لأول مرة

`pnpm audit` يخرج بالرمز **124** وبصفر بايت على هذا الـworkspace (عطل في pnpm 8)، **والسجل سليم ويستجيب خلال 723ms**. استُخرجت النتيجة من واجهة تحذيرات npm الجماعية مباشرةً على شجرة الإنتاج (٤٦٧ حزمة).

| | حرج | عالٍ | متوسط | منخفض | المجموع |
|---|---:|---:|---:|---:|---:|
| قبل | **2** | 31 | 30 | 6 | **69** |
| بعد | **0** | 3 | 3 | 1 | **7** |

> أُعيد التدقيق بعد هجرة React 19 على الشجرة الجديدة (466 حزمة إنتاج): **النتيجة نفسها — 7 تحذيرات، صفر حرج.**

**طريقة التنفيذ (لإعادة الإنتاج):** `pnpm --filter web list --prod --depth Infinity --json` ثم استعلام واجهة npm الجماعية `/-/npm/v1/security/advisories/bulk`. **لا يصلح `pnpm audit` في CI كما هو** لأنه يخرج بالرمز 124 على هذا الـworkspace؛ من يعيد تشغيله سيحصل على فراغ فيظنّ التدقيق غير منفَّذ.

**تنويه على التصنيف:** ليست الـ69 كلها في مسار الطلب؛ جزء منها أدوات بناء تصل كتبعية عبور لحزمة إنتاج (مثل `tailwindcss` عبر `uploadthing`). لذلك عولجت ثغرات مسار الطلب أولاً.

أخطر ما عولج: **تنفيذ شيفرة اعتباطية في `protobufjs`** (واصل عبر `@google-cloud/vision`)، وثغرات libvips في `sharp` (مسار تحسين صور Next)، وXSS في `dompurify` (مسار توليد PDF).

طُبّقت ١٦ `pnpm.overrides` بعد التحقق من قيد كل تبعية — مثالاً: Next يعلن `sharp: "^0.34.3 || ^0.35.4"`، فالترقية مدعومة رسمياً لا مفروضة. **لا يوجد أي تجاوز لـ`react`/`react-dom`** تجنّباً لتعارض Next↔Expo المعروف.

### ٢. أربعة تحذيرات مقبولة عمداً (إصلاحها يكسر أكثر ممّا يصلح)

| الحزمة | السبب |
|---|---|
| `effect` | uploadthing يثبّته **حرفياً** عند `3.17.7` |
| `postcss` | Next يثبّته **حرفياً** عند `8.4.31`؛ التعرّض وقت البناء فقط، لا مدخلات مهاجم |
| `uuid` | التحذير يخصّ v3/v5/v6 مع `buf`؛ وexceljs يستخدم v4 |
| `@tootallnate/once` | منخفض، ويتطلب قفزة إصدار رئيسية |

### ٣. تسريب مخرجات بناء إلى Git

`apps/web/.next-readiness` — **١٫٢ غيغابايت** غير مُدرجة في `.gitignore`؛ أي `git add .` كان سيبتلعها. أُضيف النمط `.next-*/`.

## أدلة الاختبار (نُفِّذت في هذه المراجعة، بعد تغيير الاعتماديات)

| الفحص | النتيجة |
|---|---|
| Vitest — ويب | **27 ملفاً / 444 اختباراً** ناجحة |
| Vitest — سطح المكتب | **3 ملفات / 34 اختباراً** ناجحة |
| TypeScript (ويب) | **خروج 0** |
| Build الإنتاج | **خروج 0** — نجح في 3.2 دقيقة، **293 مساراً**، بلا أخطاء |
| Prisma / Neon | **28 ترحيلاً**، *"Database schema is up to date"* |
| تكامل PostgreSQL | 19 سيناريو (14 `it` + `it.each` بخمس حالات) — يتطلب قاعدة `faramace_readiness` محلية |
| استبانة React | يُنظر قسم «توحيد React» أدناه — صارت **نسخة واحدة** بدل ثلاث |

> ملاحظة: تُشغَّل اختبارات التكامل بإعداد منفصل يرفض `DATABASE_URL` ضمنياً ويشترط قاعدة محلية معزولة — حماية سليمة لقاعدة العملاء.

## التقييم

| المجال | الدرجة | الحالة |
|---|---:|---|
| صحة المعاملات والعزل | 9.5/10 | اختبارات تزامن وتراجع ذرية |
| قاعدة البيانات والترحيلات | 9/10 | 28/28 مطبَّقة؛ يلزم backup/restore موثق |
| الأمن والاعتماديات | **9.5/10** | صفر ثغرات حرجة بعد معالجة 62 تحذيراً؛ 7 مقبولة موثّقة |
| المراقبة والإشعارات | 8/10 | جرس وPush؛ لا outbox وإعادة محاولة durable |
| التشغيل والنشر | 7.5/10 | يحتاج canary وHTTP smoke وrollback drill |

**النتيجة المجمعة: 9.3/10.**

> تصحيح: منحت المراجعة الأولى **9/10** للأمن على افتراض أن التدقيق «ناقص». لم يكن ناقصاً بل غير منفَّذ، وتنفيذه كشف ثغرتين حرجتين. الدرجة الحالية مبنيّة على تدقيق فعلي مكتمل.

## بوابة الإطلاق المتبقية

1. ~~إعادة تشغيل التدقيق الأمني~~ — **منجز**؛ 69 ← 7، صفر حرج.
2. ~~Build وTypeScript والاختبارات~~ — **منجز ومعاد تنفيذه** بعد تغيير الاعتماديات.
3. `pg_dump` واسترجاعه إلى قاعدة منفصلة — منجز في المراجعة الأولى؛ **لم يُعَد التحقق منه هنا**.
4. **نشر canary مع مراقبة الأخطاء ثم اختبار rollback** — لم يُنفَّذ (يتطلب بنية تحتية). لا تُنشر migrations يدوياً خارج pipeline.
5. **outbox/queue للإشعارات** — قرار تجاري؛ يلزم إن كان التسليم المضمون مطلوباً.
6. **تقسيم 219 ملفاً غير ملتزم** إلى commits قابلة للتراجع — لم يُنفَّذ؛ قرار المالك.
   > **قيد إلزامي:** يجب أن يهبط `package.json` و`pnpm-lock.yaml` في **نفس الـcommit**؛ فصلهما يُعيد الـ62 تحذيراً المعالَجة عند أول `pnpm install`.
   > **وقيد ثانٍ:** التجاوزات مكرّرة في `package.json` و`pnpm-workspace.yaml` (الأخير موطنها في pnpm 10+، ويتجاهله pnpm 8 — تحقّقتُ من ذلك عملياً بتغيير قيمة ومراقبة اللوك‑فايل). **يجب إبقاء القائمتين متطابقتين.**

## توحيد React — نُفِّذ ✅

كان المستودع يحمل **ثلاث نسخ فعلية من React 18.2.0**، لأن جذر `node_modules/react` كان محجوزاً بـ19.1.0 من الموبايل مع `node-linker=hoisted`. صار الآن **نسخة واحدة**:

```
apps/web · apps/mobile · apps/desktop · apps/landing  →  react=19.1.0  react-dom=19.1.0
النسخ الفعلية المتمايزة: node_modules/react   (واحدة)
النسخ المعشّشة الخمس: غائبة جميعاً
```

### تصحيح جوهري على تقييم سابق

وُصف هذا الازدواج سابقاً بأنه محصور في شاشة خطأ التطوير ولا يمسّ الإنتاج. **هذا غير صحيح.** كان بناء `apps/landing` ينهار به فعلياً:

```
TypeError: Cannot read properties of undefined (reading 'registerClientReference')
```

لأن `landing` كان يستبين React من `apps/landing/node_modules/react` بينما `react-dom` من نسخة أخرى. أي أن الموقع التعريفي كان **غير قابل للبناء**. وهو يبني الآن بنجاح.

### المفتاح: التثبيت الحرفي لا النطاق

الموبايل يثبّت React عند `19.1.0` حرفياً. كتابة `^19.1.0` في بقية الحزم كانت ستُحَلّ إلى `19.2.8`، فيعود الازدواج بأرقام مختلفة ويبدو الفحص سليماً. لذلك **ثُبِّتت `19.1.0` حرفياً**.

### التغييرات

| النطاق | التغيير |
|---|---|
| web · desktop · landing | react + react-dom → `19.1.0` مثبّت؛ `@types/react` → `~19.1.17`؛ `@types/react-dom` → `~19.1.11` |
| landing | next `14.2.29` → `15.5.25` (شرط قبول React 19) |
| packages/ui | peer → `^18.2.0 \|\| ^19.0.0` |
| 22 ملفاً | `useFormState` (react-dom) → `useActionState` (react) |
| 4 ملفات | حذف سمة `action` ميتة من `<form>`؛ `DeleteButton` زرّه `type="button"` وينفّذ الإجراء بنفسه، فالنموذج لم يكن يُرسَل قط |
| desktop | `RefObject<HTMLInputElement \| null>` — تغيّر أنواع `useRef` في React 19 |
| landing | حذف `ssr: false` من `next/dynamic` في مكوّني خادم — **عطل سابق** لهذه الهجرة كان يمنع البناء |

### عقبتان تستحقان التوثيق

**١. روابط رمزية متقادمة.** سقطت بوّابة التحقق أول مرة لا بسبب الإصدارات بل بسبب روابط تركها pnpm من تثبيت قديم: `apps/desktop/node_modules/react-dom` و`@types/react` في `packages/ui` و`apps/desktop` كانت تشير إلى إصدارات 18. وهي سبب خطأ `LabelPrimitive.Root cannot be used as a JSX component`. **لا ينظّفها `pnpm install` وحده** — يلزم حذفها يدوياً ثم إعادة التثبيت.

**٢. فخّ في تجاوزات الأمان.** الـpnpm العام على الجهاز إصداره **11.1.3**، لكن حقل `packageManager: pnpm@8.15.4` يجعل corepack يفوّض التنفيذ إلى 8.15.4 — وهو يقرأ `pnpm.overrides` بشكل صحيح. غير أن pnpm 11 يحذّر: *"The pnpm field in package.json is no longer read"*. أي أن **رفع `packageManager` وحده يُلغي التجاوزات الستة عشر بصمت**.

الوقاية: كُرِّرت التجاوزات في `pnpm-workspace.yaml` (موطنها في pnpm 10+)، ويتجاهل pnpm 8 المفاتيح غير المعروفة هناك. القائمتان يجب أن تبقيا متطابقتين.

### التحقق بعد الهجرة

| الفحص | النتيجة |
|---|---|
| بوّابة نسخة React الواحدة | ✅ نسخة واحدة؛ النسخ المعشّشة الخمس غائبة |
| web — tsc / اختبارات / build | 0 · 27/444 · **نجح، 293 مساراً** |
| desktop — tsc / اختبارات | 0 · 3/34 |
| landing — tsc / build | 0 · **نجح** (كان ينهار) |
| خادم التطوير | Ready 6.4s؛ المسارات 200/302/302؛ صفر تحذيرات ازدواج |
| تجاوزات الأمان الـ16 | سليمة بعد أربع عمليات تثبيت |

> `lucide-react@0.300.0` يبقى تحذير peer وحيداً (نطاقه يقف عند React 18). بيانات وصفية لا تؤثر في استبانة React، وتُركت عمداً خارج النطاق لأن القفز إلى 1.42 يمسّ أسماء أيقونات عبر أربع حزم.
